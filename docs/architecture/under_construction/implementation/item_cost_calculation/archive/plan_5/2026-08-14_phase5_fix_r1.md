---
plan: phase 5 (valuation surface)
role: implementer
round: 2 (fix cycle r1 — resolve review r1: B1–B4, S1–S5, N1; corrections pre-verified)
date: 2026-08-14
---

# Session prompt — phase 5 fix cycle r1

You are the **fixing agent** for phase 5. Every correction was EXECUTED by the
r1 reviewer (each gap proven by a probe that stayed green, or by a reproduced
defect with its one-line fix measured). **Resolve, don't relitigate.**
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(commands from `backend/app/`, per master plan §10).

## Doctrine (read by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md` — fix-cycle variant.

## Gate check

- Tracker: phase 5 `CHANGES_REQUESTED` (review r1). Zero owner cards.
- Read the review r1 handoff
  (`handoffs/reviewer/2026-08-13_phase5_review_r1_handoff.md`) and the plan's
  **"Fix r1 amendments" block (2026-08-14) — GOVERNING**. The corrections are
  verbatim there; this prompt only sequences them.

## Items

1. **B1 (production, ONE line):** add `ItemValuation.is_deleted.is_(False)` to
   `delete_item_valuation.py`'s item-scoped current-row select — INV-V1's full
   predicate. This fixes a REAL reachable defect (delete-then-reset → the new
   price can never be deleted; the unfiltered scalar can return the ghost row).
2. **The shared delete-then-reset fixture** (B1's arbiter + S3's C4 row + S2's
   C6 rows + L13's predicate proof) — build once, use everywhere: two
   `superseded_at IS NULL` rows / exactly one INV-V1-current; re-set row
   deletes successfully; three-supersession history ordered
   `created_at DESC, client_id DESC`, byte-identical re-read.
3. **B2:** C5 rebuilt as the 12-value parametrized enumeration (ids name
   authority rows; the six missing status rows; recorded judgments for the
   task-scoped and INV-G3-unreachable values; every row asserts null numerics
   where owed AND `item_cost_evaluations` unchanged).
4. **B3:** the L15 structural test (`inspect.getsource` sweep; snapshot column
   readable only inside `resolve_major_category`).
5. **B4 (production):** currency check reduced to the two independent clauses
   (P-AA); parametrize ids renamed to the STATE each fixture holds; the three
   fixtures themselves stay.
6. **S1:** the purchase-cost/currency-mismatch precedence fixture.
7. **S4:** race blocks count INV-V1-current rows `== 1`; path (i)'s observable
   asserted.
8. **S5:** C3's missing-currency reject row + three accept rows + the phase-2
   node-id citations.
9. **N1:** the L12 fixture's persisted rate becomes `13.0000`, expected
   allowance `76923.08` — both M10 AND M10b forms must then redden.

## Ledger (P-I sixth extension — the rule this phase earned)

**One row per named mutation, no exceptions:** B1-revert, M4, the two 2-clause
currency drops, M3.2, M8, M8b, M10, M10b — plus re-runs of every r1 ledger row
whose file this fix touches (M7a/M7b/M9/M11/M12 live in changed files). Full
observed red sets from the runs that produced them; divergence from prediction
flagged; sha256 pairs copy-pasted, never retyped. Cite the checkpoint's FINAL
hash in the handoff.

## Perimeter

Production: `delete_item_valuation.py` (B1 line),
`domain/item_economics/configuration.py` (B4 clause reduction) — nothing else.
Tests: the phase-5 test files (`test_valuation_surface.py`,
`test_configuration.py`, `test_item_economics_requests.py`,
`test_item_economics_router.py` if S4/S5 touch it). Docs: master plan tracker
row; plan Review log; your handoff. **Archgraph: ZERO delta this cycle** —
state it read-only (N6/N7 belong to the coordinator's post-approval pass; the
2 valuation rows of N4 residue are the CLOSEOUT's purge, not yours).

## Closing protocol

1. All rows green; the ledger per the rule above.
2. Full suite: expect 1950+N passed / 23 byte-identical / 1 deselected (state
   the true numbers READ OFF THE RUN — r1's arithmetic was derived, not read);
   race subset twice, residue scope named; ruff; DB at head `5caae620088c`.
3. Tracker → `IMPLEMENTED` (fix r1); Review log per P-L.
4. Checkpoint `CHECKPOINT (not approved): item-cost phase 5 fix r1 —
   <summary>`; handoff AFTER, citing the final hash, at
   `docs/architecture/under_construction/implementation/item_cost_calculation/handoffs/implementer/2026-08-14_phase5_fix_r1_handoff.md`
   (full path). `⚠ OWNER DECISIONS REQUIRED (n)`; full write perimeter +
   probe declaration.
