---
plan: 2
role: implementer
round: 2 (fix)
date: 2026-08-17
pipeline: simple_production_budget_division
---

# Fix round 2 — plan 2 (review r1 findings)

You are the implementer (implementation-executor doctrine). Round 1 is **substantially
correct and stays**: P-AGREE and P-SUM3 were verified exact in all eight M3.5b branches,
HC-6 holds mechanically, the wire is time-only and role-flat, and T7b changed exactly its
two rows without loosening anything. **Do not redesign any of that.**

This round is **delta-scoped**: seven findings, six files. Five of the seven are *guards
that do not guard* — the code is right and the test cannot fail. Treat "make the test able
to fail" as the deliverable, not "make the test pass".

## Read first

1. `…/handoffs/reviewer/2026-08-17_phase2_review_r1_handoff.md` — **B1, S1–S6 and the
   "Fix work list"**. Its "Verified correct (settled ground)" section is exactly that: do
   not re-derive it.
2. `…/planning/intention.md` §12.5 at **round 11** — M3.4's new *enforcement note* and
   M3.5b's new *exact comparison* paragraph are the two contract changes you implement.
3. `…/planning/owner_decisions.md` — **D16** (owner-ratified `share_state` reading) and the
   **D12 reachability adjudication** (the owner's challenge, upheld, with the one case that
   still breaks).
4. `…/plans/plan_2.md` — criteria rewritten this round: **C1a/C1b, C6a/C6b/C6c, C9,
   C25a/C25b, C27**. These carry their named mutations inline.
5. `…/master_plan.md` §6 — **four rules earned this phase**, especially the
   **precedence-disagreement rule**, which is the shape of five of these findings.

## The work

- **B1 (was blocking, now should-fix — still fix it).** `_governing_step`
  (`budget_division.py:181-199`) never consults liveness; it returns the most recently
  *created* step, and the documented tie-break is inverted. **Partition first, sort
  second:** prefer steps with `state NOT IN TERMINAL_STEP_STATES` (imported per B7); if any
  exist choose from that subset, else from the closed ones. Within the chosen subset apply
  M3.4's stated order — `entered_at` DESC, then `created_at` DESC, then `client_id` ASC
  (i.e. swap the last two sorts). `:239`'s call passes only completed steps: it needs the
  precedence fix, not the partition.
  **Why this matters even though the count is zero:** the owner confirmed the ordering has
  been protected *by the frontend*, not by the backend. An invariant enforced only by a
  client is not enforced.

- **S1 / D16.** `budget_division.py:362` becomes
  `section_state = "over_share" if worked > allowance else "on_track"`; delete
  `worked_for_share` (`:357-361`), which then has no reader. `left_seconds` does **not**
  change. M3.5b's exclusion rule still governs `_section_step_allowances` (`:223`) — that
  code is correct and untouched.

- **S2 → C1b.** Build the reversal clause that was specified and never built.

- **S3 → C6a/C6b/C6c.** C6's DB row must bite on the liveness mutation (r1's survived it);
  `state_entered_at` needs its first test anywhere; C6c's fixture must be non-vacuous.

- **S4.** Read `section_name_snapshot` from the **governing step** (`governing` is already
  in hand at `:151`), falling back to first-non-null only when the governing step's is
  null. Independently, give E3's step select a deterministic `order_by`
  (`get_task_production_time.py:28-38` has none) — it costs nothing and removes the class.

- **S5 → C27.** Restate the P-PROP row at the section unit, renamed, with a comment citing
  §12.6 P1. **Strengthen; never loosen.**

- **S6.** Add `.archgraph/architecture.yml` to the handoff's declared file list. Standing
  rule now: **tool-recorded state is declared by path**, not narrated in prose.

## Perimeter — nothing outside these may change

`budget_division.py` · `test_budget_division.py` · `test_production_time_query.py` ·
`test_budget_allocations_query.py` (S5 only, by strengthening) ·
`get_task_production_time.py` (S4's `order_by` only) · the handoff's perimeter list.

If a fix appears to require a file outside this list: **STOP and report.**

## Discipline

- Every named mutation in the rewritten criteria must be applied at the **definition
  site**, observed red, reverted, and the file confirmed `sha256` byte-identical. Record
  the **observed output**, not a paraphrase — r1's handoff summarised its probes and five
  of six turned out reproducible, but one (C1's) was green and the summary hid it.
- **Precedence-disagreement rule:** every fixture that pins a precedence must make each
  level of that precedence disagree with the others. A fixture where two independent
  causes both produce the expected value is not a test.
- **Criterion-kind rule:** where a criterion has both a unit and a DB row, record **which
  test bites on which mutation**.
- No weaker assertions anywhere. Loosening to make something pass is a failed round.
- The three phase-1 rows that must stay byte-identical after B1's fix (`:25`, `:72`,
  `:208`) were verified unaffected by the equal-weights split — re-verify.

## Suite

From `backend/app/`: `PYTHONPATH=. pytest -m 'not e2e'`.
Start point: **2311 passed / 26 failed / 1 deselected**, the 26 IDs byte-identical to the
phase-1 closeout set. **Diff failure IDs, never totals** — one run in three has been
observed at 25 failed and the drifting test is not identified.

Note the configured DB accumulates ~24 `task_steps` across ~17 tasks per full-suite run
(master plan §7). Row counts drift between runs; never read a changed count as evidence of
a code change.

## Checkpoint and handoff

Commit with subject `CHECKPOINT (not approved): plan2 fix r2 — governing step, share_state, guards`.
Handoff → `…/handoffs/implementer/2026-08-17_phase2_fix_r2_handoff.md`: frontmatter,
checkpoint hash, **full write perimeter declared by path including any tool-recorded
state**, suite totals + failure-ID diff, observed-red output per named mutation, a
finding-by-finding table (B1, S1–S6 → what changed → which test bites), and a
**DECISIONS I HAD TO MAKE** section for anything the artifacts did not settle.
