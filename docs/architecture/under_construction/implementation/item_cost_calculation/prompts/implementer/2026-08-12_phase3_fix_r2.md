---
plan: phase 3 (canonical calculator)
role: implementer
round: 2 (fix cycle)
date: 2026-08-12
---

# Session prompt — phase 3 fix cycle (resolve review r1: B1, B2, S1–S3 + absorbed guards)

You are the **fixing agent** for phase 3. **Resolve, don't relitigate; add nothing
beyond the findings and the routed card decisions.**
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(branch `fix/idempotent-completion-analytics`). Project folder:
`docs/architecture/under_construction/implementation/item_cost_calculation/`.

## Doctrine (read by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md` — fix-cycle variant.

## Gate check

- Tracker: phase 3 `CHANGES_REQUESTED` (reviewer r1).
- Read the FULL reviewer r1 entry in the plan's Review log and the handoff
  `handoffs/reviewer/2026-08-12_phase3_review_r1_handoff.md`. The owner answered
  all three cards (recorded in `planning/owner_decisions.md`; folded as intention
  round 9): card 1 → internal integrity alarm; card 2 → guards absorbed; card 3 →
  graph node held (not your concern).

## Findings to resolve (correction clauses verbatim)

### B1 — Decimal arithmetic outside `localcontext()` (blocking)

> wrap both in `with localcontext() as context: context.prec = 50`, as at the five
> Q sites; and extend C9's baseline/hostile tuples to **every** public function
> performing Decimal arithmetic (`calculate_remaining_worker_minutes`,
> `calculate_variance_worker_minutes`, `calculate_percent_consumed`). Named
> mutation for the new row: "remove the `localcontext()` wrapper from
> `calculate_remaining_worker_minutes`" must redden it.

### B2 — the missing C6 cell: money × None (system-supplied) (blocking)

> add the cell against a system-supplied money parameter (e.g.
> `calculate_variance_cost_minor(None, 100)` or
> `calculate_cost_per_worker_minute(None, …)`), and per charter rule 11 name the
> mutation it must survive: "`_require_money`'s system-supplied `None` branch
> returns 0".

(That mutation currently leaves 54/54 green — the inferred zero P-B forbids. Your
new row must redden it.)

### S1 — C8's disjunction assertions

> per row, assert both distinct currency values by name and the exact
> failing-pair label.

(P-O now binds: no `or` between the two values.)

### S2 — the unregistered mismatch identity (carrier change per owner card 1 / R9-1)

Replace the `ITEM_COST_SNAPSHOT_MISMATCH` `ValidationError` raises
(`calculator.py:397, 413, 418, 425`) with the **`REDERIVE_MISMATCH` structured
result** per intention §6A.11 as amended round 9: a named module-level marker
(sibling of `REDERIVE_SKIPPED`) carrying the disagreeing fields and both values;
`rederive` never raises on mismatch. Update C7's rows accordingly (mismatch row
asserts the marker and its payload, not an exception).

### S3 — the version-constant row under-asserts

> assert a distinctive token from each list, against the docstring the criterion
> means.

(Both lists: a bump token AND a never-bump token — e.g. `renames` / `widening` —
read from the docstring that actually carries §6A.10's contract.)

### Absorbed guards (owner card 2 / R9-2 — now REQUIRED rows, not optional)

- Negative `percent_value` → `ITEM_COST_TERM_SHAPE_INVALID`; negative
  `fixed_amount_minor` → same (both branches of `_term_shape`'s range check —
  one row each, exact identity asserted).
- Zero rate reaching `calculate_allowed_worker_minutes` →
  `ITEM_COST_RATE_UNDERFLOW` (one row, exact identity).

### N2 — public surface (registry decision folded into §6.5)

Add `__all__` = the 16 API names + `EvaluationSnapshot` + `TermSnapshot` +
`REDERIVE_SKIPPED` + `REDERIVE_MISMATCH`. One test row asserts `__all__` equals
exactly that set.

### Optional (fixer's judgment, documented if taken)

- **N1:** delete the byte-identical duplicate test (one of the two).
- **N5:** remove `_require_rate`'s caller-less `required=False` path (rule 4).
- **N6:** move C2's collection-time fixture evaluation inside test bodies (or
  freeze parametrize ids) so mutation runs read cleanly.

## Allowed file perimeter (the re-review verifies exactly)

- `app/beyo_manager/domain/item_economics/calculator.py`
- `app/tests/unit/domain/item_economics/test_calculator.py`
- the plan's Review log + master-plan tracker row (Note append; stamps preserved)
- your handoff

Nothing else — no other production file, no models, no phase-2 artifacts.

## Closing protocol

1. All criteria green incl. the new/changed rows; **every mutation named above run
   at its site, reverted, sha256-verified, declared per-mutation with reddened row
   ids** (P-I). Per P-L, state what was built per finding.
2. Full suite green: expect 1738+N passed / 23 failed / 1 deselected (failure set
   byte-identical; N14 flake caveat applies).
3. Archgraph: zero delta this cycle — state it (the pending node is held per
   card 3; do not touch it).
4. Tracker row → `IMPLEMENTED` (fix r2); Review log entry.
5. Checkpoint: `CHECKPOINT (not approved): item-cost phase 3 fix r2 — <summary>`.
   **Cite the FINAL hash in the handoff** (do not amend after citing).
6. Deposit the handoff at
   `docs/architecture/under_construction/implementation/item_cost_calculation/handoffs/implementer/2026-08-12_phase3_fix_r2_handoff.md`
   (full path): summary; `⚠ OWNER DECISIONS REQUIRED (n)`; finding-by-finding
   resolution; mutation declarations; final file sha256s; full write perimeter
   incl. the final checkpoint hash. **Deposit before ending the session.**
