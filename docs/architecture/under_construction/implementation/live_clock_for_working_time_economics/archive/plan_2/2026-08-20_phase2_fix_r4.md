---
plan: 2
role: implementer
round: 4
date: 2026-08-20
project: live_clock_for_working_time_economics
---

# Session prompt — plan 2 fix (round 4), `live_clock_for_working_time_economics`

## 1. Standing

Review r3 was the phase's first full review. Its verdict: **the production code is
correct** — a second independent pass re-derived the fold, the composition and the batch
path, walked all four payloads of one fixture side by side, and found every
worked-derived field agreeing on one number. That is the phase's purpose, and it is met.

Fix r2's work also held up: the reviewer re-measured three of the fourteen ledger rows
whole-suite and all three reproduced **ID-for-ID**.

What remains is one blocking gap and four should-fix items, plus one that is not
strictly a defect today but will become a failure on a date.

**Perimeter for this round:**

- `app/tests/integration/services/queries/item_economics/test_phase2_live_surfaces.py`
- **Exactly two production edits, both named below and nothing else:**
  - the two D7 comments (S4) — comments only, no logic;
  - one token in `get_working_section_typical_times.py:typical_times_statement` (N4).

Any other production line is an automatic finding at re-review. If you believe a finding
requires more, stop and report instead of doing it.

Doctrine: read `/Users/davidloorenz/agent-skills/pipeline-charter.md` and
`/Users/davidloorenz/agent-skills/implementation-executor.md` by absolute path. Your task
list is `plans/plan_2.md` — **§5's C2, C4, C6, C7 and C11, and §7's four consumption
entries**. Where this prompt differs from the plan file, the plan file wins.

**External streams:** master plan §7 lists them. The cap stream commits into this repo
while you work; its files are foreign-but-expected. **A golden JSON moved by anything but
your own work is an escalation — stop and report.** And per the rule earned this round:
**a mutation sweep is not a round.** Record the tree (`git rev-parse --short HEAD` plus
your own clean count) that **each** ledger row was measured at; if a foreign commit lands
mid-sweep, re-base the sweep rather than annotating it.

## 2. Blocking

### B1 — the two frontend criteria adopted as contract are not discriminated

`planning/intention.md` §5.2 criteria 1 and 2 are adopted verbatim as this pipeline's
contract. Neither has a test that can currently fail.

**(a) Criterion 1 — `share_state` consistent with the figures beside it.** The only
served-payload `share_state` assertion is in
`test_c2_c3_c7_live_payloads_reconcile_and_worker_face_stays_money_free`, and its
section's `allowance_seconds` is **0** (the excluded steps charge 1440 s against a 1200 s
budget, so `distributable_seconds = max(0, −240) = 0`). Coordinator-measured on that
fixture:

| Basis | `allowance_seconds` | `worked_seconds` | `share_state` |
|---|---|---|---|
| Live (shipped) | 0 | 2040 | `over_share` |
| Settled (phase reverted) | 0 | 1440 | `over_share` |

**The row returns the same verdict with the entire phase's change undone.** Plan §5 C2
asked for "a section 25 minutes into a 3 m 6 s allowance" with exact expected integers;
what shipped is a section with no allowance at all.

*Required:* a fixture with a **positive** section allowance where the settled basis is at
or under it and the live basis is over it. Assert the exact integers for
`allowance_seconds`, `worked_seconds`, `left_seconds` **and** `share_state`.
**Named mutation, call site, `get_task_production_time.py:get_task_production_time`
substitution site:** build `DivisionStep(total_working_seconds=step.total_working_seconds)`
instead of `live_seconds[step.client_id]`. **Both sides:** contract
`share_state == "over_share"`; mutation `== "on_track"` ⇒ red.

Note the general rule this earned (master plan §5): **the degenerate term is not always
the addend.** Here the arithmetic was fine and the *allowance* was degenerate, so a
comparison-based verdict returned the same category on both sides. When a criterion
asserts a categorical field, the mutation must move the **category**.

Do **not** try to rescue the companion assertion
`left_seconds == allowance_seconds − worked_seconds`: `divide_production_budget` emits all
three from one dict literal off the same locals, so no single-site mutation can falsify
it. It is a tautology; leave it, and do not count it as coverage.

**(b) Criterion 2 — two calls a few seconds apart differ only in time-dependent fields.**
Plan §5 C4 states two contracts; only "loader invocations == 1" shipped. Nothing serves an
endpoint **twice** and compares payloads, and C1's goldens cover only the idle case.

The reviewer constructed and measured the property: with `ctx.now` frozen and an open
working record, serving E-P twice and E-A twice yields byte-identical payloads. **It is
unwritten, not unwritable.**

*Required:* one byte-identity row per endpoint at a frozen `ctx.now` with an open record.
**Keep E-A's call single-task** — its task `SELECT` carries no `ORDER BY`, so a batched
byte-comparison is order-luck (the hazard already recorded for phase 1's E-A golden).

This matters beyond this phase: plan 3 edits both payload builders, and this row is the
only guard that would see an open-record determinism regression there. The goldens cannot.

## 3. Should-fix

**S1 — C6 row 1 shipped its headline and dropped all three honest-form clauses.** Add:
(i) the assertion that **no excluded step in the fixture holds an open working record**;
(ii) `charged_seconds` computed from settled values, asserted on the division input;
(iii) the `typical` blocks byte-identical (path 3, §4.3A — the plan's own "expensive
mistake", and nothing else in the phase guards it).

Clause (i) is load-bearing: `divide_production_budget` sums `total_working_seconds` over
**excluded** steps into `charged_seconds`, which on substituted rows is now the **live**
figure, so an excluded step holding an open record would make every `allowance_seconds` in
its section tick downward. **There is no live defect** —
`_step_transition_core.py:_apply_step_transition` closes the open record unconditionally
on every transition — and the intention now carries this as **§4.1A C.1** (round 4f). The
assertion's job is to pin it.

**S2 — C7's two dropped clauses.** (a) The plan requires the worker face's five fields to
equal the manager face's **and be greater than the settled-basis values** — as shipped,
both faces silently returning the settled basis satisfies the row. (b) The plan requires
walking **every key** of `serialize_task_budget_status(...)` and cites
`test_production_time_query.py:test_c14_c16_flat_time_only_degradation_and_tenant_boundary`,
which uses a **recursive** `walk()`. What shipped iterates `worker_payload["result"]`
flat, leaving the top level unguarded — including the `include_monetary=False` branch's own
`payload["allowed_worker_minutes"]` assignment. Nothing is hidden today; the guard is
simply off the surface a regression would land on. Use the recursive form.

**S3 — the C11 shim row expires on 2026-11-17 and would join the published baseline.**
`test_c11_typicals_compatibility_shim_keeps_five_sample_median` seeds
`closed_at = now − 1 day` where `now` is the hard-coded `datetime(2026, 8, 20, 12, 0, UTC)`,
then calls `typical_times_statement(workspace_id)` **without** `now` — correctly, since the
row exists to exercise the wall-clock branch. So the cutoff is `real_now − 90 days` against
filter `latest_closed_at >= cutoff`, and the row qualifies only while
`real_now <= 2026-11-17T12:00Z`. Coordinator-verified at source; arithmetic checked.

*Required:* derive **this fixture's** `closed_at` from the wall clock, as the house form
two files away already does
(`test_budget_allocations_query.py:116`, `closed_at=datetime.now(timezone.utc) - timedelta(days=1)`).
Keep the call itself argument-free — that is the point of the row.

Treat this as the round's second-highest priority after B1: master plan §7 obligation 7
publishes this tree's enumerated failure-ID set as the next pipeline's reference point, and
this would add a 27th member on a date unrelated to any commit.

**S4 — D7's recording obligation is unmet at both substitution sites.** The strict indexing
shipped correctly (`get_task_production_time.py:54`,
`get_task_budget_allocations.py:221`) but **neither carries the comment D7 required**. The
handoff records it, and handoffs archive. Name the consequence in the comment: strict
indexing is a deliberate fail-loud choice, and the obvious "fix" for a future `KeyError` —
`live_seconds.get(step.client_id, step.total_working_seconds)` — silently restores the
settled column and masks C3's population row.

**S5 — already applied by the coordinator at the fold.** Plan §7's fix-r2 entry now states
the tree each measurement was taken at, and row 4's unreproducible seven-ID set is struck
in favour of the measured single ID. Nothing owed to you.

## 4. The one other production change — N4

`get_working_section_typical_times.py:29` reads `(now or datetime.now(timezone.utc))`
while its twin at `_common.py:210` reads `now.date() if now is not None else today_utc()`.
The truthiness form is inert (datetimes are always truthy), so this is **not** a
behavioural fix — but **D4 granted one form for both shims** precisely so the codebase
would not grow two conventions for one construct, and it now has two. Change the typicals
shim to the `is None` form. One token. No other line.

## 5. Notes — recorded so you do not re-file them

- **N1** — C5's three per-endpoint rows were collapsed into one. The reviewer traced every
  case and there is no hole (autoflush catches E-P/E-B/E-B-worker, the dirty check catches
  E-A); the 5-ID ledger result was reproduced. Diagnosis only. Leave it.
- **N2** — C12's E-P and E-A halves are vacuous by construction (E-A no longer imports
  `today_utc`; E-P's fixture task holds a committed evaluation so it never reaches
  `_load_preview_inputs`). The two real call sites **are** covered on the
  `unevaluated_task` branch. The criterion is satisfied where it can be; the plan's
  four-surface phrasing over-claims. No test change.
- **N3** — D5's stub comment sits beside the wrong stub. Move it to the
  `monkeypatch.setattr(typicals_module, "datetime", NoClock)` in
  `test_c11_c12_surface_call_sites_do_not_fall_back_to_module_clocks`. One comment.
- **N5** — the price-scenario now pays a step load plus probe per request, exactly as
  intention §4.1A D predicted, and no worked-derived key reaches its payload. Closeout
  will add it to plan §6's cost note. Not yours.
- **N6** — `test_c11_typicals_statement_uses_the_request_clock_when_supplied` is marked
  `@pytest.mark.unit` inside `tests/integration/…`. Fix the marker if it costs you one
  line; leave it otherwise.

## 6. Ledger obligations

Per the rule earned this round, **every row states the tree it was measured at.** Rows
owed this cycle — the new and changed ones only, not a re-sweep of all fourteen (master
plan §5, narrowed mutation set):

- B1(a)'s settled-substitution mutation, both sides, with the category moving.
- B1(b)'s byte-identity rows: state how you established non-vacuity.
- S1(i)'s assertion: name what would have to change for it to fail.
- S2(a)'s greater-than clause: the settled-basis mutation from ledger row 7 (C7 worker
  aggregate) must now redden it — re-measure that row.
- S3: confirm the row still passes **and** that it no longer depends on the wall-clock
  date, e.g. by demonstrating it under a fixture date far from today.
- N4's token change: confirm ledger row 11 (C11 shim default, future-instant) still
  reddens exactly as before.

Cite fix r2's other rows with their tree provenance; do not re-run them.

## 7. Closing protocol

1. Full suite green except the enumerated baseline; count **and** ID-set diff, with the
   tree recorded.
2. Checkpoint commit at IMPLEMENTED, prefixed `CHECKPOINT (not approved):`.
3. Update only phase 2's tracker row; findings to `plans/plan_2.md` §7.
4. Archgraph: nothing owed unless you touch it; declare it if you do.
5. Handoff at `handoffs/implementer/2026-08-20_phase2_fix_r4_handoff.md`, frontmatter
   `plan: 2`, `role: implementer`, `round: 4`, `state`, `date`, `actor`. Carry: the
   owner-readable opening; `⚠ OWNER DECISIONS REQUIRED (n)`; the ledger with tree
   provenance per row and both sides; a line per finding B1/S1–S4/N4 saying how it closed;
   and your full write perimeter, including every file touched by a probe.
