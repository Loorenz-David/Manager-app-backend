---
plan: phase 8 (status & results)
role: implementer (fix)
round: 2 (fix cycle after re-review r2)
date: 2026-08-15
---

# Session prompt — phase 8 fix r2 (test-side only, five items)

You are the **fixing agent** for phase 8, round 2. Re-review r2:
CHANGES_REQUESTED — 2 blocking / 3 should-fix / 6 notes, all TEST-SIDE.
The production surface is proven correct (19/19 declared mutations bite;
fifteen untouched files byte-verified; the four changed files re-derived
clean). This cycle is five test items and two note ride-alongs. It should
be the shortest cycle of the phase — do not expand it.
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(commands from `backend/app/`, per master plan §10).

## Doctrine (read by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md`

## Ground

- Re-review handoff (read in full):
  `handoffs/reviewer/2026-08-15_phase8_rereview_r2_handoff.md` — its
  ledger carries the two surviving mutations (MX1/MX2) with their exact
  mutant hashes, and S1's flake evidence.
- Plan: `plans/phase_8_status_results.md` — the **fix-r2 block H1–H7 is
  GOVERNING** and is your exhaustive work list; TICK EACH ITEM in your
  handoff (row-list tick rule, §9).
- §9 as of today — note the five NEW r2 rules, especially the
  set-assertion rule (H3 is its application) and P-I 9th as a HARD ledger
  field (your ledger carries per-row mutant hashes, no exceptions).

## Baseline (re-reviewer-verified, foreground)

Head `c1d2e3f4a5b6`; suite **2138 / 23 / 1**, failure set byte-identical
to the phase-1 list; focused 11-path scope **427 / 2** (both established).
Production hashes: the four changed files as recorded in the tracker
(router `799d205d…`, delete_item_valuation `a9a987d7…`, lifetime
`1f26eeca…`, budget_status `5f89e29b…`); all others = r1b finals.

## The work (H-numbers from the plan block — the COMPLETE list)

1. **H1 (B1):** two integration rows driving `get_task_budget_status`
   against committed evaluations — `allowed <= 0` → `INFEASIBLE` +
   `percent_consumed is None`; `> 0` → `OK`. Delete the SimpleNamespace
   echo rows. MX2 (`57c4591f…d2c140`) must redden the INFEASIBLE row.
2. **H2 (B2):** one consumption fixture gains nonzero pause / ended-shift
   / inaccurate seconds; assert `actual_worker_seconds` == working seconds
   alone. MX1 (`fdae3c41…b490b7`) must redden it.
3. **H3 (S1):** the two C1 probe rows switch to candidate-SET assertions
   (authorized deviation from verbatim adoption — the defect is the
   reviewer's own, correction specified in their handoff). M1/M2/M3 must
   still bite. Run the two rows 10× to demonstrate the flake is gone
   (state the loop command and the 10/10 result).
4. **H4 (S2):** rebuild the C5 supersession row — superseding version with
   a DIFFERENT rate after close; §8A.4 set byte-identical; non-vacuity
   assertion that the new config would have changed the number.
5. **H5 (S3):** C3's second step moves to a second task with its own
   evaluation; each episode == 1800; Σ == wall clock.
6. **H6:** N1 count-not-scalar; N2 widen the `response_model is None` row
   over all `item_economics.router.routes`.
7. **Mutation ledger:** MX1, MX2 (must now bite — copy their mutant hashes
   from the r2 handoff and state reproduces/differs), plus regression
   re-runs of M1/M2/M3 (H3 touched their arbiters) and M17 if H4's file is
   shared. Per row: site → expected red id → sha256 pair (before AND
   mutant) COPY-PASTED → observed red → reverted. ZERO deferrals.

## Scope fence

Tests ONLY: `test_phase8_reviewer_r1_probe.py` (H3's two rows — nothing
else in the file), `test_phase8_status_results.py` (H1/H2/H4/H5/N1),
`test_phase8_serializers.py` (H1 echo-row deletion if they live here),
`test_item_economics_router.py` (N2). Docs: master plan tracker + plan
Review log + your handoff. **ZERO production edits (H7)** — a production
edit is stop-and-report. No migration. Graph READ-ONLY, zero delta.

## Closing protocol

1. All rows green; the ticked H1–H6 list; the mutation ledger with
   per-row mutant hashes (P-I 9th hard field).
2. Suite: expect 2138±N / 23 byte-identical (sorted diff, say so) / 1
   deselected, numbers READ off YOUR foreground run; the H3 rows 10/10;
   committing subsets twice, residue scope named; ruff on changed files;
   DB at head `c1d2e3f4a5b6`.
3. Tracker → `IMPLEMENTED` (fix r2); Review log per P-L; final sha256 for
   every TEST file touched (no production hashes change — say so).
4. Checkpoint `CHECKPOINT (not approved): item-cost phase 8 fix r2 —
   <summary>`; handoff AFTER (never inside), at
   `docs/architecture/under_construction/implementation/item_cost_calculation/handoffs/implementer/2026-08-15_phase8_fix_r2_handoff.md`
   (full path). `⚠ OWNER DECISIONS REQUIRED (n)` if any arise; full write
   perimeter + probe declaration.
