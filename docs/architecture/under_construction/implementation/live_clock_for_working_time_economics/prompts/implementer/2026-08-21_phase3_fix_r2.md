---
plan: 3
role: implementer (fix)
round: 2
date: 2026-08-21
project: live_clock_for_working_time_economics
---

# Session prompt — plan 3 fix r2, `live_clock_for_working_time_economics`

## 1. Role and scope

You are running **one fix cycle** against review r1's findings. Resolve them; do not
relitigate them; **add nothing beyond them**.

Workspace root: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
Application root: `backend/app/` (suite: `PYTHONPATH=. pytest -m 'not e2e'`)

**Read first, by absolute path, and follow as doctrine:**
1. `/Users/davidloorenz/agent-skills/pipeline-charter.md` — including the
   **"Test-evidence scope and reuse"** section, which governs §5 below.
2. `/Users/davidloorenz/agent-skills/implementation-executor.md`

Then: `handoffs/reviewer/2026-08-21_phase3_review_r1_handoff.md` (the findings),
`plans/plan_3.md` §5 C6a/C6b/**C6c** and §5B (both amended today — the plan file wins over
this prompt wherever they differ).

**Verdict context:** 0 blocking, 2 should-fix, 6 notes. **The production code is correct
and no production line is in scope.** Both should-fix findings are in the proof.

## 2. Allowed file perimeter — the re-review verifies this

- `app/tests/integration/services/queries/item_economics/test_phase2_live_surfaces.py`
- `app/tests/integration/services/queries/item_economics/test_production_time_query.py`
  (**one comment only**, N4 — no assertion changes)
- `plans/plan_3.md` (your Review log entry), `master_plan.md` (your tracker row only)
- your handoff file

**Nothing under `app/beyo_manager/`.** If any production file changes, that is an
automatic finding at re-review. The document corrections for S1 are **already applied by
the coordinator** — intention §5.3A, plan §5 C6, master plan §3 — do not touch them.

## 3. S1 (should-fix) — re-specify C6b so its `null` has ONE cause

The review's correction clause, **verbatim**:

> *"Optionally strengthen C6b by giving its **current** evaluation a positive allowance
> (status `ok`) while keeping the frozen figures summing to `≤ 0`, so its `null` can only
> come from the frozen basis. As written, C6b's fixture satisfies **two independent
> sufficient causes** for `null` — the charter rule-2 companion condition, and the same
> shape as the plan-3 round-2 B1 the charter cites."*

**The coordinator makes this required, not optional**, and the plan now says so. Reason,
measured: C6b sets `values[10].allowed_worker_minutes = Decimal("0.00")`, so its payload's
`status` is `infeasible`, and a status-blanking implementation returns exactly the `null`
C6b asserts — **the row passes under an edit it was documented as stopping.** Its own
comment claims the percentage is *"undefined independently of the current status"*, which
that fixture cannot demonstrate.

What to do: keep frozen `actual 15.00 / variance −15.00` (reconstruction `0.00` → `null`),
and give the **current** evaluation a **positive** allowance so the payload's `status` is
`ok`. Then the `null` can only come from the frozen basis. Update the fixture comment to
state what the fixture now actually shows.

**Named mutation, unchanged (run it at each reconstruction site):** substitute a positive
fallback denominator (`actual` alone) ⇒ contract `null`, mutant a number, red.
**Additionally record the complement** (lesson 1 below): under the **status-blanking**
mutant, state explicitly that C6b now reddens too — that is the whole point of the
re-specification, and "which row did *not* bite" is now a required ledger field.

## 4. S2 (should-fix) — add C6c, the over-budget region

`plans/plan_3.md` §5 now carries **C6c** in full; build it exactly as written there. In
short: stored `actual_worker_minutes = 15.00` / `variance_worker_minutes = −5.00` →
reconstructed allowance `10.00` → frozen **`"150.00"`**, current evaluation left positive
so `status` stays `ok`, asserted on **both** faces as C6a does. **Named mutation at each
reconstruction site:** clamp the frozen percent at `100.00` ⇒ contract `"150.00"`, mutant
`"100.00"`, red.

Why it exists, so you do not weaken it: the reviewer measured that clamping the frozen
percent at 100 leaves the **entire suite green (∅ added, ∅ removed)** — the most
consequential number the frozen block serves, a job that overran its budget, is
constrained by nothing in the repository. It is OD-10's own first premise row.

Do **not** satisfy this row by asserting an inequality (`> 100`) or a range; assert the
exact literal `"150.00"`, both faces.

## 5. N1 and N4 (notes, in scope)

- **N1** — in `test_c3_recommit_changes_live_denominator_not_frozen_percent`, add
  `assert before["budget"]["percent_consumed"] == "120.00"` (live `24.00` against the
  pre-recommit allowance `20.00`). The row currently shows the live percent *landing* on
  `80.00`, never *moving*; in the after state live and frozen coincide, so this makes the
  movement explicit and survives a later fixture change. One assertion; change nothing
  else in that test.
- **N4** — add a **one-line comment** above
  `test_production_time_query.py:test_c17_frozen_final_uses_live_percent_without_money`
  recording that after D9 the equality it asserts holds by fixture coincidence (frozen
  `20.00 / 80.00` reconstructs `100.00`, matching the live `20.00 / 100.00` basis), not
  because the final block tracks the live percent. **No assertion changes.** The reason
  is the medium: the correction currently lives in a pipeline document that a reader of
  this test file has no reason to open.

**N2, N3, N5, N6 are record-only** — no code, no test change. Do not act on them.

## 6. Testing — scope per hypothesis

Charter test-evidence section governs; `plans/plan_3.md` §5B assigns scopes.

- New and changed rows are **L1** questions ("does this named row redden under this named
  mutation") — run them at L1. C6c's justifying ∅ was an L4 absence claim and is
  **already measured and recorded**; do not re-spend it.
- **One authoritative L4 stamp at the close of this cycle**: full suite + ruff, tree
  identity recorded, both-direction ID delta against master §6's enumerated 26. Expect
  **+2 passed** over `26 / 2486 / 1` if you add exactly C6c and keep C6b one row.
- **Do not re-run** measurements this round did not change. The implement-r1 stamp and its
  mutation rows stand and are citable — cite them.
- Every evidence record: hypothesis · scope · command · tree identity (SHA + clean
  `git status --porcelain`; dirty ⇒ add a `git diff` digest) · result · ID delta both
  directions. **New required field this round:** where a plan divides labour between two
  rows, state which row did **not** bite, not only which did (review lesson 1 — S1 was
  sitting inside a ledger row that answered only half the question).
- If a count disagrees with baseline, **capture the failing-ID set first, then repeat**.

## 7. Closing protocol

1. The cycle's one L4 stamp (above).
2. Every named mutation for the rows you touched, at its named site, **per reconstruction
   site**, each a full evidence record, each reverted and the revert verified.
3. Tracker row → `IMPLEMENTED`, **your row only**, one line with counts.
4. Review log entry in `plans/plan_3.md`: what changed, the C6b re-specification and what
   its fixture now proves, and your judgment calls.
5. Handoff at `handoffs/implementer/2026-08-21_phase3_fix_r2_handoff.md` with the charter
   row schema, declaring your **cycle-scoped** write perimeter — the files *this session*
   changed, not the files the phase touches — plus every file a mutation probe touched,
   listed separately. Owner questions, if any, as decision cards; one line if none.
6. Checkpoint commit, subject prefixed `CHECKPOINT (not approved):`.

Where this prompt and `plans/plan_3.md` differ, **the plan file wins**.
