---
plan: 2
role: implementer
round: 4
date: 2026-08-21
project: test_isolation_and_xdist
---

# Session prompt — plan 2 fix r4, `test_isolation_and_xdist`

## 1. Role and mode

Finding-scoped fix cycle closing review r3. Resolve **B1, B2, S2, S3, S4, S5** and notes
**N1, N2, N6, N7, N9**; N3 and N4 are explicitly out of scope (§8). **Resolve, don't relitigate.
Add nothing beyond the findings.**

Workspace root: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
Suite from `backend/app/`: `PYTHONPATH=. pytest -m 'not e2e'`
Reversed: `BEYO_TEST_COLLECTION_ORDER=reverse PYTHONPATH=. pytest -m 'not e2e'`
Branch `feat/test-isolation-xdist`, HEAD's `app/` identical to checkpoint `8429442`.

**Read first, by absolute path, as this session's doctrine:**
`/Users/davidloorenz/agent-skills/pipeline-charter.md` (including **"Test-evidence scope and
reuse"**) and `/Users/davidloorenz/agent-skills/implementation-executor.md`.

**Read then:** `handoffs/reviewer/2026-08-21_phase2_review_r3_handoff.md` in full — it carries
every finding's measurement and exact line references — and `plans/plan_2.md` §7's review-r3
entry. **`plans/plan_2.md` is your task list**; where this prompt differs, the plan file wins.

## 2. Allowed file perimeter

The re-review verifies this against `git diff`; anything outside is an automatic finding.

- `app/beyo_manager/config.py` — **B1 only**, one field. See §3.
- `app/tests/database_isolation.py`
- `app/tests/conftest.py`
- `app/.env.example`
- `app/tests/integration/infrastructure/test_database_isolation.py`
- `app/tests/integration/services/queries/users/test_list_users_floor_identification.py` (N9)
- `app/tests/integration/services/commands/task_steps/test_add_task_steps_integration.py` (N6)
- `plans/plan_2.md` (Review log + frontmatter) and your handoff

## 3. B1 (blocking) — the slot is unreachable through the surface that documents it

`resolve_test_slot` (`database_isolation.py:33-40`) reads `os.getenv`. The only documentation of
the variable is `.env.example:8-10`. `.env` is parsed by pydantic-settings
(`config.py:135-139`), which **never populates `os.environ`**. Measured twice independently:
with `BEYO_TEST_SLOT=shopify` in the parsed `.env`, `os.getenv` returns `None` and the resolver
yields `beyo_test_main_main` — so the second checkout drops the first's database mid-run, which
is intention §5's hazard surviving behind a variable that looks set.

**Owner decision OD-7 selects the shape. The correction, verbatim:**

> Add a `BEYO_TEST_SLOT` field to `app/beyo_manager/config.py` and resolve the slot from settings
> with an `os.environ` override, so that **both** `.env` and an exported variable work. This is
> the one production file this phase may touch, under the carve-out `plans/plan_1.md` §3 wrote
> before phase 1 started: *"Nothing under `app/beyo_manager/` except `config.py` **only if** a
> test-database setting is genuinely required there."* One declarative field, no behaviour.

**Add a C3 row that resolves the slot the way an operator sets it** — through the environment
and through `.env` — not through the `slot=` keyword parameter. Every existing C3 row bypasses
the surface that was broken, which is why all of them stayed green while the feature was inert.
The reviewer confirmed the resolver itself is sound: mutating it to ignore `os.getenv` reddens.
**This is a reachability defect, not a resolver defect** — fix the reachability, don't rewrite
the resolver.

## 4. B2 (blocking) — the legacy sweep destroys other checkouts' live databases

`_sweep_legacy_databases` (`:303-306`) is called from `start()` (`:170`), so **every pytest
process** drops every database matching `LEGACY_TEST_DATABASE_PATTERN` (`:18`). Neither the sweep
nor the guard's legacy-name acceptance has a covering row: replacing the sweep body with `return`
→ `36 passed`; removing the legacy alternative from the pattern check → `36 passed`.

**Executed by the coordinator:** with `beyo_test_main` present, `pytest tests/unit/test_items_router.py`
— three tests, **1.30 s** — took server membership from
`['beyo_test_main', 'beyo_test_main_template']` to `['beyo_test_main_template']`.

**The correction, verbatim:**

> Make the legacy disposition a bounded reclamation rather than an unconditional destructive
> branch on every process, and record in the plan whether permanence is still intended given
> that a pre-slot checkout's live worker database is dropped by any run started from a
> slot-aware checkout. Add two criterion rows: one asserting a legacy-named database is accepted
> by the guard and reclaimed, one asserting **a slot-qualified name belonging to another slot is
> not swept**.

Task 2 asked for a **one-time disposition**; what shipped is a permanent sweep on every start.
Those are different mechanisms, and the difference is what the finding is about. How you bound it
is yours — state the choice and its reason.

## 5. S2, S3, S4, S5

**S2 — C6(b) was never implemented.** Deleting the `scan_iter`/`delete` loop
(`conftest.py:59-64`) leaves the criterion module at `36 passed`.
> Add an explicit finalizer or session-scoped assertion that writes a key under the process
> prefix and verifies it is gone after the prefix fixture finalizes.

**S3 — Redis is now a hard dependency of every pytest session.** `isolated_redis_prefix`
(`conftest.py:51-65`) is correctly session-scoped autouse, but its `finally` block opens a Redis
connection unconditionally: with `REDIS_URL` on a dead port, `tests/unit/test_items_router.py`
goes `2 failed, 1 passed` → `2 failed, 1 passed, 1 error`. This makes the authoritative
failing-ID set a function of Redis availability, which plan 3 consumes.
> Make the key sweep best-effort — catch `redis.exceptions.ConnectionError` and warn — while
> keeping the prefix override unconditional. If you instead judge Redis a legitimate hard
> requirement, say so explicitly and it becomes a plan-3 precondition rather than a silent one.

**S4 — two `_parse_database_url` sub-checks have no row.** Removing the drivername check
(`:66-67`) → `36 passed`; removing the completeness check (`:68-69`) → `36 passed`. Both
URL-parsing rows use `None` and `"not-a-database-url"`, caught earlier at `:60-61` and `:62-65`.
Nothing exercises `postgresql+psycopg://…`, `mysql://…`, or a URL missing a username, and
`target_database_url`'s failure paths have **no row at all**.
> Add three rows — non-PostgreSQL driver, URL missing username, malformed `target_database_url`
> — and re-run the per-sub-check mutation at this finer granularity. Enumerate the sub-checks
> **from the code's branch points, not from the prose description**: the URL parser is four
> checks wearing one name, which is why disabling it wholesale looked like coverage.

**S5 — record the corrected characterisation; do not repair anything.** The reversed-run
`assert 0 == 2` was **object lifetime, not collection order**: the `SkuTemplate` is absent from
the `WeakInstanceDict` identity map in all four configurations tried, and the assertion passes
without the repair. `expire_on_commit=False` is not the explanation and neither is ordering.
> Record the corrected characterisation **in this cycle's handoff and in the plan's Review log**.
> **Do not edit the fix-r2 handoff** — it is published, and a previous round's in-place edit
> destroyed the provenance of a real error. The `refresh()` repair stays; it is correct
> regardless of cause.

## 6. Notes to close

- **N1** — `conftest.py:32-34`: `assert_configured_database_unchanged()` runs before the URL is
  restored and before `isolation.stop()`, so if it raises, the worker database is never dropped.
  The residue check's own failure produces residue.
- **N2** — `database_isolation.py:166-169`: `configured_row_counts_before_run` stays `None` when
  the configured database name matches the test pattern, so `assert after == None` fails at every
  session teardown. Edge configuration; guard it.
- **N6** — `test_add_task_steps_integration.py:90` still passes `worker_role` to `add_all([...])`
  after adopting it. Harmless, cosmetic.
- **N7** — C7(a) covers `create_pause_reason.py:36` only; `seed_pause_reasons.py:102` is the
  second production writer named by the plan and has no row. Both write `False` today.
- **N9** — `test_list_users_floor_identification.py:192-209`: the `roster` fixture creates its own
  `Workspace` but its `finally` deletes only `UserWorkProfile`/`WorkspaceMembership`/`User`, never
  the `Workspace` or the `WorkspaceRole`. Harmless today only because `list_users` never commits.
  Align with the explicit-delete pattern of `test_update_user_admin_clock_in_code.py`'s
  `admin_fixture` (charter rule 11½).

## 7. Evidence budget

**This session's L4 budget is exactly 2 runs**, both on the tree you hand over: default order —
the mandatory closing stamp — and the `BEYO_TEST_COLLECTION_ORDER=reverse` run C2 compares
against it. Publish the enumerated failing-ID set with the delta in both directions; the current
set is the 21 enumerated in the fix-r2 handoff.

**State your L4 count as a number.** Two reviewers spent effort disambiguating "the first pair"
in the last handoff; it should have been a one-line fact.

Everything else runs at L1/L2, including every named mutation at its own site.

## 8. Scope fences

- **`app/beyo_manager/config.py` is open for B1's single field and nothing else.** No other
  production file, no behaviour change, no refactor while you are in there.
- **No `pytest-xdist`, no `-n`, no parallel run, no worker matrix, no new runner baseline.** Plan
  3 (OD-5).
- **N3 and N4 are out of scope.** N3 (`_normalised_endpoint` handles only literal `"localhost"`)
  is fail-closed and its blast radius is a suite that refuses to start, which is loud. N4 (the
  hardcoded `EXPECTED_HEAD` / `EXPECTED_PUBLIC_TABLE_COUNT`) is a real time bomb but it is
  phase-1 code and a behaviour decision; both are carried to plan 3.
- **Do not reopen C2.** It is met at `21 / 2561 / 1` in both orders with `comm` empty both
  directions. Your closing pair re-establishes it on the new tree; it is not a question to
  re-litigate.

## 9. Closing protocol

Per `implementation-executor.md`, with this project's specifics:

1. The closing L4 stamp on the tree you hand over, with tree identity, the **enumerated**
   failing-ID set, and the delta in both directions.
2. One evidence record **per named mutation**, at the site the criterion names, naming the
   criterion rows that reddened. Any sub-check whose disabling reddens nothing is **a finding to
   report**, not a gap to fill silently — that is how S4 was found.
3. `plans/plan_2.md` frontmatter → `state: IMPLEMENTED` with date, actor, counts. This project
   has **no master-plan tracker**; the frontmatter is the state.
4. Review-log entry in `plans/plan_2.md` §7.
5. Handoff at `handoffs/implementer/2026-08-21_phase2_fix_r4_handoff.md`, frontmatter `plan: 2` /
   `role: fix` / `state` / `date` / `actor`. Perimeter declared **cycle-scoped — the files *this
   session* changed**. Mutation-probe files listed separately from your own changes, with
   checksums. Every probe database and its verified disposition.
6. **Corrections to numbers published in earlier rounds go in this handoff.** Never edit a
   published document.
7. Anything only the owner can settle goes in the handoff as a **decision card** in the charter's
   `⚠ OWNER DECISIONS REQUIRED (n)` section. If nothing needs the owner, one line saying so.

The handoff file, not your chat message, is what the coordinator consumes.
