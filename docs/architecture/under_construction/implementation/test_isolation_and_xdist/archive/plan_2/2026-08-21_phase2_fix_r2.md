---
plan: 2
role: implementer
round: 2
date: 2026-08-21
project: test_isolation_and_xdist
---

# Session prompt — plan 2 fix r2, `test_isolation_and_xdist`

## 1. Role and mode

Finding-scoped fix cycle. Resolve B1, S1 and S2 below; N1 at your judgment, documented either
way. **Resolve, don't relitigate. Add nothing beyond the findings.**

Workspace root: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
Suite runs from `backend/app/`: `PYTHONPATH=. pytest -m 'not e2e'`
Branch `feat/test-isolation-xdist`, HEAD `a3c54b2` (`CHECKPOINT (not approved): phase 2`).

**Read first, by absolute path, as this session's doctrine:**
`/Users/davidloorenz/agent-skills/pipeline-charter.md` (including **"Test-evidence scope and
reuse"**) and `/Users/davidloorenz/agent-skills/implementation-executor.md`.

**`plans/plan_2.md` is your task list** — read its §7 Review-log entry for implement r1, which
carries the full consumption. Where this prompt differs from the plan file, the plan file wins.

## 2. Allowed file perimeter

The re-review will verify this against `git diff`, and anything outside it is an automatic
finding:

- `app/tests/integration/infrastructure/test_database_isolation.py`
- `app/tests/conftest.py` — only if B1's characterisation lands on the reversal hook
- `app/tests/integration/services/commands/sku_templates/test_sku_templates_commands.py` —
  only if B1's characterisation lands there
- `plans/plan_2.md` (Review log + frontmatter) and your handoff

**Nothing under `app/beyo_manager/`.** If B1's repair requires a production change, that is a
production-domain decision: **stop and raise an owner decision card** rather than improvising.
Read that as permission to stop, not as a hint about the answer.

## 3. B1 (blocking) — C2 is unmet; the two orders differ by one ID

Your closing pair reads default `21 failed / 2561 passed / 1` and reversed
`22 failed / 2560 passed / 1`. C2 requires the two failing-ID sets to be identical,
`comm`-empty both directions. They are not, so the phase's central criterion is unmet.

The handoff disposed of it as *"the known concurrency failure in
`test_concurrent_allocations_return_distinct_scalars`."* **It is not known**, verified by the
coordinator:

- `sku_template` occurs **zero times** in `live_clock_for_working_time_economics/master_plan.md`
  — the ID is in neither the enumerated 26 nor the derived 22.
- It was not among the 118 either: review r3's reversed measurement was `139 = 22 + 118 − 1`
  exactly, and its table places all 118 inside the eleven files. **So this test passed in the
  reversed run at `87a4b7a` and fails in the reversed run now.**
- It passes **alone, 3/3** (`4 passed` in 0.68 s / 0.54 s / 0.55 s), and `_seed_identity`
  (`test_sku_templates_commands.py:21-32`) creates its own uniquely-suffixed workspace — it
  borrows nothing, so this is **not** OD-3's class.

**The correction, verbatim:**

> Characterise the divergence before repairing it, and record the failure mode — the actual
> assertion or error text, not a label. Determine which of these it is: (a) a latent race in
> `allocate_sku_scalar_in_session` that timing under a full reversed suite exposes — the test
> asserts `{first, second} == {1, 2}` across an `asyncio.gather`, and both allocations
> returning `1` would be a real locking defect in production code; or (b) a genuine ordering
> dependency. Then resolve C2: either the sets match, or the divergence is explained in writing
> with evidence and routed as an owner card. A number is not updated to match a measurement.

**Run this probe as part of it.** The `87a4b7a` reversed figure `139 / 2422 / 1` came from a
reviewer's **local edit** to `pytest_collection_modifyitems`; this round shipped
`BEYO_TEST_COLLECTION_ORDER=reverse`. **Establish whether the two reverse identically.** If
they do not, the reversed before-side is not a comparator at all and the comparison is invalid
in both directions — which would change what B1 even means.

## 4. S1 (should-fix) — two orphaned databases, against a handoff that says there are none

Direct query against the configured server returns `beyo_test_shell_gw995` and
`beyo_test_shell_template`, both fully migrated (**107 public tables, marker present**). The
handoff states *"No probe database remained at close."* Slot `shell` appears nowhere in the
shipped code, so these are residue from an earlier iteration of
`test_unmarked_template_shell_is_absorbed`. **Nothing will ever reclaim them** — a default-slot
run touches only `beyo_test_main_*` — so two full schemas sit on the owner's server
permanently. Plan 1 C8's guarantee, *"the database set returns to its original membership"*, is
broken across this phase's boundary.

Your shipped cleanup does work: the coordinator measured the criterion module at `36 passed`
with database-set membership **identical before and after**. The finding is about what happens
when the happy path is not taken.

**The correction, verbatim:**

> Remove the two orphans, and replace the randomized probe slot with a bounded, reclaimable
> one. `monkeypatch.setenv("BEYO_TEST_SLOT", f"s{uuid4().hex[:10]}")` creates a slot no future
> run will ever reuse, so any non-happy-path exit writes a permanent orphan — and plan 1 C8's
> own *named mutation* was "make creation use a unique suffix per invocation", called out there
> as the `test_20260820_001, _002, …` failure mode the intention forbids. Use a fixed probe
> slot, or add a sweep that reclaims orphaned slots; state which and why.

Add the criterion row that would have caught this: **the server's `beyo_test_*` membership is
identical before and after the criterion module**, including on the failure path.

## 5. S2 (should-fix) — the named mutations are declared in bulk

The handoff lists the *files* probes touched and asserts *"every named probe was applied,
observed red, and reverted"*, with numbers for two of them. The executor protocol requires a
full evidence record **per mutation** (hypothesis, scope, command, tree identity, result,
ID-delta), and C4 requires **one mutation per sub-check** with the reddened rows recorded.

This matters more here than as bookkeeping. C4 exists precisely because a blanket `return True`
reddens every row regardless of which sub-check bit, so it cannot prove any individual check is
tested. **A blanket declaration has the same defect one layer up** — it cannot show that the
configured-tuple check, the endpoint-confinement check, the marker check and the URL-parse
check each have a row that tests them.

**The correction, verbatim:**

> Record one evidence row per named mutation, at the site the criterion names, including which
> criterion rows reddened for each. Report any sub-check whose disabling reddens nothing — that
> sub-check has no row testing it, and that is a finding to state, not a gap to fill silently.

You do not need to re-run mutations whose per-row result you can state accurately from this
round's work. Re-running one you already observed, on an unchanged tree, is over-evidence.

## 6. N1 (note) — the failing-ID set was described, not enumerated

C2 asks for a `comm`-diff in both directions; the handoff gives counts and a prose subtraction.
The counts reconcile and the predicted 21 is correct, so this is a reporting gap. Publish the
enumerated set with your closing stamp.

## 7. Evidence budget

**This session's L4 budget is exactly 2 runs**, both on the tree you hand over: the default
order — the mandatory closing stamp — and the `BEYO_TEST_COLLECTION_ORDER=reverse` run that C2
compares against it.

B1's characterisation may need more than that. **One additional full-suite run is
pre-authorised for it**; beyond that, write the charter's authorization line before the run and
record it. Everything else runs at L1/L2.

## 8. Closing protocol

Per `implementation-executor.md`, with this project's specifics:

1. The closing L4 stamp on the tree you hand over, with tree identity and **the enumerated
   failing-ID set** plus the delta in both directions against the published 22.
2. Per-mutation evidence records for anything this cycle re-measured (S2).
3. `plans/plan_2.md` frontmatter → `state: IMPLEMENTED` with date, actor, counts. This project
   has **no master-plan tracker**; the frontmatter is the state.
4. Review-log entry in `plans/plan_2.md` §7.
5. Handoff at `handoffs/implementer/2026-08-21_phase2_fix_r2_handoff.md`, frontmatter
   `plan: 2` / `role: fix` / `state` / `date` / `actor`. Declare the perimeter
   **cycle-scoped — the files *this session* changed**, not the files the phase touches. List
   every file a mutation probe touched separately from your own changes, and every probe
   database with its verified disposition.
6. **Corrections to numbers published in earlier rounds are recorded in this cycle's handoff.**
   Do not edit the implement-r1 handoff or any other published document — a previous round did
   that and destroyed the provenance of a real error.
7. Anything only the owner can settle — including B1 if it lands on production code — goes in
   the handoff as a **decision card** in the charter's `⚠ OWNER DECISIONS REQUIRED (n)`
   section. If nothing needs the owner, one line saying so.

The handoff file, not your chat message, is what the coordinator consumes.
