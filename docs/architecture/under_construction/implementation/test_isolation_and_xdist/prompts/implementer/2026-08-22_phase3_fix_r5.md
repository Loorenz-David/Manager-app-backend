---
plan: 3
role: implementer
round: 5
date: 2026-08-22
project: test_isolation_and_xdist
---

# Session prompt — plan 3 fix r5, `test_isolation_and_xdist`

## 1. Role and mode

Finding-scoped fix cycle closing **review r4**, the phase's first external review. Resolve
**B1**, **S1–S5**, and **N1–N2**. **Resolve, don't relitigate. Add nothing beyond the findings.**

Workspace root: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
Branch `feat/test-isolation-xdist`. HEAD `8501a51`, clean.
`app/` is byte-identical to `b96802f`, the tree every published measurement was taken on.

**Read first, by absolute path, as this session's doctrine:**
`/Users/davidloorenz/agent-skills/pipeline-charter.md` (including **"Test-evidence scope and
reuse"** and **"The owner layer"**) and `/Users/davidloorenz/agent-skills/implementation-executor.md`.

**Read then:** `handoffs/reviewer/2026-08-22_phase3_review_r4_handoff.md` **in full** — it carries
every finding's measurement, both sides, and the exact line references, and its §9 records what it
verified *correct* so you do not disturb it. Then `plans/plan_3.md` §7's r4 consumption entry.
**The plan file is your task list; where this prompt differs, the plan file wins.**

**Four new standing rules landed in `master_plan.md` §5 from this review — 10, 11, 12 and 13 — and
they bind this session.** Rules 11 and 12 are what B1, S1 and S2 are instances of; read them before
you write the repairs, not after.

## 2. Allowed file perimeter

The re-review verifies this against `git diff 8501a51 HEAD`; anything outside is an automatic
finding.

- `app/tests/integration/infrastructure/test_database_isolation.py` — B1, S1, S2, N1, N2.
- `app/.env.example` — S3, one line.
- `master_plan.md` — §6.1 (S5), §6.4 and §8 (S4).
- `plans/plan_3.md` — §7 and frontmatter.
- Your handoff.

**No production code. No `app/tests/database_isolation.py`.** Review r4 §9 verified that module
correct at nineteen host spellings and every guard boundary it could construct; none of these
findings asks you to change it. If you believe a repair requires touching it, stop and raise a
decision card.

## 3. B1 (blocking) — C2 row (c) cannot fail, and the narrowing task 3 warns about ships green

`test_concurrent_starts_survive_current_template`
(`app/tests/integration/infrastructure/test_database_isolation.py:415-435`) calls `seed.stop()`
before `_assert_concurrent_starts_succeed`, so **no connection is held anywhere.** C2 row (c)
specifies its observer as *"a held inspection connection overlapping a copy"*; the implementation
substituted two concurrent `start()` calls against an already-current template and wins on a
sub-millisecond race it has never lost, in two independent runs.

The consequence is the finding, not the flakiness. **Mutation M5 — the lock kept around
`_ensure_template` with the worker drop and copy moved outside it, which is exactly the narrowing
plan §4 task 3 warns against in writing (*"a guard around the copy alone leaves the real window
open"*) — leaves the criterion module at 51 passed.** Nothing in the repository detects it.

The hazard is real and the reviewer proved it independently of pytest: a standalone asyncpg probe
holding one connection to a template across a copy gets
`ObjectInUseError: source database … is being accessed by other users`, and the control with no
held connection succeeds.

**The correction, verbatim from review r4:**

> In `test_concurrent_starts_survive_current_template`, open a connection to the template
> (`await _connect(seed._url, seed.template_database_name)`) before
> `_assert_concurrent_starts_succeed` and close it after, asserting the copies still succeed. With
> the lock present the copy waits behind it and passes; with either M4 or M5 applied it raises
> `ObjectInUseError`. One row then covers both the removal and the narrowing.

**Run both named mutations against the repaired row and record both sides**: M4
(`_template_operation_lock` body → bare `yield`, definition site,
`app/tests/database_isolation.py:381-396`) and M5 (the copy moved outside the lock, call site,
`:242-245`). C2's contract is *"each row red with its own error, not one shared string"* — so
record row (c)'s error text, and confirm rows (a) and (b) still fail with
`UniqueViolationError` and `InvalidCatalogNameError` respectively. **Revert both.**

## 4. S1 + S2 — C8, repaired as one change

These are one repair. Do them together.

**S1** — the skip guard matches `-n0`, `--numprocesses=0` and the adjacent pair `-n` `0`, but
**not `--numprocesses 0`**, the space-separated long form of the same option. Measured both sides:
`-n 0` → 50 passed / 1 skipped; `--numprocesses 0` → **1 failed**. This matters beyond neatness:
§8's published serial-comparator row is `21 failed / 2575 passed / 1 skipped / 1 deselected`, and
**that `1 skipped` is C8**. A consuming project spelling the comparator `--numprocesses 0` gets a
failing ID that appears in neither published set — a baseline mismatch presenting as a parallel-only
failure, which is the exact class this phase exists to end. `live_clock` phase 4 and
`narrow_typical_work_times` D23 consume that row.

**S2** — the assertion requires the literal `["-n", "6", "--dist", "loadfile"]`, while **OD-10
explicitly permits raising the count with a measurement**. Measured: `-n 8` in `addopts` produces
`1 failed` with *"shipped parallel default is missing from pytest.ini addopts"* — false in every
particular, while eight workers demonstrably ran in 18.97 s. Standing rule 12.

**The correction, verbatim from review r4:**

> Assert the contract, not the number:
>
> 1. skip when the invocation args carry any `-n` / `--numprocesses` (this subsumes S1);
> 2. otherwise assert `addopts` contains `--dist loadfile` and an `-n` whose value is a positive
>    integer;
> 3. and assert `PYTEST_XDIST_WORKER` matches `gw\d+`.
>
> That survives a count change, keeps `--dist loadfile` (which OD-10 calls part of the default, not
> an incidental flag), and gives sub-check 3 a mutation the guard cannot short-circuit.

**Standing rule 11 applies and is the point of the exercise.** C8's sub-checks are sequential, so
the addopts mutation reddens the first and returns — the behavioural half was never executed by
the mutation meant to cover it. **Probe A's answer was that the sub-check is live, not inert:**
`PYTEST_ADDOPTS="-n 0"` is merged by pytest *after* the ini `addopts` but never appears in
`invocation_params.args`, so the guard misses it and the assertion bites with
`assert None`. Once the guard skips on any `-n` spelling, that becomes sub-check 3's named
mutation.

**So record three mutations for C8, one per sub-check, and state which bites on which:**
`addopts` missing `-n` entirely; `addopts` at `-n 8` (must now be **green** — that is the repair);
and `PYTEST_ADDOPTS="-n 0"` for the worker assertion. Plus the two spellings of the comparator,
`-n 0` and `--numprocesses 0`, both of which must skip.

## 5. S3, S4, S5 — three documentation surfaces that now say something untrue

**S3 — `app/.env.example:11-12`.** Still documents the legacy reclamation sweep as
`BEYO_RECLAIM_LEGACY_TEST_DATABASES=1 pytest -m 'not e2e'`, which is now six workers. Fix r3
corrected the identical command in master plan §6.1 and missed this copy — and this is the
operator-facing one, the file a developer copies. Project standing rule 5: *an environment
variable's documentation surface is part of its contract*, earned by OD-7 on this exact file. Task
9's chosen branch was *"document the command as serial-only"*, which is only sound if **every**
surface says serial-only.

> **The correction:** `# BEYO_RECLAIM_LEGACY_TEST_DATABASES=1 pytest -m 'not e2e' -n 0`, matching
> §6.1 exactly.

**S4 — master plan §6.4's final clause is measurably false.** It reads *"…so the failing-ID set is
not a function of Redis availability."* That holds for `isolated_redis_prefix`, which guards
`ConnectionError` in both setup and teardown. It does not hold for the suite:
`test_logout_user_integration.py` has two rows asserting against a live Redis **by name**
(`..._in_real_redis`), and the function-scoped `redis_client` fixture (`app/tests/conftest.py:135-142`)
calls `scan_iter` in its `finally` with no guard. Measured: Redis reachable → 2 passed; dead port →
**2 failed, 2 errors**. So a fresh checkout without Redis gets 23 failures and 2 errors, not 21 —
while §6 instructs consuming sessions to *cite* §6.4 rather than re-derive it.

> **The correction:** narrow §6.4's clause to what is true (the isolation-prefix teardown is
> availability-tolerant; the two logout rows are not), and state **"Redis reachable at
> `settings.redis_url`"** as an explicit precondition on §8's phase-3 baseline row.

§8's schema was extended for exactly this at the r4 fold — a published row now states its
failing-ID set, tree identity, database identity, **and the services that must be reachable**. Fill
that in; do not re-derive the schema.

**S5 — master plan §6.1's reversed-collection row** inherits the parallel default, so
`BEYO_TEST_COLLECTION_ORDER=reverse` now only permutes within a worker's assignment. Projection
ledger row L16 said this in advance. It was phase 2's serial order-independence probe and half of
phase 2's approval evidence, published uncaveated in a form the plan says no longer isolates
ordering.

> **The correction:** add `-n 0` to that row, or caveat it in place citing L16. State which and why.

## 6. N1, N2 — notes, in scope because the round is open

**N1 — two named loopback literals have no row.** `_normalised_endpoint` recognises `localhost`,
`localhost.localdomain` and `ip6-localhost`; C7's parametrize
(`test_endpoint_aliases_are_confined_to_same_server:300-306`) covers `LOCALHOST`, `::1` and
`localhost.`, exercising case-folding, the `is_loopback` branch and the trailing-dot strip — but
neither of the other two literals. Mutation M6, both extra literals deleted: **51 passed.** Add the
two rows. Charter rule 2: enumerate, never sample.

**N2 — C3(a)'s sibling-disjointness line cannot fail.** `_assert_concurrent_starts_succeed:361-363`
asserts distinctness over hand-constructed distinct worker ids, so the set can only shrink if the
resolver collapses them — which is the *other* row's job, and that row does bite. Delete the line
or re-point it at the resolver. State which.

## 7. Evidence budget

**This session's L4 budget is exactly 3 runs**, all on the tree you hand over:

1. **The closing stamp at the shipped default** — bare `PYTHONPATH=. pytest -m 'not e2e'`.
2. **A second run at the shipped default** — C5's second scheduling condition.
3. **The `-n 0` serial comparator** — it re-establishes §8's comparator row, and S1 changes whether
   C8 skips under it, so the published `1 skipped` must be re-confirmed rather than assumed.

Each carries tree identity — SHA plus asserted `git status --porcelain`, or SHA plus a `git diff`
digest if dirty — the enumerated failing-ID set, and the delta in **both** `comm` directions
against the phase-2 21-ID comparator. **State your L4 count as a number.** Anything beyond these
three needs the charter's authorization line, written **before** the run.

Everything else is L1/L2, including every mutation above at its own named site. The
`--numprocesses 0` check is L1 on the criterion module, not a fourth full run.

The `pg_stat_activity` peak is 25 of 100 and is carried from r2; cite it, do not re-measure it, and
say it is carried — r2 and r3 both did this correctly.

## 8. Scope fences

- **Nothing under `app/beyo_manager/`**, no requirements manifest, no `pytest.ini`, no
  `conftest.py`, no probe module, no `app/tests/database_isolation.py`.
- **Do not change the failing-ID set.** The 21 IDs are the phase's product. If a repair moves them,
  stop and report — that is more important than finishing the round.
- **Do not re-open what r4 verified correct** (its §9): the advisory lock's own design, the
  five-step guard's boundaries, the N4 removal, the `0.0.0.0` narrowing, the Makefile targets.
  Only the *test* for the third contention path is missing, and that is B1.
- **N3 is out of scope** — the three settled graph records are being repaired by a separate
  maintenance session under owner authorization. Do not touch `.archgraph/`.
- **N4 and N5 are out of scope and deliberately unassigned.** `app/run_pytest_suite.py` is
  unreferenced dead scaffolding that now spawns six workers including `e2e`; the perturbation
  harness ships permanently with no retirement owner. Both are carried to the project's closeout,
  not to you.
- **A published handoff is never edited.** Corrections go in yours.

## 9. Closing protocol

1. **The closing L4 stamp at the shipped default, on the tree you hand over**, with tree identity,
   the enumerated failing-ID set, and the delta in both directions. Master plan §5 **rule 9**:
   change anything after stamping and the stamp is void; re-taking it is not over-budget.
2. **One evidence record per named mutation, at the site the criterion names**, definition-side vs
   call-site stated, both sides computed, and — per **standing rule 11** — **one mutation per
   sub-check, with which bites on which**. C2 row (c) and C8's three sub-checks are where this is
   the whole point.
3. **Document writes** per §5. The master plan §3 tracker row is the coordinator's.
4. `plans/plan_3.md` frontmatter → `state: IMPLEMENTED` with date, actor, and the counts.
5. **Review-log entry** in `plans/plan_3.md` §7.
6. **Handoff** at `handoffs/implementer/2026-08-22_phase3_fix_r5_handoff.md`, frontmatter
   `plan: 3` / `role: fix` / `state` / `date` / `actor`. Body: what changed, the counts, your
   **cycle-scoped** write perimeter, mutation-probe files listed separately with checksums, every
   probe database and its verified disposition, and **your L4 count as a number**.
7. Anything only the owner can settle goes in the handoff as a **decision card** in the charter's
   `⚠ OWNER DECISIONS REQUIRED (n)` section. If nothing needs the owner, one line saying so.
8. Your final chat message follows the charter's **owner layer**: what you did, what it means in
   plain words, what happens next, what needs them. Not a paste of the handoff.

The handoff file, not your chat message, is what the coordinator consumes.
