---
plan: 2
role: implementer
round: 2
date: 2026-08-20
project: live_clock_for_working_time_economics
---

# Session prompt — plan 2 fix (round 2), `live_clock_for_working_time_economics`

## 1. What happened and what this round is

Implement r1 is **correct in production code**. The coordinator verified the full
perimeter against `git diff`, re-ran the suite (26 / 2465 / 1, failing-ID set identical
to baseline), and re-applied **all fourteen** named mutations at their sites — the six
you measured and the eight you did not. Every production file matches the plan. Nothing
in `app/beyo_manager/` changes this round.

What is missing is proof. Three criteria are unproven, and two of the eight unmeasured
mutations turned out to be **∅ — the whole suite stays green under them**. That is the
class this project has recorded eight times: a test whose name says it guards something
and whose fixture makes it unable to fail.

**Perimeter for this round — test files only:**

- `app/tests/integration/services/queries/item_economics/test_phase2_live_surfaces.py`
- new sibling test files under the same directory if you prefer to split C6/C9 out
- `app/tests/integration/services/queries/item_economics/test_budget_allocations_query.py`
  **only** if B3's three-task fixture belongs there rather than in the phase file

**Nothing in `app/beyo_manager/` may change.** The re-review will `git diff` this
perimeter and treat any production line as an automatic finding. If you believe a
finding below requires a production change, stop and report it instead of making it.

Doctrine: read `/Users/davidloorenz/agent-skills/pipeline-charter.md` and
`/Users/davidloorenz/agent-skills/implementation-executor.md` by absolute path and
follow them. `plans/plan_2.md` is your task list — **read §5's C3, C6, C8, C9 and C11
in full**, and §7's newest entry (the consumption record) for the measurements below.
Where this prompt differs from the plan file, the plan file wins.

## 2. Blocking findings

### B1 — C6 has no test at all

Plan §5 C6 specifies **four rows**; the phase file contains none of them. Write all four.

Rows 2 and 3 carry both-sides values **already measured against the real
`budget_division.py:_governing_step`** and recorded in plan §5 C6 — reproduce them, do
not re-derive them:

- **Row 2 (`created_at`).** Fixture: the two steps' `latest_state_record.entered_at`
  **equal**; `created_at` distinct; `client_id` order contradicts `created_at` order.
  Mutation at the substitution site in `get_task_production_time.py`: build
  `DivisionStep` omitting `created_at`. Contract governing = later-created step;
  mutation governing = the other. Measured `stp_b` → `stp_a`.
- **Row 3 (`latest_state_record`).** Fixture: `entered_at` **distinct** and ordered
  *against* `created_at`. Mutation: omit `latest_state_record`. Contract governing =
  later-**entered**; mutation = `created_at` takes over. Measured `stp_b` → `stp_a`.
- **Do not merge rows 2 and 3 into one fixture.** On row 3's fixture the `created_at`
  mutation is measurably inert (`stp_b` → `stp_b`). `_governing_step` applies three
  *stable* sorts — `client_id`, then `created_at`, then `entered_at` — so only the last
  applied is primary and a field is observable only where every key above it ties.

**Coordinator measurement you must reproduce and then close:** with `created_at=None` at
E-P's substitution site, the **whole suite is green (∅ added IDs)**. After your fix, that
same mutation must redden row 2 and nothing else.

Row 1 (allowance byte-identity across a settlement close) and row 4 (E-A's all-COMPLETED
section, mutation = adding `selectinload(TaskStep.latest_state_record)` to E-A's step
load) are specified in full in plan §5 C6. Note for row 4: the coordinator measured that
this mutation currently reddens **only C8's statement count** — an accident of a
query-count assertion, not an allowance assertion. Row 4 must fail on
`allowance_seconds`/`left_seconds` moving.

### B2 — C9 has no test at all

Plan §5 C9. This is **D8** — the owner's explicit decision to ship the settlement-window
drop and disclose it. It is the one deliberate user-visible regression in this pipeline
and nothing currently proves it behaves as disclosed.

The close recipe is plan 1 §5 C5's and is **not** `transition_step_state` (which stamps
its own clock): use `_step_transition_core.py:_apply_step_transition` with `now=t`, and
do **not** run the analytics worker. The step lands in `PAUSED`, which is neither
terminal nor excluded, so allowances do not move — verified at source by the
coordinator. Then run `_recompute_step_time_totals` and assert the value returns.

Charter 11½ applies if your fixture commits: it owns its `try/finally` teardown, and the
enqueued outbox row is committed state.

### B3 — C8 cannot fail on the property it names

`test_c8_allocations_batch_has_one_open_record_probe` serves **one** task. Per-batch and
per-task loading are indistinguishable on a one-task fixture — the identity element for
batching. **Coordinator measurement:** moving the loader inside the per-task loop leaves
the whole suite green (∅), and the amended
`test_budget_allocation_constant_query_count_for_one_and_three_tasks` does not catch it
either.

Plan §5 C8's fixture: **three** batched tasks, one active worker holding an open working
record in **each**. Contract 1 probe statement + 1 `compute_record_contributions` call;
mutation (loader moved inside the loop) **3 and 3**. Add the two-worker row: 1 probe + 2
wrapper calls. After your fix the loop-local mutation must redden this row.

### B4 — the mutation ledger

Run **every** named mutation in C3, C5, C6 (×4), C7, C8, C9, C11 (×3), C12 (×3) at its
named site, whole-suite, both-direction ID diff against the clean set, revert, verify the
revert. "Carried to review rather than claimed as measured" is not an available
disposition: the protocol is the implementer's (charter rule 11; implement prompt §7),
and in this round the unrun set is exactly where both holes were.

You may cite the coordinator's measurements for the six you already ran **only** if your
re-run reproduces them ID-for-ID; otherwise report the discrepancy.

## 3. Should-fix

- **S1 — C3's population row is not in the phase's own file.** The C3 mutation currently
  reddens only a *pre-existing* test in `test_budget_allocations_query.py`. Your file
  cannot see it: it asserts E-P's headline (E-P passes its own map, bypassing the
  mutated path) and `worker == manager` (both fold through the mutation, so they move
  together and stay equal). Plan §5 C3 requires an **absolute** assertion on the E-B
  manager face: contract `actual_worker_seconds == 840`, mutation `== 600`, with the
  SKIPPED step at `240` — not `0`, which is addition's identity element.
- **S2 — C11's shim-inertness row is absent.** Plan §5 C11's last paragraph: five
  qualifying completed section-totals, exact `sample_count == 5` and the fixture's
  median, with the **future**-instant mutation at the definition site (`datetime(2099,
  1, 1, tzinfo=timezone.utc)`) driving `sample_count == 0`. The past-instant form is
  inert. Your C12 equivalent is written and correct — mirror it.
- **S3 — the 50-task ceiling measurement.** Implement prompt §8 required it as a
  Review-log obligation with the fixture shape (§9A T8, charter rule 1: a measurement,
  not a criterion). "Remains a source-level invariant" is the claim the measurement
  exists to test. Measure it and record the shape.

## 4. Notes — do not act on these, they are recorded so you do not re-file them

- **N1** — C11/C12's four call-site mutations each redden the same single test. Coverage
  holds; diagnosis does not. Acceptable as-is.
- **N2** — the constant-query-count assertion you moved to `first_count + 1` is
  legitimate (the shared probe), and `first_count == 11` still pins the batch. It is
  covered by B3's new row from here on.
- **N4** — you edited `master_plan.md`'s header state line as well as your tracker row.
  The edit was accurate so nothing is owed; the header is the coordinator's, the tracker
  row is yours.

## 5. Resolve, don't relitigate

Close the findings above and add nothing beyond them. No new production behaviour, no
new delegation, no criterion rewriting — if a criterion looks wrong, that is a report,
not an edit. D4–D9 are settled and their comments are in place.

## 6. Closing protocol

1. Full suite green except the enumerated baseline; count **and** ID-set diff recorded.
2. Checkpoint commit at IMPLEMENTED, subject prefixed `CHECKPOINT (not approved):`.
3. Update only phase 2's tracker row; findings to `plans/plan_2.md` §7.
4. Archgraph: nothing is owed this round (test-only). If you touch it, declare it.
5. Handoff at `handoffs/implementer/2026-08-20_phase2_fix_r2_handoff.md`, frontmatter
   `plan: 2`, `role: implementer`, `round: 2`, `state`, `date`, `actor`. It carries: the
   owner-readable opening; `⚠ OWNER DECISIONS REQUIRED (n)`; **the complete fourteen-row
   mutation ledger with both sides per row**; the C8 ceiling measurement; a line per
   finding B1–B4/S1–S3 saying how it was closed; and your full write perimeter from
   `git status` / `git diff --name-only`, including every file touched by a probe.

## 7. Added after authoring — external commit streams (owner note, 2026-08-20)

Two owner-committed streams run in parallel with this pipeline. They will appear in
`git log` and possibly in your diff base; **neither is yours and neither is a finding.**

- **Production budget cap** (commits prefixed `CHECKPOINT (not approved): production
  budget cap`), perimeter `domain/item_economics/calculator.py`,
  `domain/item_economics/price_scenario.py`,
  `services/queries/item_economics/get_task_price_scenario.py`, their test files, and one
  frontend handoff. It bumps `CALCULATION_VERSION` 1 → 2 deliberately — expected, not a
  contradiction of this intention. **It adds tests, so the suite baseline moves under
  you:** measure the baseline on the tree you actually run on and diff the *enumerated
  failure-ID set* from master plan §6, never a carried pass count. Your mutation ledger's
  both-sides comparisons must be against *your own* clean run from the same tree.
- **`narrow_typical_work_times`** — a documents-only planning folder, sequenced behind
  this pipeline by its own D23. Do not read it; nothing in it changes your task.

**One escalation trigger:** if you observe this pipeline's three golden JSONs or
`test_live_clock_goldens.py` changed by anything other than your own work, stop and
report it. The cap session runs our golden test as its tripwire, so a moved golden is a
real cross-stream defect, not noise.

Your own perimeter is unchanged and still test-only. Full details in master plan §7,
*Recognized external commit streams*.
