---
plan: 1
role: implementer
round: 2 (fix)
date: 2026-08-19
pipeline: inline_valuation_versioning
---

# Fix round 2 — plan 1 (review r1 S1)

You are the implementer. Review r1 returned **CHANGES_REQUIRED with one should-fix and
zero blocking**. The mechanism is correct and stays untouched: M1's compare / inherit /
version decision, the zero-write no-op, the credit and the chain all closed under mutation,
and C1–C8 and C10 all bite. **Do not redesign any of it.**

This round is **one file and roughly two lines**, plus two recording tasks.

## Read first

1. `…/handoffs/reviewer/2026-08-19_phase1_review_r1_handoff.md` — **S1** and **N1**. Its
   "What I re-derived independently (settled ground)" section is exactly that: do not
   re-derive it.
2. `…/plans/plan_1.md` — criterion **C9** is the authority for the fix.

## The work

**S1 — make C9's guard cover the perimeter C9 states.**

`app/tests/unit/docs/test_item_economics_handoff_accuracy.py:220-225`
(`test_retired_inline_refusal_identity_is_absent_from_live_sources`) scans
`_PACKAGE` (`app/beyo_manager`), `_APP_ROOT / "tests"` and `_HANDOFFS`
(`docs/handoff/to_frontend`), for `*.py` and `*.md` only. C9 names **`app/`** and
**`docs/handoff/`**. So `app/scripts/`, `app/migrations/`, `app/run.py`, `app/seed_*.py`,
`docs/handoff/from_frontend/` and `docs/handoff/presentation_system/` are all outside the
guard.

The reviewer proved it: planting the literal in `app/scripts/_reviewer_probe_c9.py` **and**
`docs/handoff/from_frontend/_reviewer_probe_c9.md` left the docs-accuracy suite at
**51 passed** — both plants invisible.

Widen the roots to match the criterion: `_HANDOFFS` → `_BACKEND_ROOT / "docs" / "handoff"`,
and the package/tests pair → `_APP_ROOT`. If `_APP_ROOT` sweeps noise (`__pycache__`,
`.venv`, logs), **exclude those explicitly rather than narrowing back to subdirectories** —
and if you deliberately leave any root out, say so **in the criterion**, not silently in
the test.

**Verification is the deliverable, not the edit.** Re-run the reviewer's plant: write the
literal into those same two paths, confirm the test now goes **red**, delete both plants,
confirm `git status` is clean, and record the observed red output. A widened glob that
nobody re-planted against is the same defect one layer out.

**N1 — record the rename (one line, no code change).** The pre-existing phase-8b test
`test_c1_inline_birth_writes_valuation_and_handles_exact_auto_statuses` was renamed to
`test_c7_…` and its parameter ids `C1-row-*` → `C7-row-*`. That was not asked for and was
not declared. **Do not revert it** — reverting would break this plan's citations, and the
charter forbids rewriting archives. Instead add the `C1-row-* → C7-row-*` mapping to
`plans/plan_1.md`'s Review log so the two archived phase-8b handoffs that cite the old names
stay followable.

Also: your r1b handoff's DECISIONS section said "None" while this rename was in the diff.
State it this time.

## Perimeter

`app/tests/unit/docs/test_item_economics_handoff_accuracy.py` and
`docs/…/inline_valuation_versioning/plans/plan_1.md`. Nothing else. If a fix appears to
need another file: **STOP and report** — that instinct was right last time.

**Not yours this round:** the architecture graph. Card 1 was answered and the coordinator
already widened `command-task-create`'s anchor 72 → 594 under owner authorization; the
graph is at revision `50b39402…`. Do not touch it.

## Suite

From `backend/app/`: `PYTHONPATH=. pytest -m 'not e2e'`. Start point **2320 passed /
26 failed / 1 deselected**. The test count should not change — you are widening an existing
test's roots, not adding one.

**A single run is not evidence.** This suite has been observed at 25, 26 and 27 failures on
unchanged code. If your count disagrees, repeat and diff the **ID set**; only an ID added or
removed across repeated runs is a finding.

## Checkpoint and handoff

`CHECKPOINT (not approved): inline valuation versioning — C9 guard perimeter`, and
`…/handoffs/implementer/2026-08-19_phase1_fix_r2_handoff.md`: perimeter generated from
`git`, suite totals and failure-ID diff, the **observed red output from the re-planted
probe**, the N1 mapping, and a **DECISIONS I HAD TO MAKE** section that is honest about
anything you settled — including any root you chose to exclude.
