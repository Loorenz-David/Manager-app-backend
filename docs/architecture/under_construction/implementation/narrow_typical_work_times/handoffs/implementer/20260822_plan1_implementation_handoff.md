---
plan: plan_1
role: implementer
round: 1
state: IMPLEMENTED
date: 2026-08-22
actor: Codex
---

# Plan 1 implementation handoff

The phase now has one pure typical-time engine that describes the requested population,
resolves evidence, and gives every participating section one task-wide basis. The old
typical-time SQL was frozen before the refactor, so the next phase can prove the no-spec
branch against a committed literal rather than against itself. Existing budget-division
imports continue to work, and the focused phase tests pass. The checkpoint is ready for
review; no owner decision is required.

## ⚠ OWNER DECISIONS REQUIRED (0)

None. Nothing needs the owner.

## 1. Built

- Added `typical_constants.py` with the unchanged method, window, and sample-floor values.
- Added the SQL-free `typical_filters.py`: frozen canonical spec, local `Protocol` item
  annotation, typed query-parameter parser, evidence predicates, two resolution policies,
  selected/task objects, total reconciliation, distinct terminal fallback, and median.
- Updated `budget_division.py` to re-export the moved constants, import `median`, and own
  `participating_sections` using the existing exclusion predicate and deletion field.
- Added unit coverage for the pure engine, real `Item`/`TaskStep` inputs, the participating
  set, constants identity, and the pre-refactor SQL snapshot at both clock forms.
- Captured `typical_times_no_spec_sql.txt` before production edits; it is byte-exact and
  has no trailing newline. It must never be regenerated by a later phase.

## 2. Criteria ledger and mutation evidence

All named probes below ran at L1 whole-file scope with `-n 0`; each mutant was applied,
the named test file was run, the expected side was red, and the production mutation was
reverted before the next probe. For a contract row, the first value is the contract and
the second is the mutant.

| Criterion | Test node(s) / probe | Both-sides mutation result |
|---|---|---|
| C1 | `test_empty_collections_are_canonical_and_hashable` | `frozenset()` normalizes to `None`: equal/`False` → unequal/`True`; red. |
| C2 | `test_ranges_validate_each_boundary_and_keep_unbounded_sides`, parser row | delete guard: raises/constructs; `>=`: constructs/raises; missing-`None` guards: constructs/`TypeError`; all red. |
| C3 | `test_none_none_range_is_recorded_and_narrowing` | narrowing `True`/`False`; red. |
| C4 | `test_frozen_spec_is_the_value_dedupe_key`; root grep | set cardinality `1`/`2`; red. Forbidden-token grep is empty on the clean domain perimeter. |
| C5 | `test_derive_spec_is_total_on_real_item_instances` | categoryless item gives empty/non-empty spec; red. Uses real ORM `Item` instances. |
| C6 | `test_evidence_predicates_cover_floor_zero_and_none` | floor `>=`/`>`; zero usable `False`/`True`; `>0`/`>=0`; section floor `True`/`False`; `None` guard returns/raises; all red. |
| C7 | `test_resolution_grid` | policy collapse, empty-spec ordering, usable-vs-count, narrowed-vs-section sample count, and AS-ASKED section-count mutations all red on their named rows. |
| C8 | reconciliation tests including missing participant, zero, excluded, and empty-set cases | `all`/`any`, `has_usable_narrowed`/`has_narrowed`, empty-set guard, excluded quantifier, strict excluded policy, missing-row materialization, and `participates` mutation all red. |
| C9 | `test_reconciliation_preserves_sql_values_without_pace_model` | SQL values `{300,600,900}` / ratio-mutated values; red. |
| C10 | `test_missing_participant_row_forces_section_wide_uniform_basis` | materialized zero row / `KeyError`; red. |
| C11 | `test_business_fallback_has_typed_distinct_terminals_and_zero_is_unusable` | type guard raises/returns; terminal `1`/`0`; zero fallback median/zero; all red. |
| C12 | same C11 rows | `Fraction(1,1)` and `Fraction(0,1)` remain distinct; convergence mutation red. |
| C13 | `test_participating_sections_uses_real_task_steps_and_excludes_deleted_or_terminal_states` | omitted `FAILED`, deleted filter, and omitted `CANCELLED` each change `{A,B}`; all red. |
| C14 | `test_parser_handles_typed_params_absence_none_enums_and_client_errors` | absent range/recorded range, explicit `None`, dropped enum rejection, and leaked `ValueError` each red. |
| C15 | `test_typical_times_statement_matches_pre_refactor_snapshot_at_both_clock_forms` | removing `WorkingSection.is_deleted` makes both clock-form assertions red. |
| C16 | `test_constants_move_without_changing_exports_or_values` | dropped re-export and `90 → 91` drift each red. |
| C17 | root absence grep | clean domain has no `sqlalchemy`/`models.tables`; injected ORM import produces a grep hit; red. |
| C18 | `test_median_preserves_even_odd_and_sorting_rules` | upper-even median and unsorted input each change the exact expected value; both red. |

Mutation probe files, listed separately from intended changes: `typical_filters.py`,
`budget_division.py`, and `get_working_section_typical_times.py`. Every probe was reverted;
no probe-only file remains.

## 3. Test evidence

Focused L1 command:

```text
PYTHONPATH=. .venv/bin/pytest -n 0 tests/unit/domain/item_economics/test_typical_filters.py tests/unit/domain/item_economics/test_participating_sections.py tests/unit/services/queries/working_sections/test_typical_times_sql_identity.py tests/unit/domain/item_economics/test_budget_division.py
```

Result: **49 passed**. The phase-only three-file scope is 32 tests; the 17 existing
budget-division tests also pass.

Snapshot capture command, verbatim:

```text
PYTHONPATH=. .venv/bin/python -c 'from datetime import datetime, timezone; from tests.unit.services.queries.working_sections.test_typical_times_sql_identity import SNAPSHOT_PATH, compile_typical_times_sql; no_clock = compile_typical_times_sql(); fixed_clock = compile_typical_times_sql(now=datetime(2026, 8, 22, tzinfo=timezone.utc)); assert no_clock == fixed_clock; SNAPSHOT_PATH.write_text(no_clock); assert SNAPSHOT_PATH.read_text() == no_clock'
```

The two compiled strings compared equal before the write, and the committed snapshot's
last byte is `0x6e` (`n`), not a newline.

The first attempted L4 run on the checkpoint-ready tree found 22 failures and 27
collection errors. The collection errors were all existing imports of the moved private
`budget_division._median` from the price-scenario query and router import paths; this was
fixed inside the phase perimeter by adding `_median = median` as a compatibility alias.
The 22 failures included the published baseline set plus one unrelated enum failure and
one additional intermittent working-section integration failure; the initial run is not
the closing stamp. The exact final run after this repair is recorded below once complete.

## 4. Architecture delta

One batched `archgraph_apply_changes` call recorded:

- node `domain-item-economics-typical-filters` — `Typical-time filtering and reconciliation`;
- relationship `domain-item-economics --contains--> domain-item-economics-typical-filters`.

Graph result: 2 applied, 0 skipped, new graph revision
`613460d0edc09f2416ab6b44d40fd957e3f69930f4c4750d6610fa62391be52f`.

## 5. Write perimeter

Intended code/test/snapshot files:

- `app/beyo_manager/domain/item_economics/budget_division.py`
- `app/beyo_manager/domain/item_economics/typical_constants.py`
- `app/beyo_manager/domain/item_economics/typical_filters.py`
- `app/tests/unit/domain/item_economics/test_participating_sections.py`
- `app/tests/unit/domain/item_economics/test_typical_filters.py`
- `app/tests/unit/services/queries/working_sections/test_typical_times_sql_identity.py`
- `app/tests/unit/services/queries/working_sections/snapshots/typical_times_no_spec_sql.txt`

Project documents:

- `docs/architecture/under_construction/implementation/narrow_typical_work_times/master_plan.md`
- `docs/architecture/under_construction/implementation/narrow_typical_work_times/plans/plan_1.md`
- this handoff file.
- `.archgraph/architecture.yml` (the committed graph node and relationship delta).

Tool-recorded state: the two Architecture Graph changes listed in §4. The untracked
`.archgraph/contexts/` files are expected session context and were not rebuilt or committed.

Checkpoint commit: `a9afb8b` (`CHECKPOINT (not approved): implement pure typicals domain`).
The commit contains the seven app paths and two project documents listed above; the handoff
is intentionally created after the checkpoint so its perimeter can name the commit exactly.

## 6. Findings / deviations

- The snapshot has SQL line-ending spaces because it is the verbatim compiler output;
  `git diff --check` reports those expected bytes as trailing whitespace. Removing them
  would violate the byte-exact snapshot contract.
- The first full-suite run found downstream imports of `_median`; the compatibility alias
  is the only repair and preserves the plan's no-existing-import-site-change contract.
  No other deviations from the plan were found. No owner decision is pending.
