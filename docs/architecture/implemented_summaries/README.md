# Implemented Summaries

Store concise post-implementation summaries linked to source plans.

## Regression Validation Reference

For implementation plans that require full regression evidence before closure, use the canonical command block documented in:

- `application_contracts/backend/tests/README.md` under **Repeatable Full Regression Run (backend/app)**

This keeps summary files concise while ensuring every plan points to one repeatable test procedure.
