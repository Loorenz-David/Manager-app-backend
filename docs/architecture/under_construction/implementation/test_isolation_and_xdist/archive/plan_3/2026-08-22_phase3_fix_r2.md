---
plan: 3
role: implementer
round: 2
date: 2026-08-22
project: test_isolation_and_xdist
---

# Session prompt — plan 3 fix r2, `test_isolation_and_xdist`

## 1. Role and mode

Finding-scoped fix cycle closing the coordinator's consumption of implement r1. Resolve
**B1–B4** and **S1–S6**, and answer the one open question in §7. **Resolve, don't relitigate.
Add nothing beyond the findings.**

Workspace root: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
Suite from `backend/app/`: `PYTHONPATH=. pytest -m 'not e2e'`
Branch `feat/test-isolation-xdist`. Implement r1 is checkpointed at **`8f62dde`**, clean.

**Read first, by absolute path, as this session's doctrine:**
`/Users/davidloorenz/agent-skills/pipeline-charter.md` (including **"Test-evidence scope and
reuse"**) and `/Users/davidloorenz/agent-skills/implementation-executor.md`.

**Read then:** your own `handoffs/implementer/2026-08-21_phase3_implement_r1_handoff.md`, and
`plans/plan_3.md` §7's implement-r1 entry. **`plans/plan_3.md` is your task list**; where this
prompt differs, the plan file wins.

**Numbering note.** `master_plan.md` §5 gained a rule since the r1 prompt was written. The
closing-stamp rule — *your stamp is defined by the tree you hand over* — is now **rule 9**, not
rule 8. Rule 8 is the gate-check rule. The r1 prompt's §5 quoted rule 8's text correctly under the
old number; nothing about the obligation changed.

## 2. What was verified and is not in question

Cited, tree-matched, coordinator-measured at `8f62dde`. **Re-running any of it is over-evidence
and a finding against the round.**

- **The harness is collection-neutral.** Measured by ID count, not by the summary line: unset →
  2594 IDs / 0 probes, `off` → 2594 / 0, each of `prefix`/`middle`/`suffix` → 2595 / 1, `bogus` →
  `pytest.UsageError`. The gate keys off the environment variable and never off worker identity,
  so every worker collects identically. Delegation 1's bound is met.
- **Residue is exactly as claimed.** `beyo_test_%` on `localhost:5433` returns
  `beyo_test_main_template` and nothing else.
- **Closing arithmetic is consistent with the tree:** `21 + 2573 = 2594` selected, which is what
  collection yields today.
- **The perimeter you declared is complete** — 15 files, matching `git diff c73c017 8f62dde`.
- **Archgraph** is 193 nodes / 290 edges at revision `6144a01a…`, additive, nothing promoted or
  edited. The 2 pending items are the node and its edge; they are the coordinator's to review.
- **`sorted(TERMINAL_STEP_STATES, key=…)` is correct and necessary.** A set iterated into
  `parametrize` orders per-process under `PYTHONHASHSEED`, workers disagree on collection, and
  xdist aborts. It is the round's best piece of work and it stays.

**Task 1's conclusion stands: the unstable union is empty.** Do not re-open it, do not re-run the
probe matrix. §7's open question is about a count, not about that conclusion.

## 3. Allowed file perimeter

The re-review verifies this against `git diff 8f62dde HEAD`; anything outside is an automatic
finding.

- `app/requirements.txt` — **B1 only**, a revert. See §4.
- `app/tests/database_isolation.py` — S1, S2, S3.
- `app/tests/integration/infrastructure/test_database_isolation.py` — the rows S1/S2/S3 need.
- `app/tests/integration/services/queries/app_update_presentations/test_get_active_presentation_integration.py`
  — **B4, and only if the repair is unambiguously inside the test.** See §5.
- `master_plan.md` (§6.1, §6.5, §8), `planning/intention.md` (revert only), `plans/plan_3.md`
  (§7 and the delegation table), and your handoff.

Do not touch the three probe modules, `pytest.ini`, `conftest.py`, or `requirements-dev.txt`.

## 4. Blocking findings

### B1 — `requirements.txt` is this session's `pip freeze`, reported as the owner's change

Your handoff says the `requirements.txt` diff "was preserved as an owner change visible at session
start" and "was not mechanically altered by the implementation"; `plans/plan_3.md` §7's delegation
table repeats it as "the existing owner change in `requirements.txt` was preserved".

**That is false.** `c73c017` was committed clean by the coordinator, and your own blocked report
before starting asserted a clean worktree. The entire diff belongs to this session. Its contents
are a freeze signature, not a hand edit: `pytest-xdist==3.6.1`, `execnet==2.1.2` (xdist's
transitive dependency), `psycopg2-binary==2.9.12`, and `Pillow` → `pillow` recased.

Delegation 3 asked you to name **which manifest** gets the plugin and why. You answered
`requirements-dev.txt` — correctly — but it shipped in both, so a test-only runner plugin and two
unrelated packages now enter every production install.

> **Revert `app/requirements.txt` to blob `f0d25ed` exactly.** Verify with
> `git diff c73c017 HEAD -- app/requirements.txt` returning empty, and assert on that — do not
> eyeball it (§5's standing rule 8 applies to your own scripted edits as much as to gate checks).
> Correct both published-forward statements: the handoff's perimeter section and plan_3 §7's
> delegation row. **Do not edit the r1 handoff** — corrections to a published document go in
> *this* cycle's handoff.

### B2 — thirteen evidence runs, zero tree identities

§7 of the r1 prompt required **SHA plus a `git diff` digest** on probe runs; §9.1 required tree
identity on the closing stamp. Neither the handoff nor `plan_3.md` carries a SHA, a
`git status --porcelain` assertion, or a diff digest anywhere, for any run.

Two documents then assert it exists. `master_plan.md` §8's new phase-3 row reads "closing
checkpoint recorded in the implementer handoff, clean". `planning/intention.md` §6 reads "The
phase-3 handoff carries the exact closing wall time, **tree identity**, residue query, and complete
ID enumeration." Both point at nothing, and every other row in §8 carries a SHA.

This is not bookkeeping. Phase 3 republishes a baseline that three other projects consume; without
a tree identity, no later session can establish that its tree matches, so **every number this phase
produced is unreusable as tree-bound evidence** and the next phase must re-measure from scratch.

> Every evidence record in your r2 handoff carries the tree it was taken on — SHA plus asserted
> `git status --porcelain`, and where the tree is dirty, the sha256 of `git diff`. `master_plan.md`
> §8's phase-3 row carries a real SHA in its Tree column like rows 1 and 2 do. Where a number in
> §8 was measured on a tree you can no longer identify, **say so in that row** rather than
> implying an identity you cannot produce.

### B3 — the handoff contradicts itself on the parallel results

Progressive narrative:

> Row `n+2` final result: `21/2572/0` in 78.35s, matching the serial comparator's 21 IDs in both
> directions; `pg_stat_activity` peak 20.
> Row `n+3` result: `21/2572/0` in 52.20s, matching the serial set in both directions;
> `pg_stat_activity` peak 22.

Closing table, same document:

> `-n 4 --dist loadfile` | `22 failed / 2572 passed`; serial 21 plus app-update ID | 52.38 s /
> 52.08 s | 23 / 23

And `master_plan.md` §8, published: "`-n 2` = 21/2573 on one run and 22/2572 on the
re-measurement".

A reader of the narrative concludes the parallel set matches serial. A reader of the table
concludes it does not. **OD-9's branch turns on exactly that question** — the shipped default is
serial *unless* the parallel failing-ID set matches the serial comparator exactly.

> Re-measure `-n 2` and `-n 4` on the tree you hand over and publish one unambiguous result per
> worker count, each with its tree identity. Mark every superseded r1 number as superseded, with
> the reason, in this handoff. If the narrative rows described runs on an earlier tree, say which
> tree and stop citing them as results.

### B4 — "the app-update path was not present when targeted inspection was attempted"

It is present, at
`app/tests/integration/services/queries/app_update_presentations/test_get_active_presentation_integration.py:182`,
named `test_selected_users_only_targeting`. It is findable by grep.

This is the most valuable thing phase 3 produced — the one real race parallelism exposed, the
reason OD-9's serial default is the right call — and it was retired to an unexplained
"parallel-only observation" on the strength of a file that could not be found. `intention.md` §1
forbids that disposal route by name, and phase 2's implement r1 was returned for the same shape
("a known concurrency failure").

It reproduces at `-n 4` and `-n 6`, so it is diagnosable.

> **Diagnose it and publish the mechanism.** Read the test and the query it exercises, and say
> what shared state makes it order- or concurrency-sensitive.
>
> Then apply the intention's own rule, which is a three-way branch, not a licence to repair:
> - If the repair is **unambiguously inside the test** — a fixture that adopts a globally-unique
>   row instead of creating one, a missing scope, an assertion over a set the test does not own —
>   repair it, and add nothing else while you are in the file.
> - If it needs a **production-domain call**, do not improvise: raise it as a **decision card** in
>   the charter's `⚠ OWNER DECISIONS REQUIRED (n)` section, with the mechanism, the branches, and
>   one recommendation.
> - If diagnosis genuinely does not converge, say what you ruled out and what evidence would
>   settle it. **"Not present" is not an available answer.**
>
> Weakened assertions, retries, `xfail` and `skip` remain forbidden by `intention.md` §1.

## 5. Significant findings

**S1 — the table-count assertion was deleted, not derived.** `_migrate_and_assert` now checks the
derived head plus `REQUIRED_PUBLIC_TABLES`, three tables out of 104. Removing the time bomb was
right; removing the check was not the only way. The projection handed you the derivation in F3:
`len(Base.metadata.tables)` = 104, `+ alembic_version`, `+ 2` migration-owned `_journal` tables
kept out of ORM metadata by the convention at `migrations/env.py:20-31` = **107**. A partially
applied migration that leaves 54 tables passes today.

> Restore a count assertion **derived at runtime** rather than pinned — the ORM metadata count
> plus the enumerated non-metadata tables — so a new revision moves it automatically and a partial
> application still reddens. Add the row that catches it: a database missing tables the metadata
> declares, with the required three present. If you judge the derivation unsound for a reason the
> projection missed, say what it is and leave the count out — but then `master_plan.md` §6.5's
> claim that the count "is not a schema contract" must state what does bound it instead.

**S2 — `_normalised_endpoint` folds `is_unspecified` into loopback.** N3 delegated *equivalent
loopback spellings*. `0.0.0.0` and `::` are the unspecified address, not loopback spellings, and
`plans/plan_3.md` §7's delegation table lists `0.0.0.0` among them — so this is a stated judgment,
which is why it needs answering rather than patching. Inside a fail-closed guard that decides
whether a `DROP DATABASE` is authorised, treating "any interface" as "this host" is a widening.
C7's negative row tests a genuinely different host; nothing tests the unspecified address in
either direction.

> Drop `address.is_unspecified` from the loopback branch, and add the row that pins it: a
> `target_database_url` on `0.0.0.0` against a configured `127.0.0.1` endpoint is **refused**. If
> you believe the widening is required for a real configuration, name the configuration — a guard
> relaxation needs a reason that exists, not one that might.
>
> `::1` → `127.0.0.1` equates two address families and is a smaller version of the same question.
> Keep it if you can name why the two are the same server here; say so either way.

**S3 — `_set_marker(self.worker_database_name)` was removed from `start()` without mention.** The
r1 diff replaces four calls with three inside the lock. It is probably correct —
`CREATE DATABASE … TEMPLATE` copies the marker schema — but the marker is **step 4 of the
five-step fail-closed guard**, and its provenance changed from "written by us after creation" to
"inherited from the copy". No design note records the change and no row proves the worker database
still carries it.

> State the removal and its reason in the review log, and add the row that proves the guarantee:
> a freshly started worker database has the marker, and `assert_disposable_database` accepts it.

**S4 — the reported L4 total of 11 is not the number you ran.** Your own closing table gives `-n 4`
two wall times (52.38 s / 52.08 s) and two peaks (23 / 23); your own narrative describes both an
`-n 2` diagnostic *and* an `-n 2` re-measurement. Enumerating what the document describes gives at
least **13**. The re-measurements carry pre-run authorization lines and are legitimate under the
charter; the second `-n 4` has none.

This is the third round in this project where the stated count and the narrated runs disagree.

> Report r1's actual total as a number, derived by enumerating the runs your own document
> describes, with the two unauthorized ones named. Then report **this** cycle's total separately.
> The count is a fact about the session, not a restatement of the budget line.

**S5 — `master_plan.md` §6.1 now carries a false environment fact.** It reads
"`pytest-xdist==3.6.1` is now pinned in the development manifest". After B1's revert that becomes
true; until then §6 — the section every future session is told to **cite rather than re-derive** —
is wrong about the tree. Correct it as part of B1, and confirm it matches the reverted manifests.

**S6 — `planning/intention.md` was written by the implementer.** §9.6 of the r1 prompt said "the
intention's named deliverables", meaning the deliverables the intention names; it was ambiguous
enough to invite the reading you took, and the coordinator owns that half. Nothing upstream was
corrupted — §6 is a delivery record, not a semantic amendment — but the intention is the semantic
authority and implementation sessions do not write into it.

> Revert `planning/intention.md` to `c73c017`. Move the before/after table and the lifecycle
> diagram into `plans/plan_3.md` §7, where §9.6's deliverables belong, with the corrected numbers
> from B3 and the tree identities from B2.

## 6. Evidence budget

**This session's L4 budget is exactly 5 runs**, enumerated, all on the tree you hand over:

1. `-n 2 --dist loadfile` — B3's re-measurement.
2. `-n 4 --dist loadfile` — B3's re-measurement, and the row OD-9 turns on.
3. `-n 6 --dist loadfile` — the published §8 row, re-established on the fixed tree.
4. The closing serial stamp at the shipped default.
5. A second run at the shipped default — C5's second scheduling condition.

Each carries tree identity, the enumerated failing-ID set, and the delta in **both** `comm`
directions against the serial comparator. **State your L4 count as a number.**

Everything else is L1/L2: every named mutation at its own site, B4's diagnosis (run the app-update
file alone under workers — that is L2, not L4), the criterion module, and every reality check.
Anything beyond these five needs the charter's authorization line **written before the run**.

## 7. One open question — answer it, no run required

Rows 0 and 0b reported `21 / 2562 / 1` = **2583 selected**. The published baseline is
`21 / 2561 / 1` = **2582 selected**, and rows 0/0b ran with the probes filtered out. So the
noise-floor tree carried one test the comparator's tree did not.

The failing-ID sets matched in both directions, so task 1's conclusion is unaffected — but **C5's
contract is the word "explained"**, and a one-test drift in the control run is exactly the kind of
thing that criterion exists to force into the open.

> Reconstruct what the tree held at row 0 and account for the extra row. This is archaeology
> against your own change sequence, not a measurement. If you cannot account for it, say so
> plainly — an unexplained +1 that is *reported* is a note; an unexplained +1 that is *unmentioned*
> is what C5 was written to catch.

## 8. Scope fences

- **No new features, no refactors, no scope beyond B1–B4, S1–S6 and §7.**
- **Do not re-open task 1.** The harness is verified collection-neutral and the unstable union is
  empty. Do not re-run the probe matrix, do not move the probe modules, do not change their
  gating.
- **Nothing under `app/beyo_manager/`.** The `config.py` carve-out is spent (OD-7).
- **No default `-n` or `--dist` in `pytest.ini`.** OD-9 fixes the shipped default as serial unless
  the parallel failing-ID set matches the serial comparator exactly — and after B3 and B4 you will
  know whether it does. Report the matrix either way; if it now matches, that is an owner decision
  card, not a change you make.
- **`migrations/versions/` stays outside the perimeter.** C6's temporary revision is created and
  removed inside the test and declared in your write perimeter.
- **The three archgraph items are `human_confirmed`.** Record deltas additively; never promote,
  reject or edit. If B4's diagnosis changes the isolation contract, that is an additive record.
- **A published handoff is never edited.** Every correction to an r1 number goes in the r2 handoff.

## 9. Closing protocol

1. **The closing L4 stamp on the tree you hand over**, with tree identity — SHA plus asserted
   `git status --porcelain` — the enumerated failing-ID set, and the delta in both directions.
   Master plan §5 **rule 9**: if you change anything after stamping, the stamp is void and
   re-taking it is **not** over-budget. Two rounds in this project have lost a stamp to the
   opposite reading.
2. **One evidence record per named mutation**, at the site the criterion names, stating which
   criterion rows reddened. Not classes. A sub-check whose disabling reddens nothing is **a finding
   to report**, not a gap to fill silently.
3. **Document writes**: `master_plan.md` §6.1 (B1/S5), §6.5 (S1), §8 (B2/B3 — a real SHA in the
   Tree column); `planning/intention.md` reverted (S6); `plans/plan_3.md` §7 gains the corrected
   delegation row, the deliverables moved out of the intention, and this cycle's review-log entry.
   The master plan §3 tracker row is the coordinator's.
4. `plans/plan_3.md` frontmatter → `state: IMPLEMENTED` with date, actor, and the corrected counts.
   **This project's state lives in plan frontmatter**; there is no separate tracker file.
5. **Handoff** at `handoffs/implementer/2026-08-22_phase3_fix_r2_handoff.md`, frontmatter
   `plan: 3` / `role: fix` / `state` / `date` / `actor`. Perimeter declared **cycle-scoped — the
   files *this session* changed** — with mutation-probe files listed separately from your own
   changes, with checksums, every probe database and its verified disposition, and **your L4 count
   as a number**.
6. **Corrections to numbers published in r1 go in this handoff**, each naming the r1 statement it
   supersedes and why.
7. Anything only the owner can settle goes in the handoff as a **decision card** in the charter's
   `⚠ OWNER DECISIONS REQUIRED (n)` section. B4's second branch is the likely candidate. If
   nothing needs the owner, one line saying so.

The handoff file, not your chat message, is what the coordinator consumes.
