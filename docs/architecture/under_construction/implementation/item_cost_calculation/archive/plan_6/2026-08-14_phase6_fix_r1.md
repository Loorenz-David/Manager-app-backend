---
plan: phase 6 (legacy money migration & API bridge)
role: implementer
round: 2 (fix cycle r1 — resolve review r1: B1, B2, S1–S4 + named notes; corrections pre-verified)
date: 2026-08-14
---

# Session prompt — phase 6 fix cycle r1

You are the **fixing agent** for phase 6. Both blockers live in the migration's
safety net — the one run nobody gets to repeat — and both corrections were
EXECUTED by the r1 reviewer. **Resolve, don't relitigate.**
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(commands from `backend/app/`, per master plan §10).

## Doctrine (read by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md` — fix-cycle variant.

## Gate check

- Tracker: phase 6 `CHANGES_REQUESTED`. Owner card 1 ANSWERED: **leave deleted
  prices deleted** — folded as **R15-1** (§10A.1(c) as corrected: eligibility
  = `NOT EXISTS (any item_valuations row for the item)`; never-valued items
  only).
- Read the review r1 handoff
  (`handoffs/reviewer/2026-08-14_phase6_review_r1_handoff.md`), the plan's
  **"Fix r1 amendments" block — GOVERNING**, and the r1 Review log entry (the
  reviewer's correction hashes R9/R10 live there).

## Items

1. **B1:** rebuild `_assert_postconditions` per R14(a) — the created-valuation
   side compared against the ELIGIBLE-at-entry count (independent
   constructions). The reviewer's executed correction is your target shape
   (R9 green / R10 aborts + rolls back). Named mutation: the `rows[1:]` skip
   must ABORT the upgrade with the unmigrated-count message.
2. **B2:** the eligibility predicate per R15-1. C1 gains the four
   per-valuation-state sole-predicate rows (never-valued → migrated;
   current-valued → collision, journaled only; soft-deleted-only → SKIPPED;
   superseded-only → SKIPPED with the recorded unreachability judgment).
3. **S1:** the nine census rows each consume `endpoint_id` with a distinct
   expression; ORM `Item` fixtures (rule 3). The reviewer's R3 probe (inline
   re-exposure in `upholstery_orders_query.py`) must redden exactly its row.
4. **S2:** refusal row-report content assertions (ids present; per-class
   identity). The R2 probe must redden.
5. **S3:** the three missing TestClient 422 rows (PUT/PATCH/find-or-create).
6. **S4:** parametrize the migration case tables with authority-naming ids.
7. **Notes:** N2 (plan text + structural row: enum users at head = 2), N4
   (idempotency single-cause + post-conditions re-run on pass 2), N5
   (identity not count), N6 (intermediate assertion between the downgrades),
   N7 (labeled synthetic tie test), N8 (docstring parent + logger).

## Ledger (the r1 defect class closes here)

Every row declared against the FINAL committed hashes — never pre-checkpoint
states (r1's probes 1–2 were unverifiable for exactly this). One row per
named mutation, full observed red sets, divergences flagged, hashes
copy-pasted. Include: the B1 abort mutation, the R3 census probe, the R2
report probe, and re-runs of any r1 row whose file this fix touches.

## Perimeter

Production: `migrations/versions/5420acc6a7b3_….py` ONLY (B1 guard + B2
predicate — the migration is UNAPPLIED on any real database beyond dev, and
the dev DB is at head with zero migrated rows, so editing the file is safe;
state this in the handoff; rule 7 still binds: verify `alembic_version` on
the configured DB is untouched and the journal stays empty). Tests: the
phase-6 test files + the phase-5 valuation-surface file (N7's labeled tie
test). Docs: plan text correction (N2), master plan tracker, Review log,
your handoff. **Nothing else.**

## Closing protocol

1. All rows green; the full re-declared ledger; the migration lifecycle
   re-run on seeded disposables (all four eligibility states + the three
   refusals + run-twice + full round-trip), end states by STATE QUERIES;
   every disposable dropped and listed.
2. Suite: expect 1997+N / 23 byte-identical / 1 deselected (read off the
   run); ruff; configured DB at head `be9dfe42a035`, journal at 0 rows.
3. Tracker → `IMPLEMENTED` (fix r1); Review log per P-L.
4. Checkpoint `CHECKPOINT (not approved): item-cost phase 6 fix r1 —
   <summary>`; handoff AFTER, citing the FINAL hash, at
   `docs/architecture/under_construction/implementation/item_cost_calculation/handoffs/implementer/2026-08-14_phase6_fix_r1_handoff.md`
   (full path). `⚠ OWNER DECISIONS REQUIRED (n)`.
