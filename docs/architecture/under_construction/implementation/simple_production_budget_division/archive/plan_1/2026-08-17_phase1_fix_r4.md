---
plan: 1
role: implementer
round: 4 (fix cycle after re-review r3)
date: 2026-08-17
pipeline: simple_production_budget_division
---

# Fix round 4 — plan 1 (S7: the tenant-boundary row)

One finding, one fixture row, one probe. Re-review r3 closed everything else in
the phase; this round exists because criterion C14 has demanded an
other-workspace row since round 1 and that row was never written — so E2's
top-level workspace filter is currently unguarded (the reviewer's probe deleted
it and all 26 phase tests stayed green). **Production is correct; do not change
it.** You are adding the test that keeps it correct.

Owner authorized this round explicitly on 2026-08-17 after hearing the trade-off:
the failure mode is cross-workspace task-existence disclosure on an all-roles
endpoint, and it is the only tenant boundary in the phase.

## Read first

1. `handoffs/reviewer/2026-08-17_phase1_rereview_r3_handoff.md` — finding S7,
   including the reviewer's correction sketch and the probe-4 equivalence you
   must NOT act on (the M1 subquery's step-level workspace filter is redundant
   via globally-unique section ids — leave it exactly as it is).
2. `plans/plan_1.md` — the lettered rows as amended this round: **C14c** is now
   unknown-id omission only, and **C14d** is the new tenant-boundary row with its
   named RED mutation.
3. `master_plan.md` §6 — the rules in force for this round: tenant-boundary-row,
   letter-verification, no-weaker-assertions, guard-is-the-reason.

## Fix item

- **F1 / S7 → criterion C14d** — in
  `app/tests/integration/services/queries/item_economics/test_budget_allocations_query.py`:
  1. In `_seed`, create a second `Workspace` and one `Task` inside it (minimal —
     the task needs no item, evaluation, or steps; its mere existence is the
     test subject). Follow the file's existing seeding style and return it from
     `_seed` like the other fixture handles.
  2. Extend the existing three-task call's id list to include that foreign task
     id as a fourth element, and assert it is **absent**:
     `assert foreign_task.client_id not in {row["task_id"] for row in three["budget_allocations"]}`.
     Keep the existing `len(...) == 2` assertion — with four ids in and two rows
     out, it independently proves the omission (no-weaker-assertions rule: this
     round only adds).
  3. `_cleanup` must delete the foreign task AND the foreign workspace, in FK-safe
     order, in the same teardown-owning structure the file already uses (charter
     rule 11½). Name the tables your residue check covers in the handoff — the
     configured DB must be left with zero rows from this fixture.
  4. If the query-count fixture is the same test node, verify the added id does
     not change the pinned `first_count == 11` (a fourth id is filtered out in
     the same visibility query — the count should hold). If it DOES change, do
     not weaken the pin: report it as a STOP item with the observed numbers, so
     the coordinator can decide whether the count moves legitimately.

## Discipline

- **Delta ledger: exactly one probe.** Apply C14d's named mutation at its
  definition site — delete `Task.workspace_id == ctx.workspace_id` from
  `get_task_budget_allocations.py:109-113` — run the C14 test node, record the
  exact red output line, revert byte-for-byte. Confirm afterwards with
  `git diff 99ade31 -- app/beyo_manager` returning empty.
- Perimeter: **that one test file** plus the pipeline docs rows that are yours.
  No production files, no README, no graph changes.
- Full suite: `PYTHONPATH=. pytest -q -m 'not e2e'` from `backend/app/` —
  expected 26 failures = the 23 baseline IDs + the 3 foreign
  `test_seed_item_economics_configuration.py` IDs, zero others. Record totals and
  the full failure list.

## Checkpoint + handoff

`CHECKPOINT (not approved): plan1 fix r4 — tenant-boundary row for C14d`.
Handoff: `handoffs/implementer/2026-08-17_phase1_fix_r4_handoff.md` — frontmatter
(`round: 4, state: IMPLEMENTED`), checkpoint hash, write perimeter, F1 outcome,
the one-probe ledger (mutation → node → exact red line → revert + the empty-diff
confirmation), the residue-check table list, suite totals + failure list, and
STOP items if the query count moved.
