---
plan: 4
role: implementer
round: 4
date: 2026-08-22
project: live_clock_for_working_time_economics
---

# Phase 4 — fix r4: two should-fix and two notes, all in one document

You are the **implementing agent** for phase 4's second fix cycle. Review r3 (independent,
Opus 5) returned **CHANGES_REQUESTED — 0 blocking, 2 should-fix, 3 notes**. Every change
below lands in **one file**, in **two sections**.

Workspace root: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`

Doctrine by absolute path, as before: `/Users/davidloorenz/agent-skills/pipeline-charter.md`
and `/Users/davidloorenz/agent-skills/implementation-executor.md`.

**Resolve, don't relitigate; add nothing beyond these findings.** Where this prompt
differs from `plans/plan_4.md`, the plan file wins.

## What review r3 settled in your favour — do not touch, do not re-verify

The reviewer read source rather than your account of it, and everything held. **All five
node descriptions are confirmed true at source**, clause by clause — including the two most
falsifiable, the batched allocations' "one shared map loaded once per request"
(`get_task_budget_allocations.py`: one `load_live_worked_seconds` call at 124, indexed by
every task row) and production time's "passed both to budget status and into the
allocator's response-only step rows" (`get_task_production_time.py`: 42, 48, 55). The
price-scenario node's three clauses hold, including the load-bearing one — it consumes only
`budget_status.status` and `budget_status.item_binding`, neither worked-derived. C1–C4, C6,
C7, C8 pass; C4 carries both citation halves and the eight-row consumer list, checked row by
row against §2.5A. The graph delta is finished: **the five items were adjudicated by the
owner on 2026-08-22 and promoted — 0 pending, 0 stale, 0 diagnostics.** No graph work
remains in this phase.

**Your §4.1 cost passage was singled out** as the most carefully-carried in the document —
it matches §3.4A term for term, including the corrected `min(...)` denominator rather than
§3.4's superseded 50-task cap. Leave it alone.

## S1 (should-fix) — record deletion is named to the client

**Where.** New handoff §5, decrease mode 2.

**Correction clause, quoted from the review verbatim:**

> Delete the sentence *"Record deletion is not a shipped client event and is not a cause to
> handle."* from §5 mode 2, changing nothing else in that mode. **Source of truth:
> intention §5.4 and master_plan §7 obligation 5**, both of which state that record
> deletion is not named to the client. Mode 2's surviving general rule — *"A drop larger
> than 1 second is authoritative: snap down immediately to the served value…"* — already
> covers a decrease from any cause, including one our API cannot emit, so nothing is left
> uninstructed by the removal.

**You were told to leave this sentence alone in fix r2, and that instruction was correct
at the time** — it was reserved as the reviewer's judgment call so they would meet it
unresolved. They decided it against the sentence, on the authorities' literal text: both
say the event *"is not named to the client"*, not merely that it is not named *as a cause*.
Plan 4's C5 carried the narrower paraphrase, which is why every earlier round passed it;
**C5 has been amended to quote the authority instead**, so the criterion and the authority
now agree.

The reviewer also established at source that **no behavioural harm followed** — E5's only
writer is the workspace reset, which deletes the steps, tasks and workspace in the same
run, so no client could observe such a decrease — and that no published handoff has ever
named this event, so the sentence retires nothing and introduces the topic. Delete it.

## S2 (should-fix) — the published baseline omits its instability caveat

**Where.** New handoff §7, which publishes the 21-ID set as *"the durable comparator"*.

**Correction clause, quoted verbatim:**

> Add to §7's baseline block that **at least two named tests in this suite are intermittent
> and are not members of the 21**, that a third intermittent test's identity is
> unrecoverable, and that **a single run is therefore not evidence — repeat and ID-diff
> before concluding the set has changed**. **Source of truth: master_plan §6**, the
> "⚠ Suite instability" and "⚠ A THIRD intermittent test" bullets. If the isolation work is
> believed to have retired that instability (plausible — its causes were dev-database and
> collection-order coupling), that must be **measured and stated as re-measured**, not
> assumed by silence; the bullets stand unamended in §6 today.

**Why this matters more than it looks.** The reviewer measured that neither named flaky
test is a member of the published 21 — so for the consumer, `narrow_typical_work_times`
D23, **a flake cannot show up as a missing ID; it shows up as a new failing ID, which reads
as a regression they caused.** That is the exact misreading your own subset bullet prevents
for the 26→21 transition, reproduced one bullet later by omission. The third intermittent
test makes it symmetric: the set can also shrink.

The two named tests, from master §6:
`test_phase4_fix_coverage.py::test_c3_real_concurrent_open_insert_translates_the_loser[model]`
and
`test_process_shopify_products_integration.py::test_process_shopify_products_fans_out_to_all_active_workspace_shops_and_enqueues_one_task`.

**Take the last sentence of that clause seriously.** Do not write that the isolation work
fixed the flakiness. If you want to claim it, measure it; otherwise state the caveat as §6
states it. Master §6's gate block has been amended today to say explicitly that it does
**not** supersede those bullets — they are live.

## N1 (note, folded into the same edit) — the Redis precondition ships without its number

§7 states that Redis must be reachable and omits what happens otherwise. **Correction:**
add that a machine without Redis measures **23 failed / 2 errors, not 21**, so a mismatched
count is diagnosable rather than mysterious. Source of truth: master_plan §6's Redis
precondition bullet. This sits in the same block as S2 — one edit.

## N2 (note, reviewer-endorsed clarification) — name the referent of "snap"

**This is not a defect in your text.** §5's closing rule — *"smoothing may add elapsed time
after receipt, but it must snap down to the served value rather than clamp"* — is a
near-verbatim lift of intention §5.4, and you carried your authority faithfully. The
ambiguity was **in the intention**, which used "snap" for two different surfaces across
three sections, and it has been resolved upstream today (intention round 4i): **"snap"
governs the smoothing baseline, never the rendered value.**

**Correction, in the reviewer's own words** — *"a strict improvement [that] contradicts
nothing"*: make §5's closing rule read that smoothing must snap its **smoothing baseline**
down to the served value rather than clamp. That removes the last place where a frontend
developer would have to reconcile "must snap down" against mode 1's "a visible snap is not
required" — which are now both true and no longer look contradictory.

## Scope fence

Four edits, one file, two sections: §5 mode 2 (delete one sentence), §5 closing rule (one
noun), §7 baseline block (instability caveat + Redis number). **No graph work** — the queue
is adjudicated and closed. **Nothing under `app/`.** No other section of the document, and
no other document except your own tracker row, plan Review log and handoff.

## Allowed file perimeter — the re-review will verify this

- `docs/handoff/to_frontend/HANDOFF_TO_FRONTEND_live_working_time_clock_20260822.md`
- `docs/…/live_clock_for_working_time_economics/plans/plan_4.md` — frontmatter + §7 log
- `docs/…/live_clock_for_working_time_economics/master_plan.md` — **your own new tracker
  row**, added above the previous one, never over it (fix r2 got this right)
- `handoffs/implementer/2026-08-22_phase4_fix_r4_handoff.md`

## Evidence budget

**L4: exactly 0.** The derivation is unchanged and still holds — `git diff 0aae85e HEAD --
app/` is empty and this cycle changes nothing under `app/`, so the authoritative
21 / 2576 stamp is citable by tree identity. Run `PYTHONPATH=. pytest tests/unit/docs/`
from `app/` after your edits (L1); the new document is inside that guard's sweep, and
**that sweep is measured non-vacuous over this exact file** — the coordinator planted the
retired identity token in it and the guard went red, 1 failed / 58 passed. You do not need
to re-establish that.

**Identify your tree properly this round:** SHA **plus a digest of `git diff`** if it is
dirty when you measure. Fix r2's evidence row gave a SHA and a prose description; the
charter asks for the digest, because a SHA alone can name two different dirty trees.

## Closing protocol

Docs guard green → plan frontmatter + §7 Review log → your own tracker row → checkpoint
commit prefixed `CHECKPOINT (not approved):`, **explicit paths, never `git add -A`** →
handoff at `handoffs/implementer/2026-08-22_phase4_fix_r4_handoff.md` declaring the full
write perimeter including tool-recorded state (**none expected this round — say so
explicitly**), one row per finding, your L1 evidence row with tree identity, and an
`⚠ OWNER DECISIONS REQUIRED (n)` section or one line saying zero.

**Rule 14 binds:** if you do not implement one of the corrections quoted above, say which
and why in its own section.
