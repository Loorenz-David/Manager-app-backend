---
plan: phase 5 (valuation surface)
role: implementer
round: 3 (fix cycle r2 — resolve re-review r2: S1 + the N3 ride-along; correction pre-verified)
date: 2026-08-14
---

# Session prompt — phase 5 fix cycle r2 (one test-side item)

You are the **fixing agent** for phase 5, round 3. ONE should-fix, test-side
only, correction executed and verified by the r2 reviewer. **Resolve, don't
relitigate; touch nothing else.**
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(commands from `backend/app/`, per master plan §10).

## Doctrine (read by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md` — fix-cycle variant.

## Gate check

- Tracker: phase 5 `CHANGES_REQUESTED` (re-review r2: 0 blocking, 1 should-fix).
- Read the r2 handoff
  (`handoffs/reviewer/2026-08-14_phase5_rereview_r2_handoff.md`, finding S1 +
  note N3) and the plan's **"Fix r2 amendments" block — GOVERNING**.

## The item

**S1 — the L15 structural guard quantifies over its module set.** In
`tests/unit/domain/item_economics/test_configuration.py`, rework
`test_item_major_category_snapshot_is_read_only_by_the_registered_resolver`:
every in-scope occurrence of `item_major_category_snapshot` (across ALL of
`module_sources` — the two L15 roots) must be a
`resolve_major_category(...)` argument; assert `unmediated == {}` per the
reviewer's executed six-line correction (verbatim in the handoff). Drop the
`ItemMajorCategoryEnum(` absence check (does not generalise); keep or drop the
`set_source` string asserts at your judgment.

**N3 ride-along (same file):** one comment above the currency-mismatch
parametrize naming which CLAUSE each row arbitrates (`[basis-model]` proves
`valuation ≠ basis`; `[valuation-basis]` proves `basis ≠ model`).

## Ledger (P-I as extended — one row per named mutation, hash beside each)

Three rows, each with its OWN mutant hash (P-I seventh ext — no label reuse):

- **M4a** (inline chain in `set_item_valuation._load_preview_inputs`, mutant
  `df1f79b3…`) → the guard row red.
- **M4b** (second unmediated reader, same module, mutant `e1ca0625…`) → red
  (was 363 green in r2).
- **M4c** (snapshot-classifying helper in `delete_item_valuation.py`, mutant
  `88c9f5aa…`) → red (was 363 green in r2).

Expected baseline: `test_configuration.py` **9 passed**; focused selector 363;
full suite **1968 / 23 / 1** (test rework adds no node — state the numbers
READ off your run). Copy-paste all hashes.

## Perimeter

`app/tests/unit/domain/item_economics/test_configuration.py` — the ONLY code
file. Docs: master plan tracker row; plan Review log; your handoff. Archgraph:
read-only, zero delta (12 pending stay held; state revision `b5e6fe09…`).

## Closing protocol

1. Rows green; the three-row ledger with observed ids + copy-pasted sha256
   pairs.
2. Full suite once (numbers read off the run, failure set byte-identical);
   ruff on the changed file; DB at head `5caae620088c`.
3. Tracker → `IMPLEMENTED` (fix r2); Review log per P-L.
4. Checkpoint `CHECKPOINT (not approved): item-cost phase 5 fix r2 — L15 guard
   quantified`; handoff AFTER, citing the final hash, at
   `docs/architecture/under_construction/implementation/item_cost_calculation/handoffs/implementer/2026-08-14_phase5_fix_r2_handoff.md`
   (full path). `⚠ OWNER DECISIONS REQUIRED (n)`.
