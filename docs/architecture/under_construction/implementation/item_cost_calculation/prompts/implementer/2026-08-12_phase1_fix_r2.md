---
plan: phase 1 (worker money redaction)
role: implementer
round: 2 (fix cycle)
date: 2026-08-12
---

# Session prompt — phase 1 fix cycle (resolve review r1 findings S1, S2)

You are the **fixing agent** for phase 1. Resolve the review's findings; **resolve,
don't relitigate; add nothing beyond the findings.**
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(branch `fix/idempotent-completion-analytics`). Project folder:
`docs/architecture/under_construction/implementation/item_cost_calculation/`.

## Doctrine (read first, by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md` — you are the
   fix-cycle variant of this role.

## Execution environment (NOT optional)

Run all test commands **with elevated permissions** — PostgreSQL and Redis are local
Docker services on `127.0.0.1:5433` and `127.0.0.1:6380` and are inaccessible from
the normal Codex sandbox (master plan §10). A sandboxed run must never be recorded
as evidence; if elevation is unavailable, stop and report.

## Gate check

- Tracker shows phase 1 `CHANGES_REQUESTED` (reviewer r1).
- Read the full **Reviewer r1** entry in
  `plans/phase_1_worker_money_redaction.md` Review log — it is the authoritative
  findings source.

## Findings to resolve (correction clauses quoted verbatim from the review)

### S1 — five acceptance-criteria rows have no asserting test (rows 9, 12, 17, 20, 23)

> *Correction:* add `"admin"` to the three parametrize lists named above (and an
> ADMIN pass for the pending assertion at `:276`), and give
> `test_worker_stats_endpoint_split_integration.py::_ctx` a `role_name` parameter so
> `test_last_interacted_steps_keep_money_for_manager` (`:256`) also runs under
> ADMIN; each added row asserts `== 4321`, not key presence.

The three parametrize lists (from the finding):
`test_list_working_section_steps_payload_characterization.py:228`,
`test_get_user_last_active_step_record_integration.py:154`,
`test_reassigned_steps_integration.py:262`. The coordinator declines the
alternative (amending the criteria table): the 24-cell enumeration was deliberate
(round-5 correction); the tests meet it.

### S2 — the recorded suite baseline is wrong

> *Correction:* replace the baseline numbers in the implementer entry (or add a
> correction line) with the verified pair above and the complete 23-item list, so
> phase 2 does not compare against a number that was never measured.

The verified pair (from the review): `545e504` → 1578 passed / 23 failed /
1 deselected; `4416570` → 1600 passed / 23 failed / 1 deselected; failure sets
byte-identical. Add a **correction line** under the implementer r1 entry (do not
rewrite the original — the wrong record is part of history) carrying the pair and
the complete 23-item failing-test list, which you obtain from your own elevated-
permissions run at your working HEAD (the sets are stable; if your run disagrees,
report the difference rather than papering over it).

## Optional notes (fixer's judgment; each documented in your handoff if taken)

- **N3:** have the seed helper return the task (or query it) instead of
  reconstructing `client_id` in `test_worker_money_redaction.py:32,:48`.
- **N4:** whitespace tidy at `test_get_worker_daily_step_breakdown.py:136-137` and
  `test_reassigned_steps_integration.py:250`.
- **N6:** split rows 19/22 into per-endpoint test functions in
  `test_reassigned_steps_integration.py`.

## Allowed file perimeter (the re-review will verify this exactly)

- `app/tests/integration/services/queries/working_sections/test_list_working_section_steps_payload_characterization.py`
- `app/tests/integration/services/queries/working_sections/test_get_user_last_active_step_record_integration.py`
- `app/tests/integration/services/queries/task_step_acknowledgments/test_reassigned_steps_integration.py`
- `app/tests/integration/services/queries/worker_stats/test_worker_stats_endpoint_split_integration.py`
- `app/tests/integration/services/queries/tasks/test_worker_money_redaction.py` (N3 only)
- `app/tests/integration/services/queries/worker_stats/test_get_worker_daily_step_breakdown.py` (N4 only)
- `docs/architecture/under_construction/implementation/item_cost_calculation/plans/phase_1_worker_money_redaction.md` (Review log: S2 correction line + your fix entry)
- `docs/architecture/under_construction/implementation/item_cost_calculation/master_plan.md` (your tracker row note ONLY — and keep the existing actor stamps intact; see N5 in the review)
- your handoff file

**No production code changes** — the review found no behavioral defect. Any file
outside this list is an automatic re-review finding.

## Closing protocol

1. Full suite green against the verified 23-failure baseline (elevated permissions).
2. Review log: your fix-r2 entry (what each finding got; which optional notes you
   took and why; test counts before/after).
3. Tracker row → `IMPLEMENTED` (fix r2) — add detail to the Note column; do not
   overwrite prior actors' stamps in ways that erase who did what.
4. Checkpoint commit: `CHECKPOINT (not approved): item-cost phase 1 fix r2 — <summary>`.
5. Deposit the handoff at
   `handoffs/implementer/2026-08-12_phase1_fix_r2_handoff.md` (frontmatter `plan`,
   `role`, `round: 2`, `date`, `state`, `verdict`, `actor`): summary;
   `⚠ OWNER DECISIONS REQUIRED (n)` (one line if zero); finding-by-finding
   resolution; full write perimeter incl. the checkpoint hash. **Depositing this
   handoff is part of the work** — review r1 skipped its handoff file and that gap
   is recorded; do not repeat it.
