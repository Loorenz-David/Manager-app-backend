---
plan: phase 3 (canonical calculator)
role: implementer
round: 3 (fix cycle)
date: 2026-08-12
---

# Session prompt — phase 3 fix cycle r3 (resolve re-review r2: B3, S4, S5)

You are the **fixing agent** for phase 3, round 3. **Resolve, don't relitigate;
add nothing beyond the findings and the routed card decision.**
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(branch `fix/idempotent-completion-analytics`). Project folder:
`docs/architecture/under_construction/implementation/item_cost_calculation/`.

## Doctrine (read by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md` — fix-cycle variant.

## Gate check

- Tracker: phase 3 `CHANGES_REQUESTED` (reviewer r2).
- Read the reviewer r2 Review-log entry and the handoff
  `handoffs/reviewer/2026-08-12_phase3_rereview_r2_handoff.md`. The owner answered
  the card (recorded in `planning/owner_decisions.md`; folded as intention
  **round 10, R10-1**): **cover every malformed input** — `rederive` is total.

## Findings to resolve

### B3 — no `ValidationError` escapes `rederive`, on ANY path (scope per R10-1)

Intention §6A.11's round-10 paragraph is the contract: all three input classes —
(i) value disagreement, (ii) malformed term snapshot (incl. NULL typed values and
duplicate `item_purchase_cost` rows), (iii) malformed evaluation snapshot (zeroed/
invalid stored rate, missing snapshot field) — return the integrity-marker result;
`rederive` catches/converts the calculation-path guards' exceptions into the marker
payload. The reviewer proved three live escapes:
(a) stored rate `0` → the S2 refactor records the mismatch but then dies at
`calculate_allowed_worker_minutes(budget, stored_rate)` (`calculator.py:465`) —
`ITEM_COST_RATE_UNDERFLOW` escapes;
(b) corrupt term shape → `ITEM_COST_TERM_SHAPE_INVALID` out of
`calculate_term_amounts` (`:439`);
(c) purchase term with NULL cost → `ITEM_COST_PURCHASE_COST_REQUIRED`.

> Correction (reviewer, scope resolved by the card): no path out of `rederive` may
> be a `ValidationError`; fold these into the `REDERIVE_MISMATCH` payload (or a
> sibling integrity marker), with one criterion row per class and a named mutation
> each.

Cascade behavior is pinned (R10-1): a mismatched rate ALSO reports its derived
`allowed_worker_minutes` entry — keep it, and S5's cascade row asserts it.

### S4 — the inert C9 row (correction verified end-to-end by the reviewer)

> swap the fixture in **both** C9 tuples to
> `calculate_percent_consumed(Decimal("0.01"), Decimal("100000.00"))` — with the
> wrapper intact 59 pass; with the wrapper removed the row reddens
> (`InvalidOperation` at `prec=6`).

### S5 — the mismatch payload's field branches

> one row per field branch asserting its exact payload, plus a row pinning the
> cascade.

Four branches: `term[<name>].amount_minor`, `production_budget_minor`,
`allowed_worker_minutes`, `cost_per_worker_minute_minor_snapshot` — plus the
cascade row (rate mismatch → both `cost_per_worker_minute_minor_snapshot` AND
`allowed_worker_minutes` entries, exact fields list asserted).

### Optional (fixer's judgment, documented if taken)

- **N9:** give the `CALCULATION_VERSION` constant's own docstring an arbiter (or
  consolidate to one docstring so two copies cannot drift).
- **N10:** remove the redundant outer `localcontext()` in
  `calculate_variance_worker_minutes`.
- **N11:** fix the stray indentation at `calculator.py:390`.

## Per-row mutation discipline (P-I as extended by L6 — binding)

Every row you add or change gets its own named mutation, run and reverted, with
the reddened row id declared **per row** — never a blanket "the new rows red".
B3's three classes: e.g. "re-raise instead of convert at the class-(a) seam" must
redden exactly the class-(a) row; same shape for (b) and (c). S4's row: wrapper
removal reddens it. S5: for each branch row, corrupt exactly that field.

## Allowed file perimeter (the re-review verifies exactly)

- `app/beyo_manager/domain/item_economics/calculator.py`
- `app/tests/unit/domain/item_economics/test_calculator.py`
- the plan's Review log + master-plan tracker row (Note append; stamps preserved)
- your handoff

## Closing protocol

1. All rows green; per-row mutations declared; final sha256s of both files.
2. Full suite: expect 1743+N passed / 23 failed / 1 deselected (failure set
   byte-identical; N14 caveat).
3. Archgraph: zero delta (the pending node stays held per card 3) — state it.
4. Tracker row → `IMPLEMENTED` (fix r3); Review log entry per P-L.
5. Checkpoint: `CHECKPOINT (not approved): item-cost phase 3 fix r3 — <summary>`.
   Cite the FINAL hash.
6. Deposit the handoff at
   `docs/architecture/under_construction/implementation/item_cost_calculation/handoffs/implementer/2026-08-12_phase3_fix_r3_handoff.md`
   (full path). **Deposit before ending the session.**
