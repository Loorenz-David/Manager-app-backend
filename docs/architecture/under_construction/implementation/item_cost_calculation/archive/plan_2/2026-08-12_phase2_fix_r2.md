---
plan: phase 2 (schema, models & migration)
role: implementer
round: 2 (fix cycle)
date: 2026-08-12
---

# Session prompt — phase 2 fix cycle (resolve review r1 findings B1–B4, S1–S3)

You are the **fixing agent** for phase 2. **Resolve, don't relitigate; add nothing
beyond the findings.** The schema itself was verified correct — this is a
test-coverage cycle plus one README/registry correction; with one narrow exception
below, **no production code changes**.
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(branch `fix/idempotent-completion-analytics`). Project folder:
`docs/architecture/under_construction/implementation/item_cost_calculation/`.

## Doctrine (read first, by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md` — fix-cycle variant.

## Gate check

- Tracker shows phase 2 `CHANGES_REQUESTED` (reviewer r1).
- Read the FULL **reviewer r1** entry in `plans/phase_2_schema_models.md` Review log —
  it is the authoritative findings source, including the "Verified correct" settled
  ground and every correction clause verbatim.

## The one rule that shapes every mutation in this cycle (P-G(a) extension)

The tests run against the **migrated** database. A DDL mutation (index predicate,
CHECK drop) applied to an ORM model is **inert** — a false green. Every schema-object
mutation you run is applied **in the migration or by direct DDL on a disposable
database** re-created from the dev schema (`pg_dump --schema-only` clone per master
plan §10; the from-scratch recipe stalls — known, owned elsewhere). The configured
dev DB is never mutated and never downgraded.

## Findings to resolve (correction clauses quoted verbatim from the Review log)

### B1 — C2 entirely unimplemented (0 of 22 rows)

> implement all 22 rows exactly as the C2 table enumerates — one (a) conflict row per
> index asserting `IntegrityError`, and one (b) accepted row per predicate clause
> whose fixture differs from (a) in exactly that clause — then run the P-G(a) named
> mutation per clause and declare each result.

### B2 — C1(b)'s downgrade static proxy is decoration

> the proxy must inspect the `downgrade` function body itself (e.g.
> `inspect.getsource(migration.downgrade)`) and assert (i) the set of enum type names
> dropped equals exactly the five new types, (ii) none of `business_task_type_enum`,
> `task_return_source_enum`, `task_state_enum` appears in any drop in `downgrade`,
> and (iii) the set of table names passed to `op.drop_table` in `downgrade` equals
> the set passed to `op.create_table` in `upgrade`. Re-run mutations (i)–(iii) above
> and declare that each turns it red.

### B3 — the five basis-CHECK rows pass on a second sufficient cause

> give each row a group with no open basis version (or set `effective_to` on the
> fixture's version) so the CHECK is the sole cause, and assert the violated
> constraint **name** appears in the raised error. Re-run the five deletions and
> declare that each row reddens on its own CHECK alone.

Audit `_foundation()` (and any other shared helper you touch) for constraints it
pre-satisfies or pre-violates — P-K applies to every row in this cycle.

### B4 — 9 of 16 registered CHECKs have no behavioral test

> add every row listed above, one assertion per row with its exact expected outcome
> and exception class, each fixture built so its own predicate is the only reason the
> outcome holds (B3's rule applies to all of them). Per P-I, the fix cycle
> mutation-tests its own new rows and declares the results.

The "rows listed above" are enumerated in the Review log's B4 entry (boundary rows
for five columns, the eight D10 money-CHECK rows, the valuation rows incl. cost-only
accept, and all eight window-CHECK rows across both chains). This also closes the
structural gap: `ProductionCostGroupSection`, `ItemCostEvaluationTerm` and
`ItemCostResult` gain their first row-level coverage via these fixtures.

### S1 — the fourth currency column (registry decision made: option (a), reuse ratified)

The coordinator amended master plan §6.3: `item_cost_evaluations.currency` reusing
`item_valuation_currency_enum` (`create_type=False`, ownership on `item_valuations`)
is now the registered form — **no schema change**. Your task is the second half of
the clause:

> correct the README sentence to say four columns / three types

(`models/tables/item_economics/README.md` — state the reuse and its ownership
explicitly, matching §6.3.)

### S2 — the five new PG enum types asserted nowhere

> add to the inventory test a `pg_type` query asserting the five new type names
> exist, and — cheap and directly protective of C5 — that `business_task_type_enum` /
> `task_return_source_enum` / `task_state_enum` exist and are still the declared
> types of `tasks.task_type` / `tasks.return_source` / `tasks.state`.

### S3 — test name overclaims (P-G(b))

> add the cost-only accept row (and the NULL-currency reject row, per B4), or rename.
> Per P-G(b), the name must describe what is actually covered.

### Optional (fixer's judgment, documented if taken)

- **N2:** pin the `percent_value` column precision (assert the DB column's
  `numeric(6,3)` type in the inventory test) so a wrong-scale migration cannot pass.

## Allowed file perimeter (the re-review verifies this exactly)

- `app/tests/integration/models/item_economics/test_item_economics_schema.py`
  (and, if you split files, new test modules under the same directory)
- `app/beyo_manager/models/tables/item_economics/README.md` (S1 sentence only)
- `docs/architecture/under_construction/implementation/item_cost_calculation/plans/phase_2_schema_models.md` (Review log: your fix entry)
- `docs/architecture/under_construction/implementation/item_cost_calculation/master_plan.md` (tracker row Note append only; keep all actor stamps)
- your handoff file

**No model, migration, enum, or registration changes** — the schema is settled
ground. Any file outside this list is an automatic re-review finding.

## Closing protocol

1. All criteria green; every mutation named in B1–B4 run at the correct site
   (disposable DB / migration source), reverted, sha256-verified, and **declared
   per-mutation with its reddened row ids** (P-I; and per P-L, state what was built
   per criterion, not only what remains).
2. Full suite green against the §10 baseline (23 known failures, none new); dev DB
   at head, untouched.
3. Archgraph: zero delta expected (tests + README only) — state it explicitly.
4. Tracker row → `IMPLEMENTED` (fix r2, Note appended); Review log fix entry.
5. Checkpoint commit: `CHECKPOINT (not approved): item-cost phase 2 fix r2 — <summary>`.
6. Deposit the handoff at
   `handoffs/implementer/2026-08-12_phase2_fix_r2_handoff.md` (frontmatter complete):
   summary; `⚠ OWNER DECISIONS REQUIRED (n)`; finding-by-finding resolution with
   per-mutation declarations; test counts before/after; full write perimeter incl.
   the checkpoint hash. **Deposit the handoff before ending the session.**
