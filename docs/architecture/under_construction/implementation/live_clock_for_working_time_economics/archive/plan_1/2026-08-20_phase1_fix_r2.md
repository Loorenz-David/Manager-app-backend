---
plan: 1
role: implementer
round: 2 (fix cycle)
date: 2026-08-20
project: live_clock_for_working_time_economics
---

# Session prompt — plan 1 fix (round 2), `live_clock_for_working_time_economics`

## 1. Role, workspace, doctrine

You are the **implementing agent** for phase 1's fix cycle. Resolve the review's
B/S findings and the three notes marked below — **resolve, don't relitigate; add
nothing beyond the findings.** The shipped production code was found correct; every
correction in this cycle lands in the **test file** (and its docstrings/comments).

Workspace root: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
Application root: `backend/app/` (tests: `PYTHONPATH=. pytest -m 'not e2e'`)
Project folder:
`backend/docs/architecture/under_construction/implementation/live_clock_for_working_time_economics/`

Doctrine, read first by absolute path:
1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md`

Read then: `plans/plan_1.md` (its §5 now carries the amended C9 and the new
**C11/C12** — the plan file is your task list; where this prompt differs, the plan
file wins); the review at
`handoffs/reviewer/2026-08-20_phase1_review_r1_handoff.md`; intention §1A HC-3A
(as amended round 4c) and §3.1A A–B; master plan §5 (the identity-element and
isinstance rules earned by this review) and §6 (baseline **26 / 2454 / 1**, ID set
enumerated).

## 2. Allowed file perimeter — the re-review will verify this against git

- `app/tests/integration/services/queries/item_economics/test_live_worked_seconds.py`
- `docs/…/live_clock_for_working_time_economics/plans/plan_1.md` — **§7 Review log
  entry only**
- `docs/…/live_clock_for_working_time_economics/handoffs/implementer/2026-08-20_phase1_fix_r2_handoff.md` — your handoff

**No production file changes.** Mutation-ledger probes touch
`live_worked_seconds.py` temporarily and are reverted byte-identical
(hash `6d11b922fbec3031be49adf1313b6d1685bef95659caf81f2b6cb7e918fa82ca`),
declared per probe in your handoff.

## 3. The findings, with their correction clauses verbatim

**B1 — the settled term has no test that can fail.** Correction, verbatim from the
review: *"Two rows with `settled != 0`: (a) a step with a non-zero settled column
**and** an open working record, asserting `settled + share`; (b) a step with a
non-zero settled column and **no** open record, asserting exactly the settled
value. Register the named mutation 'drop the settled term ⇒ both rows redden',
both sides computed, site named as definition-site."* This is now criterion
**C11** in the plan.

**B2 — the `int` output and the rounding locus have no test that can fail.**
Correction, verbatim: *"(a) An explicit type row —
`assert all(isinstance(v, int) for v in result.values())` — which `==` cannot
express. (b) One row whose exact share lands on a half-second (a two-way batch
split of an odd second count, per §3.1A A / §4.1A), asserting the half-even
result, so the locus is discriminated. Register the mutation 'return the raw
float ⇒ the type row reddens' and '`math.floor(x + 0.5)` ⇒ the half-second row
reddens', both sides computed."* This is now criterion **C12**.

**S1 — the naive-`now` row asserts a mechanism that cannot fire.** This-phase
correction, verbatim: *"Rename to
`test_c9_naive_now_fails_closed_at_the_loader_boundary` and add a docstring
recording that the sweep cannot raise because the driver normalizes the naive
bind first (cite the 0-rows observation)."* The upstream contract is already
amended (HC-3A round 4c; plan C9) — cite them in the docstring, do not restate
them. Then **re-measure the delete-the-guard mutation whole-suite** so the ledger
carries the red set under the test's **new** name.

**S2 — the deleted-record row is missing its required docstring.** Correction,
verbatim: *"Add the docstring, naming
`reset/phases/delete_step_state_records.py` (a hard workspace-wide `DELETE`) as
the only writer, per §3.1A D."*

**Notes resolved in this cycle (at judgment, documented — these three are cheap
and land in the same file):**
- **N1**: docstring on the zero-cases row recording which mutation answers which
  half (missing-attribution ⇒ the `if user_id is not None` deletion;
  future-entry ⇒ the both-args clock-read shape — cite the Review log's
  consumption entry).
- **N3**: add the free assertion `result[second.client_id] == 1800` to the C7 row,
  pinning the second open record.
- **N7**: one comment on the C3 row-3 test recording D1's granted choice (two
  overlapping 30-minute cross-task records ⇒ 900), per the named-medium rule.

N2 and N6 are **not yours**: N2 is folded upstream already; N6 (the graph summary
count) is queued for owner adjudication. Do not touch `.archgraph/`.

## 4. Ledger obligations

For C11's mutation, C12's two mutations, and S1's re-measured guard deletion:
apply at the named site, run the **whole** non-e2e suite, record the complete
added-ID set diffed against master plan §6's enumerated baseline, revert, verify
the revert hash. A count disagreeing with baseline is repeated and ID-diffed (two
named flaky tests). Compute both sides of every mutation **for the specific
fixture** before trusting the run.

## 5. Closing protocol

1. Full suite; expected shape: 26 failed / 2454 + (new rows) passed / 1
   deselected, failure IDs byte-identical to §6's set.
2. Append your fix-cycle entry to `plans/plan_1.md` §7: what changed per finding,
   the four-row mutation ledger, revert hashes.
3. **Checkpoint commit** prefixed `CHECKPOINT (not approved):` under the standing
   authorization.
4. Handoff at
   `handoffs/implementer/2026-08-20_phase1_fix_r2_handoff.md` — charter
   frontmatter, findings-addressed table (B1, B2, S1, S2, N1, N3, N7 — one row
   each, what was done, where), the ledger, probe declarations with reverts, and
   your full write perimeter from `git status` / `git diff --name-only`.
5. Do not update the master plan tracker.
