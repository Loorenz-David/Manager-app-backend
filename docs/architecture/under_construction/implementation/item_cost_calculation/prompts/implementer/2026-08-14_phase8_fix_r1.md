---
plan: phase 8 (status & results)
role: implementer (fix)
round: 1 (fix cycle after review r1)
date: 2026-08-14
---

# Session prompt — phase 8 fix r1

You are the **fixing agent** for phase 8. Review r1: CHANGES_REQUESTED —
7 blocking / 6 should-fix / 6 notes, 0 owner cards. The verdict in one
line: **the production surface is correct — 16 of 18 mutations bite the
reviewer's probes, all green on the shipped tree — but only 2 of 18 bite
the shipped suite.** The work of this cycle: adopt the reviewer's 19 probe
rows verbatim, build the handful of rows nobody has built, make five small
production corrections, and run the ENTIRE mutation ledger with zero
deferrals.
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(commands from `backend/app/`, per master plan §10).

## Doctrine (read by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md`

## Ground

- Review handoff (read in full — its mutation ledger is your work list):
  `handoffs/reviewer/2026-08-14_phase8_review_r1_handoff.md`.
- Plan: `plans/phase_8_status_results.md` — A1–A18 + the **fix block
  G1–G10, GOVERNING**.
- §9 as of today — note the six NEW rules this review earned: the
  **deferral cap** (zero deferrals this cycle — every row has an in-tree
  arbiter once you adopt the probes), the **expected-red rule** (your
  ledger states site → expected red node id, per row), the call-graph /
  endpoint-boundary / hand-written-literal / red-coverage rules.
- **The probe file — ADOPT VERBATIM (G1):**
  `docs/architecture/under_construction/implementation/item_cost_calculation/probes/reviewer_r1_phase8/test_reviewer_r1_phase8_probe.py`
  sha256 `b5ac470c704e5f62be3d8752d7eb2b6f4e908469c5e944f764ee1a9d454abe3c`
  (891 lines, 19 rows, all green on shipped tree). Verify the hash, copy to
  `app/tests/integration/services/commands/item_economics/` under a phase-8
  name, align parametrize ids (P-V), keep `_cleanup` whole. Do NOT weaken
  any assertion — the re-review diffs your adoption against this source.

## Baseline (reviewer-verified, foreground ×2)

Head `c1d2e3f4a5b6`; suite **2111 / 23 / 1** with the failure set
byte-identical to the phase-1 list; all 19 production hashes verified at
entry and exit of the review; graph 172/254, rev `c74eb913…`, 21 pending
held (READ-ONLY for you, zero delta).

## The work (ordered; G-numbers from the plan block)

1. **G1** — adopt the probe file. 2. **G2** — rebuild C7: each of the
   twelve rows drives its REAL producer (ten via
   `resolve_item_economics_status`, `ok`/`infeasible` via the
   committed-evaluation branch); no two rows share a call graph.
3. **G3** — build from scratch: C2 four-bucket policy (ended-shift =
   `PAUSED` + `transition_reason == SHIFT_ENDED`; marked-wrong lands in
   `inaccurate_working_seconds` only), C3 batch dilution (Σ = wall clock),
   C6b re-entry convergence, C5 config-supersession-after-close →
   byte-identical recompute, C4's no-steps COALESCE row.
4. **Production corrections (exactly five):**
   - **G4 (S1):** replace the route's inline `{WORKER, SELLER}` deny-list
     with `include_monetary_step_fields(role_name)` — fail-closed.
   - **G5 (S2):** delete the dead `_MANAGER_ONLY_ROUTES` /
     `_ALL_ROLE_ROUTES` / `_ROUTES` block in `item_economics.py:405-409`;
     re-target M16 at the TEST module's hand-written table.
   - **G6 (S3/S4):** `get_item_lifetime_economics` calls
     `serialize_item_lifetime_economics` (delete the inline duplicate);
     collapse `get_task_budget_status.py`'s verbatim copy onto ONE
     `_build_evaluated_status` both services call — the per-service
     FILTERS stay separate literals (A7's mutation sites).
   - **G8 (S6):** the DELETE-valuation re-resolution must not refuse on a
     soft-deleted item — resolve to `ITEM_UNVALUED` instead (restores
     pre-phase behaviour); new row + mutation.
   - **G7 (S5):** add the `route.response_model is None` structural row
     (test-side).
   Nothing else in production — an unplanned edit is stop-and-report.
5. **G9 — the FULL mutation pass, zero deferrals:** all 18 review-ledger
   rows + G8's, per row: site → EXPECTED RED NODE ID (state it before you
   run), sha256 pair copy-pasted, observed red set, reversion proven
   (tree == checkpoint blobs). The reviewer's mutant hashes are in the
   handoff — where your mutant text matches, say "reproduces"; where it
   differs, flag it (P-I 7th/9th ext). M15/M17 are regression re-runs.
6. R2-N2 and the phase-7 files: untouched this cycle.

## Scope fence

Production: `routers/api_v1/item_economics.py` (G4 + G5),
`services/queries/item_economics/get_item_lifetime_economics.py` (G6),
`get_task_budget_status.py` + `get_task_budget_status_worker.py` (G6
dedupe), `domain/item_economics/serializers.py` (only if G6 requires the
serializer's signature to fit), `services/commands/item_economics/delete_item_valuation.py`
(G8). Tests: the adopted probe file + the G3 builds + the router test
(G5/G7) + C7's rebuild. Docs: master plan tracker + plan Review log + your
handoff. **No migration; no changes to the emission/handler/transition
files** (their correctness is proven — only their tests change). If
anything else seems needed, stop and report.

## Closing protocol

1. All criterion rows green (C1–C11 per A+G blocks); the FULL per-row
   mutation ledger with expected-vs-observed red ids; zero deferrals.
2. Suite: expect 2111+N / 23 byte-identical (sorted diff, say so) / 1
   deselected — numbers READ off YOUR foreground run; committing subsets
   twice with residue scope named (the probe file's `_cleanup` is
   load-bearing — rule 11½); ruff; DB at head `c1d2e3f4a5b6`; disposables
   dropped and listed.
3. Tracker → `IMPLEMENTED` (fix r1); Review log per P-L; FINAL sha256 for
   every production file touched.
4. Checkpoint `CHECKPOINT (not approved): item-cost phase 8 fix r1 —
   <summary>`; handoff AFTER (not inside the checkpoint — the r1b slip),
   citing final hashes, at
   `docs/architecture/under_construction/implementation/item_cost_calculation/handoffs/implementer/2026-08-14_phase8_fix_r1_handoff.md`
   (full path). `⚠ OWNER DECISIONS REQUIRED (n)` if any arise; full write
   perimeter + probe declaration; every delegation stated.
