---
plan: 4
role: implementer
round: 2
date: 2026-08-22
project: live_clock_for_working_time_economics
---

# Phase 4 — fix r2: one blocking finding, two should-fix

You are the **implementing agent** for the fix cycle of phase 4 of
`live_clock_for_working_time_economics`. Implement r1 was consumed by the coordinator and
verified against the tree; this round is **finding-scoped**.

Workspace root: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`

Doctrine, by absolute path, as in r1:
`/Users/davidloorenz/agent-skills/pipeline-charter.md` and
`/Users/davidloorenz/agent-skills/implementation-executor.md`.

**Resolve, don't relitigate; add nothing beyond these findings.** The plan file is still
your task list, and where this prompt differs from it, the plan file wins.

## What r1 got right — settled, do not re-spend effort here

Verified by the coordinator against the tree, not read from your ledger: the write
perimeter is exactly the four declared files plus the handoff; nothing under `app/`
changed; the only file touched under `docs/handoff/` is the new one, so no published
handoff was edited. **The 21 published failing IDs `comm`-diff empty in both directions**
against the authoritative enumeration, and the five removed IDs match master §6 exactly.
The L4 budget was honoured — zero L4 runs, two L1 docs-guard runs at 59 passed. Evidence
summaries carry no counts; every anchor is symbol-without-span. No review item was
promoted, rejected or edited, and the 5 pending relationships are the **correct** outcome
of adding `ai_inferred` relationships, not a defect to clear. C1, C2, C3, C4 (both
citation halves), C8 and the eight-row consumer answer are present and correct at source.

The frontend document is good. Three things need repair, and one of them is structural.

## B1 (blocking) — the graph delta added edges and updated no node

**Finding.** The batch moved the graph 194 → 194 nodes and 291 → 296 edges, with **zero
deletions in `architecture.yml`**. No node description changed. The four present-tense
projection nodes therefore still describe the settled-only basis —
`projection-item-economics-task-budget-status` still reads *"resolves the current
committed evaluation, **live non-deleted task-step seconds**, item binding/status, and the
durable result boundary"* — which is the precise text intention §8 identifies as the drift
this closeout exists to correct. §8 names the description update **first** and the edges
second; `plans/plan_4.md` §3 and C6 both say "five nodes updated, edges recorded". The
edges landed; the nodes did not.

**Your divergence was declared, and that part was correct** (charter rule 14): your Review
log entry says *"existing settled graph descriptions were preserved rather than rewritten
through maintenance."* The declaration is why this is a fix and not a finding about
process. But the **reason does not hold**, and this was measured rather than argued:

> A dry-run `archgraph_preview_maintenance_changes` with
> `{kind: "edit", itemId: "node:projection-item-economics-task-budget-status",
> fields: {description: …}}` **is accepted** on that `human_confirmed`, non-pending node.
> It returns a clean before/after diff on the description field alone, with no cascades
> and no review adjudication. The preview writes no mutation and no change record; the
> coordinator did not apply it.

The r1 prompt's prohibition was that **you never promote, reject or edit a *review item*** —
that governs pending `ai_inferred` items awaiting the owner. It was never a prohibition on
maintenance, which is the tool's designated path for exactly this, and which the phase
already anticipated by making you read the `repair_anchors` tooling findings first.

**Correction.** Update the descriptions of the four present-tense projection nodes
(`…-task-budget-status`, `…-task-budget-status-worker`, `…-task-budget-allocations`,
`…-task-production-time`) so each states the live basis it now actually has: settled step
time **plus** the concurrency-averaged share of any open working interval, computed once
per request from the shared loader, persisted nowhere. Then handle the fifth node,
`projection-item-economics-task-price-scenario`, on its own terms — its dependency is
**transitive and it publishes no live worked-time field**, so its description must not
acquire language implying it reads live seconds directly. Your r1 edge description already
draws that line correctly; keep it.

Binding constraints on this repair:

- **Do not touch any evidence `summary` or `inferenceReason`.** Those are immutable
  through both review and maintenance; "correcting" one means reject-and-re-record, which
  is the owner's adjudication and is explicitly out of scope. Note that
  `…-task-budget-status`'s evidence summary says "aggregates non-deleted task-step
  seconds" — **leave it**. Descriptions are the target; summaries are not.
- **Keep, never restate,** the budget-allocations node's HC-5 invariant. You kept it in
  r1; keep it byte-identical now.
- `archgraph_repair_anchors` remains **one operation per call**. If you need it at all,
  read the two open tooling findings in
  `implementation/archGraph_mapping_mantainance/open/…` first — one file, two findings.
- Re-measure `archgraph_status` after and report before/after (revision, nodes, edges,
  pending, stale, diagnostics). **Stale and diagnostics must be 0.** Report whatever the
  pending count is; if maintenance edits change it, say so plainly rather than steering
  toward a number.

## S1 — a per-event client rule drifted in substance

**Finding.** Intention §6A C's rule for a drop larger than one second reads, verbatim:

> **On a drop larger than 1 s:** treat the served value as authoritative immediately —
> reset the smoothing baseline to it and continue accruing from time-of-receipt. **Do not
> animate the descent over time; the time is gone at once, not gradually.**

The document ships: *"Never clamp to the previous maximum and never animate time that the
workspace has disowned."* Those are **two different instructions**. The authority forbids
animating **the descent** — the visual slide from the old value down to the new one. The
shipped sentence forbids animating **disowned time**, which a developer satisfies by
easing the value down over 400 ms and continuing to think they are compliant. The
operative instruction is lost while the sentence still sounds right.

**Correction.** Carry §6A C's rule in substance: the drop is rendered at once, not as a
gradual descent. Keep the no-clamp rule, which is separately correct and comes from the
same section's "any decrease" row.

## S2 — the published tree identity cannot be resolved by the pipeline that consumes it

**Finding.** §7 publishes *"Tree identity: `996a77a` plus the coordinator's two-test
deletion, with `git status --porcelain` empty at measurement time."* That phrasing is
copied from the isolation project's internal tracker row, and to a reader outside that
project it describes a **dirty tree they cannot reconstruct** — the one thing a published
baseline exists to prevent. The source row's own wording is *"`996a77a` + the
coordinator's two-test deletion, **gate-committed**"*, and the dropped word is the whole
difference.

Measured by the coordinator: that state is committed as **`dc76db8`** (subject
`CHECKPOINT (not approved): gate stamp + two rows that cannot fail, deleted`), and
**`git diff dc76db8 HEAD -- app/` is empty** — so the citation is sound and only its
identity is unusable.

**Correction.** Publish a resolvable commit SHA as the tree identity. `narrow_typical_work_times`
D23 needs a tree it can check out; state the one the measurement was taken on, and you may
note that the current `app/` tree is identical to it.

## What is NOT in scope

- **N1 is not yours to fix.** The document names record deletion to the client
  ("not a shipped client event and is not a cause to handle"). Intention §6A A says naming
  E5 *as a decrease cause* would tell another codebase to handle an event our API cannot
  emit — and you named it as a **non**-cause, so C5's literal is met. This is a judgment
  call routed to the independent reviewer as a probe. **Leave the sentence as it is**; do
  not pre-empt the reviewer, and do not re-argue it.
- The absence of §6A B's "one disowning action produces two drops at different times" from
  the document was examined and is **correct** — §6A C opens with "the closeout handoff
  tells the frontend what to do, not what we believe", and C's "any decrease → render the
  served value" already covers the later drop. No change.
- Everything in the "settled" list above.

## Allowed file perimeter — the re-review will verify this

- `docs/handoff/to_frontend/HANDOFF_TO_FRONTEND_live_working_time_clock_20260822.md` (S1, S2)
- `.archgraph/` — node descriptions via maintenance (B1)
- `docs/…/live_clock_for_working_time_economics/plans/plan_4.md` — frontmatter + Review log
- `docs/…/live_clock_for_working_time_economics/master_plan.md` — **your own tracker row
  only, added as a new row**; the convention here is that superseded rows stay as
  provenance and are never overwritten (r1 replaced the coordinator's row, which had to be
  restored).
- `handoffs/implementer/2026-08-22_phase4_fix_r2_handoff.md`

Nothing under `app/`. No edit to any published handoff — the 2026-08-22 document is
**ours and unpublished-to-them until this phase approves**, so editing it in this cycle is
correct and is not the scarred case.

## Evidence budget

**This session's L4 budget is exactly 0 runs**, for the same derivation as r1, which still
holds: `git diff 0aae85e HEAD -- app/` is empty and your perimeter is docs plus graph
state. Run `PYTHONPATH=. pytest tests/unit/docs/` from `app/` after your edits (L1, and
the new document is inside that guard's sweep). The r1 pre-write run does not need
repeating — its tree identity is recorded and nothing about it has changed.

The authoritative baseline is cited, never re-measured: master §6's gate block.

## Closing protocol

1. Docs guard green; `git diff --name-only` shows nothing under `app/`.
2. Plan frontmatter + append your round to `plans/plan_4.md` §7; add your own master
   tracker row.
3. Checkpoint commit, prefixed `CHECKPOINT (not approved):`, **explicit paths, never
   `git add -A`**.
4. Handoff at `handoffs/implementer/2026-08-22_phase4_fix_r2_handoff.md` declaring the
   full write perimeter **including tool-recorded state**, one row per finding (B1, S1,
   S2) saying what changed and where, the graph before/after status, your L1 evidence row,
   and an `⚠ OWNER DECISIONS REQUIRED (n)` section — or one line saying zero.

**Rule 14 still binds:** if you do not implement one of the corrections quoted above,
say which and why, in its own section. A quoted correction can be unimplementable for a
reason only you discover, and that reason is worth more to the record than silent
compliance would be.
