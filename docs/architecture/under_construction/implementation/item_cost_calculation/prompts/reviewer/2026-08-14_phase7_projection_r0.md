---
plan: phase 7 (evaluations — commit/supersede, projections, auto-commit)
role: reviewer (projection)
round: 0 (pre-implementation projection gate)
date: 2026-08-14
---

# Session prompt — phase 7 projection, round 0

You are the **projectionist** for phase 7 — the heart of the domain: committed
evaluations, the mirror rule, projections/promotion, and the task-creation
auto-commit path. The plan was written 2026-08-11, BEFORE rounds 11–15 and
phases 4/4B/5/6 shipped. Find where it no longer survives contact; deposit an
amendment ledger.
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(commands from `backend/app/`, per master plan §10).

## Doctrine (read by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/plan-projection.md`

## Ground

- Plan under projection: `plans/phase_7_evaluations.md` (incl. its forward
  notes — esp. phase-3 N15: the rederive marker is an integrity signal, NOT
  proof of corruption; and the phase-4 forward: §7.5's residual hazard
  verified LIVE → this phase's **FOR SHARE counterparty** is load-bearing).
- Semantic authority: intention §7 ENTIRE (§7.2 commit+mirror in one
  transaction; §7.3 projections/promotion; §7A.6 guard race), §8B (re-emit at
  every episode boundary), §4A (state snapshot + nullable closed_at; refresh
  at reopen), §4.1 (auto-commit at task creation NEVER fails task creation —
  R1-4), §7C (category selection — the commit path's group resolution),
  §11A.4 as amended (rounds 13–15), §6A (money math, Q1–Q5, snapshot
  closed-set), R13-1 (preview vs committed figures NEVER merge).
- Registry: master plan §6 (identities incl.
  `ITEM_COST_ITEM_MISSING_MAJOR_CATEGORY` forwarded from 4B; the §6.5
  files/routes for this phase; `resolve_economics_selection` /
  `resolve_item_economics_status` — phase 5's resolvers are this phase's
  entry points); §9 P-A…P-AA + ALL extensions bind.
- Shipped reality: the phase-3 calculator (19-name `__all__`, rederive
  totality); phase-5's valuation surface + resolvers; phase-6's world
  (items carry NO money; head `be9dfe42a035`).

## Environment facts (verified at closeout, 2026-08-14)

- Head `be9dfe42a035`; suite baseline **2012/23/1** (= 2035 selected);
  dev DB at head; economics tables at zero rows except the journal (present,
  0 rows). Graph: 156 nodes / 201 edges, 2 pending (the re-recorded
  table-item pair), revision `53261a23…`.
- Live data: ~483 items (grows with suite runs — the known residue class);
  53+ NULL-category items (re-measure if the plan leans on it).

## Projection axes (minimum — the ledger is yours)

1. **Rounds 11–15 drift:** the plan predates the category contract (§7C),
   the R13-1 preview key, R14/R15 — every claim about group resolution,
   status vocabulary, valuation reads, or item money is suspect. The commit
   path consumes `resolve_economics_selection` + `resolve_item_economics_status`
   (registered phase-5 additions) — never re-derives selection.
2. **The §7.2 transaction:** commit + supersede + mirror rule + audit in ONE
   transaction; §8B re-emit; the §4A snapshot/reopen semantics; what the
   auto-commit path does on every §11A.4 failure (R1-4: NEVER fails task
   creation — enumerate the failure log line's shape).
3. **The §7A.6 guard race and the FOR SHARE counterparty:** phase-4's review
   proved the residual hazard LIVE; the harness precedent is phase-4's C3/C6
   gate (bounded waits, P-T). Name observables, counterparties, both race
   paths where applicable.
4. **Dependencies greps re-run** (N-f): the plan's file list, "shipped tests
   that change", `ItemCostEvaluation(`, `item_cost_evaluations`,
   `rederive`, `REDERIVE_MISMATCH`, `promote`, task-creation call sites —
   against the CURRENT tree. Payload-key greps too (Projection practice).
5. **Criteria quality under §9 as it stands** — the full rulebook now
   includes P-V (ids name CURRENT authority numbering; expressions differ;
   params consumed), P-I (N=N rows, full red sets, final hashes, per-label
   hashes, records-are-evidence), P-J (quantified sets + non-vacuity +
   production guards owe abort mutations), P-Q (mutations checked against
   the implementation they meet), P-T/P-R/P-S/P-W/P-AA, the row-report and
   precedence-arbitration and surviving-conjunct rules.
6. **N15 discipline:** phase 7 must not read the rederive marker as proof of
   corruption (phase-3's catch-all converts programmer errors into integrity
   markers) — check the plan's wording wherever it consumes rederive.
7. **Decidability:** reject any clause whose prose and verbatim predicate
   disagree (the phase-6 lesson, now projection practice).

## Closing protocol

1. Deposit an amendment LEDGER (numbered rows, severity, section amended,
   verified corrections — executed where cheap). `⚠ OWNER DECISIONS REQUIRED
   (n)` for anything semantic (story-shaped cards, branches + recommendation
   + on-silence).
2. No code, no plan edits, archgraph READ-ONLY (state revision `53261a23…`,
   2 pending — zero delta).
3. Deposit at
   `docs/architecture/under_construction/implementation/item_cost_calculation/handoffs/reviewer/2026-08-14_phase7_projection_r0_handoff.md`
   (full path, AFTER your writes): ledger; any live measurements; full write
   perimeter + probe declaration.
