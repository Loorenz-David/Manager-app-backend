changeId: 2026-08-29T09-13-20-013Z--00abe2
operations:
  - itemId: node:projection-item-economics-task-price-scenario
    kind: edit
    before:
      description: "Manager-only, task-scoped read model that composes
        workspace-scoped task/item binding, the saved valuation and byline, live
        item-economics configuration, an item-aware task typical time - drawn
        from the same-category slice of each participating section's history
        through the shared typical-times engine, resolved by the shared
        reconciliation with a zero-duration price terminal, and windowed by the
        injected request clock so two reads of one task agree - and the pure
        price-scenario model into break-even anchors and a slider domain. Its
        published typical total is quantity-projected: the business fallback
        runs in per-unit space and each participating section's per-unit typical
        is scaled by the current item's quantity (clamped to at least one)
        before the shared per-section half-even rounding, with
        total_unit_seconds and quantity_applied disclosed beside total_seconds,
        so break-even and the slider domain price the whole order. Non-bound
        bindings govern the payload by nulling valuation/model/anchor/domain
        structure while keeping the step-derived typical; the projection
        performs no writes. It composes task budget status and so inherits that
        read's worked-time dependency transitively; it publishes no live
        worked-time field of its own and reads no open interval record
        directly."
    after:
      description: "Manager-only, task-scoped read model that composes
        workspace-scoped task/item binding, the saved valuation and byline, live
        item-economics configuration, an item-aware task typical time - drawn
        from each participating section's comparable history through the shared
        typical-times engine, resolved by the shared reconciliation with a
        zero-duration price terminal, and windowed by the injected request clock
        so two reads of one task agree - and the pure price-scenario model into
        break-even anchors and a slider domain. The derived spec carries the
        PRIMARY item's properties_signature when present, so the typical can
        resolve on the most specific item_properties_narrowed tier (same
        category AND same signature) before falling back to category-narrowed
        then section-wide under the same shared ladder. Its published typical
        total is quantity-projected: the business fallback runs in per-unit
        space and each participating section's per-unit typical is scaled by the
        current item's quantity (clamped to at least one) before the shared
        per-section half-even rounding, with total_unit_seconds and
        quantity_applied disclosed beside total_seconds, so break-even and the
        slider domain price the whole order. Non-bound bindings govern the
        payload by nulling valuation/model/anchor/domain structure while keeping
        the step-derived typical; the projection performs no writes. It composes
        task budget status and so inherits that read's worked-time dependency
        transitively; it publishes no live worked-time field of its own and
        reads no open interval record directly."
    rationale: "Item-properties complexity tier shipped: price-scenario spec
      inherits properties_signature and resolves on the most specific tier
      through the shared ladder."
  - itemId: node:projection-working-section-typical-times
    kind: edit
    before:
      description: The read model of live working sections and their median completed
        section-total durations over the fixed 90-day window. It groups
        contributing completed steps by task and section, excludes deleted or
        marked-wrong records, and retains sections with insufficient samples as
        explicit null rows. The shared statement additionally computes per-unit
        twin medians - each group total divided by its task's PRIMARY-item
        quantity, clamped to at least one, with missing items counting as one -
        under the same window and sample gates; the twins feed the
        item-economics quantity projections and never alter the raw medians or
        this endpoint's published rows. The grouping and window admission must
        remain aligned so reassignment totals are not reduced to a recent rework
        fragment.
    after:
      description: The read model of live working sections and their median completed
        section-total durations over the fixed 90-day window. It groups
        contributing completed steps by task and section, excludes deleted or
        marked-wrong records, and retains sections with insufficient samples as
        explicit null rows. The shared statement additionally computes per-unit
        twin medians - each group total divided by its task's PRIMARY-item
        quantity, clamped to at least one, with missing items counting as one -
        under the same window and sample gates; the twins feed the
        item-economics quantity projections and never alter the raw medians or
        this endpoint's published rows. For a narrowing spec that carries a
        properties_signature the spec branch also computes a third,
        most-specific column set - properties-narrowed raw and per-unit medians
        with their own sample count, gated at the same minimum, restricted to
        history whose PRIMARY item matches both the spec and the signature;
        signature-less specs read empty properties columns and this endpoint's
        no-spec rows are untouched. The grouping and window admission must
        remain aligned so reassignment totals are not reduced to a recent rework
        fragment.
    rationale: "Item-properties complexity tier shipped: spec branch gains
      properties-narrowed raw/unit medians and count, gated at the same minimum
      sample size; no-spec branch untouched (HC-4 snapshot held)."
  - itemId: node:table-item
    kind: edit
    before:
      description: "The item row: identity, category snapshots
        (item_major_category_snapshot String(64) nullable at :46), position and
        lifecycle fields. Carries NO money columns — phase 6 (migration
        `be9dfe42a035`) dropped the legacy item_value_minor / item_cost_minor /
        item_currency; item money lives exclusively in item_valuations (R1-3,
        §10.2)."
    after:
      description: "The item row: identity, category snapshots
        (item_major_category_snapshot String(64) nullable at :46), position and
        lifecycle fields. Carries NO money columns — phase 6 (migration
        `be9dfe42a035`) dropped the legacy item_value_minor / item_cost_minor /
        item_currency; item money lives exclusively in item_valuations (R1-3,
        §10.2). Also carries the externally-owned properties snapshot:
        properties JSONB (NULL = no snapshot yet, {} = snapshotted propertyless,
        GIN jsonb_path_ops indexed), properties_signature String(64) derived
        from the canonical blob (partial btree with workspace_id), and
        properties_snapshot_at. The signature is the item-complexity narrowing
        key for typical-time comparability; there is deliberately no property
        versioning - a snapshot update corrects the item's innate
        characteristics and regroups its historical samples on the next read."
    rationale: Migration b9d0e1f2a3c4 added the properties snapshot columns and
      indexes; canonical-snapshot (no versioning) semantics are an owner
      decision.
retiredItemIds: []
authorizerSource: client-approval
clientInfo:
  name: claude-code
  version: 2.1.226
timestamp: 2026-08-29T09:13:20.013Z
graphRevisionBefore: ba00617f2ca541851e69245f45a596d3f05540c9cb6f1d6bf95880cf76a7e4ce
graphRevisionAfter: c79e87077c153113ed8f32e2824b68ad3da2f40921a66d77774821d1dd401aaa
humanInstruction: "Owner-ratified item-properties complexity project: update
  price-scenario and typical-times projection descriptions and the items table
  description for the properties snapshot columns and signature tier."
changeTokenCorrelation: cc010673a1ea
