---
plan: 3
role: implement
round: 3 (fix)
date: 2026-08-19
project: simple_valuation_editor
kind: fix — two coordinator findings on the r2 delta
---

# Session prompt — fix r3, phase 3 (`simple_valuation_editor`)

## 1. Why there is a third round

**Fix r2 was applied correctly and completely.** The coordinator verified every criterion
independently rather than reading the ledger:

- The service file's diff is **pure comment** — zero executable lines, exactly as D1 required.
- The test file's complete non-comment diff is three lines: one import, two deletions.
- Both replacement texts landed verbatim, including both coordinator amendments.
- `ruff check` / `ruff format --check` clean; **F401 clean** — the `text` import is gone.
- Focused file 49/49. Whole suite **26 / 2430 / 1**, failure IDs **byte-identical** to the
  pre-fix set.
- **D3 re-measured by the coordinator on the shipped tree**: with both `SET LOCAL` statements
  deleted, dropping `superseded_at.is_(None)` reddens exactly
  `test_phase3_c1_saved_uses_current_valuation_in_a_supersession_chain` across the whole
  suite — one ID added, none removed. **The GUCs were doing nothing.** That is now the fifth
  independent agreement and F-2's STOP condition is formally closed.
- Both files hash to the values the r2 handoff recorded (`07b7842e…`, `5e52ac0f…`).

This round exists because **two claims in the newly-landed comments do not hold**, and one of
them opened a coverage gap that is the exact twin of the gap this whole phase was raised to
close. Both were found by re-reading the landed text as if the coordinator had written it —
which, after two amendments, is partly true. That is the project's recurring lesson arriving
one more time.

## 2. Perimeter — the same two files

| Path | What changes |
|---|---|
| `app/beyo_manager/services/queries/item_economics/get_task_price_scenario.py` | G-1 (one comment clause) |
| `app/tests/integration/services/queries/item_economics/test_price_scenario_query.py` | G-2 (one new test) |

**Nothing else.** Phases 1, 2 and 4 are APPROVED and closed. **A third file is a STOP and a
report.**

**The working tree carries an uncommitted owner change** to `purchase_api.py`, its unit test
and `.archgraph/architecture.yml`, unrelated to this pipeline. **Do not touch, revert, stage,
commit or count it.** It is why the suite reads 2430.

## 3. G-1 — the F-3 comment says `TaskBudgetStatus` carries no objects. It carries one.

The landed comment at the `get_task_budget_status(ctx)` call site reads:

> `# it means returning those objects from get_task_budget_status, whose TaskBudgetStatus`
> `# carries item_id and no objects and is a contract other screens consume.`

`TaskBudgetStatus` (`get_task_budget_status.py:34-48`) carries **`result: ItemCostResult | None`**
— an object. The review's own C6 row stated this correctly (*"carries `item_id: str | None`
and `result: ItemCostResult | None` — no `Task`, `Item`, selection, terms or valuation"*); the
replacement text then lost the qualification.

It matters because the comment's whole job is to stop someone collapsing the duplication, and
the first thing that someone does is open the dataclass. Finding an object there, one line
under "no objects", discredits the paragraph at exactly the moment it needs to be believed.

**Verbatim replacement for that one clause:**

```python
    # it means returning those objects from get_task_budget_status, whose TaskBudgetStatus
    # carries item_id and the evaluation result but none of the objects re-read here — not
    # the Task, the Item, the selection, the terms or the valuation — and is a contract
    # other screens consume. Reusing this
```

Splice it so the surrounding sentences are unchanged. **Comment-only: zero executable lines.**

## 4. G-2 — the comment calls `ItemValuation.is_deleted.is_(False)` load-bearing, and nothing tests it

F-1's replacement ends *"The three predicates below this one are load-bearing."* Two of the
three are asserted. The third is not.

**Measured:** delete `ItemValuation.is_deleted.is_(False)` from `_current_valuation` and the
focused file is **49/49 green**.

**This is F4's exact twin, and the state is user-reachable.**
`delete_item_valuation.py:41` sets `valuation.is_deleted = True`, and it is a live route —
`routers/api_v1/item_economics.py:305`, `route_delete_item_valuation`. So a soft-deleted
valuation is an ordinary live state, and the predicate that hides it from this screen has no
guard. The failure a regression would produce is the same one F4 was raised for: **the price
screen serves a deleted valuation's price and byline, under the wrong person's name, with no
error** — and `can_commit` flips to `true` on a row the user deleted.

The code is correct. What is missing is the evidence, which is the whole subject of this phase.

**Add one row**, modelled on `test_phase3_c1_…`'s construction — it must exercise the real
query, so it needs `db_session` and a committed row, not `_run_scenario`'s monkeypatch.

- **Fixture:** one workspace, user, item, and **one** `ItemValuation` with `is_deleted=True`
  (`superseded_at` left null — the partial unique index only covers live rows, so this is
  legal and is the realistic post-delete state).
- **Assert** the payload behaves as though no valuation exists: `saved.valuation_id` is null,
  `saved.expected_sale_price_minor` is null, `saved.created_by` is null, and **`can_commit` is
  `false`**.
- **Named mutation, definition site** (`_current_valuation`): drop
  `ItemValuation.is_deleted.is_(False)` → this row red. **Compute both sides** — state what
  `saved.valuation_id` and `can_commit` become under the mutant.
- **Rule 11½**: teardown in `try/finally` naming its tables, residue assertions outside it —
  copy C1's shape, which review r1 confirmed correct on both halves.

**If you factor C1's setup into a shared helper, you must re-measure C1's own named mutation**
(drop `superseded_at.is_(None)`, whole suite) and show its observed-red set is still exactly
C1. Sharing a fixture between two rows that each guard a different predicate is the classic
way one of them stops discriminating. **Not sharing is also fine** — say which you did.

## 5. Acceptance criteria

| C | Criterion |
|---|---|
| E1 | G-1's clause replaced. `get_task_price_scenario.py` still has **zero executable-line changes** across r2 **and** r3 combined — verify against `ef55f6d`, not against r2. |
| E2 | G-2's row exists, asserts all four fields, and its teardown follows rule 11½. |
| E3 | **Named mutation**: drop `ItemValuation.is_deleted.is_(False)` → observed-red set measured **across the whole non-e2e suite**, both sides computed, reverted, SHA-256 checked. |
| E4 | **C1's mutation re-measured** if and only if you shared a fixture. State either way. |
| E5 | `ruff check` and `ruff format --check` clean on both files. |
| E6 | Suite **26 / 2431 / 1** — one row added. Failure IDs **diffed, not counted**; master plan §6 binding. |

## 6. What is NOT yours

Unchanged from r2's §5, and still true. In particular **N-2 belongs to plan 5**
(`collapse_terms`'s deleted-term skip, guarded only by phase 3's C2 row) — do **not** reach
into `price_scenario.py` or `test_price_scenario.py`, which are phase 1's and closed. If G-2
tempts you toward them, that is the STOP.

**The drifting test**: eight observations now stand — implementer r1b 27, 27; coordinator 26;
reviewer 26; fix r2 26, 26; coordinator 26, 26. If it appears in your run, report it as a
ninth and move on.

## 7. Closing protocol

Deposit at `handoffs/implementer/2026-08-19_phase3_fix_r3_handoff.md`, charter frontmatter.

Mutation ledger with both sides and the complete suite-wide observed-red set. Full write
perimeter from the closing `git status --porcelain --untracked-files=all`, listing the owner's
three concurrent paths separately as not-yours. Owner cards story-shaped and ≤120 words if any;
none is expected.

Do not update the master plan tracker or plan 3's Review log. Do not commit.
