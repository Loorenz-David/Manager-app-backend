---
plan: 1
role: implementer
round: 5 (fix cycle)
date: 2026-08-20
project: live_clock_for_working_time_economics
---

# Session prompt — plan 1 fix (round 5), `live_clock_for_working_time_economics`

## 1. Role and scope

You are the **implementing agent** for phase 1's final fix cycle: one blocking
finding and two one-line notes. Rounds r2 and r3 were verified correct and
complete; **nothing from them is reopened.** Resolve these findings, add nothing
beyond them.

Workspace root: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
Application root: `backend/app/` (tests: `PYTHONPATH=. pytest -m 'not e2e'`)

Doctrine, read first by absolute path:
1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md`

Then read: `plans/plan_1.md` §5 **C9 as amended round 4d** (the plan file is your
task list; where this prompt differs, the plan file wins), §6's structural-facts
note, and §7's history; the review at
`handoffs/reviewer/2026-08-20_phase1_rereview_r4_handoff.md` (B1-r4, N1-r4,
N4-r4); intention **§1A HC-3A as amended round 4d**; `master_plan.md` §5's four
rules earned this round and §6's two new environment facts.

## 2. Allowed file perimeter — the re-review verifies this against git

- `app/beyo_manager/services/queries/item_economics/live_worked_seconds.py` —
  **the guard's message string only.** No other executable line changes.
- `app/tests/integration/services/queries/item_economics/test_live_worked_seconds.py`
- `docs/…/live_clock_for_working_time_economics/plans/plan_1.md` — **§7 Review log
  entry only**
- `docs/…/live_clock_for_working_time_economics/handoffs/implementer/2026-08-20_phase1_fix_r5_handoff.md`

Note the change from r2/r3: **one production line is in scope this round**, by
coordinator decision (§3). The loader hash will change; record the new one.

## 3. B1-r4 — the fix, and the coordinator's choice between the review's two options

The review offered a test-only fix (assert the traceback origin) and a
one-production-line fix (give the guard a distinctive message), recommending the
former and marking the choice as the coordinator's. **Take the message fix.**
Reason, recorded so it is not relitigated: the guard currently raises CPython's
*exact* message at a lower frame than the failure it pre-empts, so the two are
indistinguishable **by construction**; a traceback assertion works around that
ambiguity, while the message removes it at its source — and it also tells an
operator reading a production log which boundary rejected the value. The loader is
this phase's own new file, so editing it is in-perimeter; "zero production lines"
was an observation about r2/r3, never a goal.

**Do exactly this:**

1. In `live_worked_seconds.py:load_live_worked_seconds`, change the guard's message
   to name its own boundary — the string must contain `load_live_worked_seconds`
   and say an aware UTC `now` is required. Keep `TypeError`; keep the guard's
   position and condition unchanged.
2. In `test_c9_naive_now_fails_closed_at_the_loader_boundary`, assert with
   `pytest.raises(TypeError, match=...)` against that message (a substring
   sufficient to exclude CPython's — `load_live_worked_seconds` is enough).
3. Replace the docstring with what was **measured**, not what was assumed.
   Correction clause, verbatim from the review:

   > Rewrite the docstring to record what was measured — the naive bind is
   > silently **shifted by the client host's local UTC offset** (not
   > "normalized"), so the un-guarded behaviour is *a wrong live term or a
   > `TypeError` from the sweep, depending on the host and the timestamps*;
   > either way the loader must fail closed at its own boundary.

   Cite `§1A HC-3A` (round 4d) rather than restating its derivation.

## 4. N1-r4 and N4-r4 — two lines

**N1-r4**, correction clause verbatim:

> Suggested one-line addition: *"No shipped command sets
> `StepStateRecord.is_deleted = True`; the only hard `DELETE` is
> `reset/phases/delete_step_state_records.py` (whole workspace). This row is
> defense-in-depth (§3.1A D)."*

Replace the existing `test_c4_deleted_record_is_excluded_defensively` docstring
with that text (its current sentence is true but leaves "this hard DELETE"
without an antecedent, and omits the "no shipped writer" half C4 names verbatim).

**N4-r4:** in `test_c12_loader_output_values_are_ints`, change the assertion to
`assert result and all(isinstance(value, int) for value in result.values())` —
the current form is `True` on an empty dict and the loader has an early
`if not settled: return {}` branch.

## 5. Ledger obligation — the host-independence proof is the point

The C9 mutation (delete the guard, definition site) must now redden its row **on
every host**. Prove it, do not assume it:

| Run | Command shape | Expected |
|---|---|---|
| 1 | guard deleted, whole suite, host's own zone | `B ∪ {test_c9_naive_now_fails_closed_at_the_loader_boundary}` |
| 2 | guard deleted, whole suite, **`TZ=UTC`** | the same single added ID |

Both runs whole-suite, ID sets diffed against `master_plan.md` §6's enumerated 26
in **both** directions, then revert and record the restored hash. For reference,
the coordinator measured the *pre-fix* behaviour: at `+02:00` the row failed
("DID NOT RAISE"); under `TZ=UTC` it **passed** — that inversion is exactly what
this fix must eliminate.

Also re-measure, whole-suite, since the production file changes: **M-locus**,
**M-mode**, **M-float** (per `plans/plan_1.md` §5 C12) — one run each, to confirm
the C12 orthogonality survives the message change. Compute both sides of every
mutation for its named fixture before trusting a run.

**A run whose count disagrees with baseline: capture its failing-ID set FIRST,
then repeat** (master plan §6 — a repeat performed against a bare count made this
project's third flaky test permanently unattributable).

## 6. Closing protocol

1. Full suite; expected 26 failed / 2459 passed / 1 deselected, failure IDs
   byte-identical to §6's set. Ruff clean.
2. Append the fix-r5 entry to `plans/plan_1.md` §7: what changed per finding, the
   ledger (including both `TZ` runs), the **new** loader hash.
3. **Checkpoint commit** prefixed `CHECKPOINT (not approved):`.
4. Handoff at
   `handoffs/implementer/2026-08-20_phase1_fix_r5_handoff.md` — charter
   frontmatter, findings-addressed table (B1-r4, N1-r4, N4-r4), the ledger, probe
   declarations with reverts, and your full write perimeter from `git status` /
   `git diff --name-only`.
5. Do not update the master plan tracker.
