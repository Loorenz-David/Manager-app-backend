---
plan: 1
role: implementer
round: 3 (fix cycle)
date: 2026-08-20
project: live_clock_for_working_time_economics
---

# Session prompt — plan 1 fix (round 3), `live_clock_for_working_time_economics`

## 1. Role and scope — one row

You are the **implementing agent** for a single-row fix cycle. Round 2 was
verified correct and complete against every finding it was given; **nothing from
r2 is being reopened**. This round closes one gap the coordinator found at
consumption **in its own criterion** — C12 promised to discriminate the rounding
*locus*, named only *mode* and *type* mutations, and placed its fixture where the
two loci coincide.

Workspace root: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
Application root: `backend/app/` (tests: `PYTHONPATH=. pytest -m 'not e2e'`)

Doctrine, read first by absolute path:
1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md`

Then read: `plans/plan_1.md` §5 **C12 as amended** (three rows, each with its
isolating arithmetic — the plan file is your task list; where this prompt differs,
the plan file wins) and its §7 last entry (the finding, F-C1, with the measurement).
Intention §3.1A A is the authority for the locus.

## 2. Allowed file perimeter — the re-review verifies this against git

- `app/tests/integration/services/queries/item_economics/test_live_worked_seconds.py`
- `docs/…/live_clock_for_working_time_economics/plans/plan_1.md` — **§7 Review log
  entry only**
- `docs/…/live_clock_for_working_time_economics/handoffs/implementer/2026-08-20_phase1_fix_r3_handoff.md`

**No production change.** The loader is already correct; this is a proof gap.
Mutation probes touch `live_worked_seconds.py` temporarily and are reverted
byte-identical to
`6d11b922fbec3031be49adf1313b6d1685bef95659caf81f2b6cb7e918fa82ca`.

## 3. The work

**Add C12 row (c) — the locus row, mode-neutral.** Correction clause, verbatim
from the amended criterion:

> **(c) locus**, mode-neutral. Two batch steps opened together, elapsed **63 s**
> ⇒ each share exactly `31.5`; the measured step carries an **odd** `settled`
> (use `settled = 1`) ⇒ asserted `1 + round(31.5) = 1 + 32 = 33`. The arithmetic
> that makes this row isolate the locus: `round(31.5) = 32` and
> `floor(31.5 + 0.5) = 32` **agree**, so the mode cannot move it; while
> `int(round(1 + 31.5)) = int(round(32.5)) = 32 ≠ 33`, so the sum-locus does.

Keep row (b) exactly as it is — it is the mode's isolating row and its
`settled = 0` is deliberate, not an oversight. Give row (c) a docstring stating
in one line that it isolates the locus because the two rounding modes agree at
`31.5` (a reader who does not know that will "simplify" the fixture back to
`settled = 0`).

## 4. Ledger obligation — one new mutation, plus two confirmations

Apply at the loader's **definition site**, whole suite, revert, hash-verify:

| # | Mutation | Expected |
|---|---|---|
| M-locus | accumulate `+= contribution.seconds` **and** return `int(round(settled_seconds + live_by_step.get(step_id, 0)))` | **(c) reddens; (a) and (b) stay green** |
| M-mode | `int(math.floor(x + 0.5))` for `int(round(x))` | **(b) reddens; (a) and (c) stay green** |
| M-float | `+= contribution.seconds` alone | (a) and (b) redden — re-confirm (c)'s status and record it |

Compute both sides per row before trusting a run. Record the complete added-ID
set for each, diffed against master plan §6's enumerated 26. A count disagreeing
with baseline is repeated and ID-diffed (two named flaky tests, §6).

## 5. Closing protocol

1. Full suite; expected 26 failed / 2459 passed / 1 deselected, failure IDs
   byte-identical to §6's set.
2. Append the fix-r3 entry to `plans/plan_1.md` §7: the row added, the three-row
   ledger, revert hashes.
3. **Checkpoint commit** prefixed `CHECKPOINT (not approved):`.
4. Handoff at
   `handoffs/implementer/2026-08-20_phase1_fix_r3_handoff.md` — charter
   frontmatter, what changed, the ledger, probe declarations with reverts, and
   your full write perimeter from `git status` / `git diff --name-only`.
5. Do not update the master plan tracker.
