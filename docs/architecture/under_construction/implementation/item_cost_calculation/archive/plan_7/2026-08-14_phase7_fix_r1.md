---
plan: phase 7 (evaluations — commit/supersede, projections, auto-commit)
role: implementer (fix)
round: 1 (fix cycle after review r1)
date: 2026-08-14
---

# Session prompt — phase 7 fix r1

You are the **fixing agent** for phase 7. Review r1 verdict:
CHANGES_REQUESTED — 3 blocking, 5 should-fix, 8 notes, 0 owner cards. The
implementation is substantially RIGHT (the reviewer re-derived §7B.1 and ran
~50 hand-built rows against it — all pass except one): the work of this cycle
is **one production `if`, three small deletions, and adopting the reviewer's
probe suite as the phase's real proof**. Do not redesign anything.
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(commands from `backend/app/`, per master plan §10).

## Doctrine (read by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md`

## Ground

- Review handoff (read in full):
  `handoffs/reviewer/2026-08-14_phase7_review_r1_handoff.md`.
- Plan: `plans/phase_7_evaluations.md` — the **"Amendments (fix r1)" block
  F1–F9 is GOVERNING** over A1–A5 and the base criteria where they conflict.
- Master plan §6.4/§6.5 as updated today (incl. the `no_primary_item`
  skip-line literal) and §9 — note the NEW rules this review earned: P-T 3rd
  ext (counterparty lock MODE), P-Q 4th ext (fixtures vs engine semantics),
  P-R 2nd ext (committing harness named), **P-AB** (parameter-gated helper
  effects enumerated), the deferral rule, P-L 2nd ext.
- **The reviewer's probe files — ADOPT, do not re-derive (F2):**
  `docs/architecture/under_construction/implementation/item_cost_calculation/probes/reviewer_r1/test_reviewer_r1_probe.py`
  (sha256 `a26f11c178d39f000d08c5080cf8b5dfbc1e451848a3d205182e386f2170f9e4`, 484 lines: C1, C2, C3×9, C4, C5 rows 1–4 + the B1
  row, C6/C7 ten refusal rows, C8, C9 skip line, C10, C14 ordering pins) and
  `…/test_reviewer_r1_concurrency_probe.py`
  (sha256 `e42d59d35a395f09ae1155c2bd628a38da1b76338ca1691d2715a7aa58c9035e`, 343 lines: C11 + variants, C5 row 6, C12 row 1 ×
  both chains; committing harness, per-test try/finally teardown, 0.4 s
  bounded waits). Verify the sha256s before adopting; move them into
  `app/tests/integration/services/commands/item_economics/` under phase-7
  names and align parametrize ids to authority rows (P-V standing form).

## Baseline (reviewer-measured, foreground)

Checkpoint `a7f421f`; suite **2037 / 23 / 1 deselected** (failure set
byte-identical to the phase-1 list); focused 92 (router 88 + integration 4);
create_task integration 29; phase-5 valuation 54; DB at head `be9dfe42a035`
(no migration in this phase); graph 166/239, 52 pending, rev `0a71061…`.

## The work (ordered; F-numbers are the plan block's)

1. **F1 (B1 — THE production fix):** gate the mirror block
   (`commit_item_cost_evaluation.py:342-355`) on
   `kind is ItemCostEvaluationKindEnum.COMMITTED`. Add C5 row 7 (projection
   with differing override → NO valuation row; current valuation `client_id`
   + figures unchanged; exactly one valuation row). Named mutation: remove
   the gate → exactly that row reds. Record the P-AB effect enumeration in
   the helper's docstring (chain-close scope, `committed_at`, mirror,
   history, audit, event — all `kind`-gated).
2. **F2 (B2):** adopt both probe files as real phase-7 test files; ids per
   P-V; keep the committing harness and bounded waits. Run the per-row named
   mutations: F1's gate; M1 under F5's corrected `FOR NO KEY UPDATE`
   counterparty; M2 under F6's no-override fixture; M3/M4 (`read=True`
   deletions — **row 1 only** per chain reds; a run where row 2 also reds is
   a finding, flag it); M5 (C1 row 1b); M6/M7 regression re-runs. Ledger:
   N mutations = N rows, per-row sha256 pairs COPY-PASTED, observed pytest
   node ids, distinct hash per label (P-I).
3. **F3 (B3):** rebuild C8's byte-unchanged check on
   `database._session_factory()` (phase-4 recipe,
   `test_phase4_fix_coverage.py:508-582`) — second-session reads before and
   after, ALL columns including `updated_at`; delete the same-session
   assertion.
4. **F4 (S1):** delete the direct-INSERT fixture direction from the adopted
   C2 row set; add the unit translation row (constructed `IntegrityError`
   naming `uix_item_cost_evaluations_current` →
   `ITEM_COST_CONCURRENT_COMMIT` with the uniform sentence).
5. **F5/F6 (S2/S3):** the corrected C11/C5r6 observables are already IN the
   concurrency probe — keep them; do not resurrect the inert forms.
6. **F7 (S5):** delete the dead `task_client_id` branch
   (`promote_item_cost_projection.py:32-34`); the adopted C8 row set keeps
   the cross-WORKSPACE `NotFound` row.
7. **F8 (notes):** N1 delete the no-op validator; N2 fix the
   `auto_commit_…_in_session` docstring ("never raises" is false — the
   handler lives in `create_task.py`); N3 assert the literal
   `no_primary_item` in C9's tenth row (registered §6.5); N4 C10's seam
   asserts fired-exactly-once + second-session visibility (the
   which-module claim is deleted); N7 the P-Z property row asserts
   `superseded_by_id == successor.client_id`; N8 remove the double blank
   line (`create_task.py:308-309`).

## Scope fence

Production: `commit_item_cost_evaluation.py` (F1 gate + N2 docstring),
`promote_item_cost_projection.py` (F7 deletion),
`requests/__init__.py` (N1 deletion), `create_task.py` (N8 whitespace ONLY).
**Nothing else in production** — the reviewer proved the rest correct; an
unplanned production edit is a stop-and-report. Tests: the two adopted
phase-7 files (+ edits to `test_phase7_evaluations.py` for B3/N7, or merge
it into the adopted files — your choice, declared), and
`test_item_economics_router.py` only if C13 ids need aligning. Docs: master
plan tracker + plan Review log + your handoff. **No migration.**

## Closing protocol

1. All criterion rows green (C1–C14 per A-block + F-block); full mutation
   ledger per P-I (incl. the F1 gate mutation and the M3/M4 row-1-only
   declarations).
2. Suite: expect 2037+N / 23 byte-identical / 1 deselected — numbers READ
   off YOUR run (P-L; the tracker's last figures were wrong — S4); the
   committing concurrency subset run twice with residue scope named
   (rule 11½); ruff on changed files; DB at head `be9dfe42a035`; every
   disposable dropped and listed.
3. Tracker → `IMPLEMENTED` (fix r1); Review log entry per P-L (items, never
   bare counts); state every delegation you exercised (file naming, merge
   choice).
4. Checkpoint `CHECKPOINT (not approved): item-cost phase 7 fix r1 —
   <summary>`; handoff AFTER, citing the FINAL hashes of every production
   file you touched, at
   `docs/architecture/under_construction/implementation/item_cost_calculation/handoffs/implementer/2026-08-14_phase7_fix_r1_handoff.md`
   (full path). `⚠ OWNER DECISIONS REQUIRED (n)` if any arise; full write
   perimeter + probe declaration.
