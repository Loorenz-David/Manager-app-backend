# SUMMARY_bootstrap_and_roles_20260515

## Metadata

- Summary ID: `SUMMARY_bootstrap_and_roles_20260515`
- Status: `summarized`
- Owner agent: `GitHub Copilot`
- Created at (UTC): `2026-05-15T00:00:00Z`
- Source plan: `backend/docs/architecture/under_construction/implementation/PLAN_bootstrap_and_roles_20260515.md`
- Related debug plan (optional): none

## What was implemented

- `RoleNameEnum` updated to `{ADMIN, WORKER, MANAGER, SELLER}`. `MEMBER` and `FIELD` removed.
- Bootstrap settings added to `config.py`: six fields (`bootstrap_secret`, `bootstrap_admin_email`, `bootstrap_admin_username`, `bootstrap_admin_password`, `bootstrap_workspace_name`, `bootstrap_workspace_timezone`).
- `.env.example` updated with placeholder values for all six bootstrap env vars.
- `build_ok()` in `routers/http/response.py` extended to support an optional `warnings` field in success responses.
- Alembic migration `ec9017a0245c` created and applied: recreates `role_name_enum` with the new value set (`admin`, `worker`, `manager`, `seller`), cascade-deletes stale `member`/`field` rows, and provides a mirrored downgrade.
- Bootstrap command package created under `services/commands/bootstrap/` with a single-transaction orchestrator (`bootstrap_app.py`) and nine phase files.
- Bootstrap API router created at `routers/api_v1/bootstrap.py` with `POST /api/v1/bootstrap`, protected by `X-Bootstrap-Secret` header guard before any DB interaction.
- Bootstrap router registered in `routers/api_v1/__init__.py`.

## Files changed

- `beyo_manager/domain/roles/enums.py`: `RoleNameEnum` values changed to `admin`, `worker`, `manager`, `seller`.
- `beyo_manager/routers/utils/roles.py`: Role string constants updated to match the new enum values.
- `beyo_manager/config.py`: Six bootstrap `Field` declarations added to `Settings`.
- `beyo_manager/app/.env.example`: Bootstrap env var block appended.
- `beyo_manager/routers/http/response.py`: `build_ok()` updated to accept and return `warnings`.
- `beyo_manager/routers/api_v1/__init__.py`: Bootstrap router import and `include_router` call added.
- `beyo_manager/routers/api_v1/bootstrap.py`: New file — bootstrap route with secret-header guard.
- `beyo_manager/services/commands/bootstrap/__init__.py`: New stub.
- `beyo_manager/services/commands/bootstrap/phases/__init__.py`: New stub.
- `beyo_manager/services/commands/bootstrap/bootstrap_app.py`: New orchestrator — validates env vars, single transaction, calls all nine phases.
- `beyo_manager/services/commands/bootstrap/phases/seed_roles.py`: New — seeds 4 roles idempotently.
- `beyo_manager/services/commands/bootstrap/phases/seed_workspace.py`: New — seeds workspace + 4 `WorkspaceRole` rows, handles partial-seed recovery.
- `beyo_manager/services/commands/bootstrap/phases/seed_admin_user.py`: New — seeds admin `User` + `WorkspaceMembership` idempotently.
- `beyo_manager/services/commands/bootstrap/phases/seed_item_categories.py`: New — seeds 34 item categories (7 seating, 27 wood).
- `beyo_manager/services/commands/bootstrap/phases/seed_issue_types.py`: New — seeds 9 issue types (`source = internal_inspection`).
- `beyo_manager/services/commands/bootstrap/phases/seed_issue_severities.py`: New — seeds 3 severities with `Decimal` multipliers.
- `beyo_manager/services/commands/bootstrap/phases/seed_working_sections.py`: New — seeds 13 sections + 15 dependency edges.
- `beyo_manager/services/commands/bootstrap/phases/seed_issue_category_configs.py`: New — seeds 27 `WorkingSectionSupportedIssueType` + 279 `IssueCategoryConfig` rows. **FIXED**: Initial bug in loop logic only created seating configs for `upholstery_damage` (56 rows missing). Fixed by splitting into explicit loop 1 (all 9 types × 7 seating = 63 rows) and loop 2 (8 wood-applicable types × 27 wood = 216 rows).
- `beyo_manager/services/commands/bootstrap/phases/seed_working_section_item_categories.py`: New — seeds 178 `WorkingSectionItemCategory` rows.
- `migrations/versions/ec9017a0245c_update_role_name_enum_worker_manager_.py`: New migration — role enum recreation with cascade cleanup.

## Contract adherence

- `01_architecture.md`: All seeding logic in command/phase files; router is wiring-only.
- `04_context.md`: `ServiceContext(identity={})` used correctly — bootstrap has no JWT; `ctx.user_id` and `ctx.workspace_id` intentionally empty.
- `05_errors.md`: `ValidationError` (not `ValidationFailed`) raised for missing env vars; `HTTPException(403)` used directly for the secret-header guard, before `run_service`.
- `06_commands.md`: Single `async with ctx.session.begin()` block wraps all nine phases; no per-phase transactions.
- `09_routers.md`: Router uses `run_service` + `build_ok`/`build_err`; secret check fires before service call.
- `30_migrations.md`: Autogenerated migration manually replaced with correct enum-recreation SQL; downgrade mirrors upgrade.
- `40_identity.md`: All seeded models use `client_id` as PK; `flush()` called after each `add()` to trigger identity assignment.
- `21_naming_conventions.md`: Phase files named `seed_<domain>.py`; orchestrator named `bootstrap_app.py`.

## Validation evidence

- `python -m compileall -q beyo_manager migrations`: no errors.
- Import check: `from beyo_manager.services.commands.bootstrap.bootstrap_app import bootstrap_app; print('OK')` → `OK`.
- `APP_ENV=development alembic upgrade head`: `Running upgrade 243e62bcd858 -> ec9017a0245c, update_role_name_enum_worker_manager_seller` — applied successfully.
- Direct DB query `SELECT unnest(enum_range(NULL::role_name_enum))` → `['admin', 'worker', 'manager', 'seller']`.
- **Live HTTP tests** (May 15, 2026):
  - `POST /api/v1/bootstrap` with correct secret → `200 OK`, idempotent on re-run (same IDs returned).
  - `POST /api/v1/bootstrap` with wrong secret → `403 Forbidden`.
  - JWT token issued by bootstrap, sign-in with admin user → valid JWT, correct identity.
  - Database spot-checks (all 12 bootstrap tables):
    - roles: 4 ✅
    - workspace_roles: 4 ✅
    - users: 1 admin ✅
    - workspace_memberships: 1 ✅
    - item_categories: 34 (7 seat, 27 wood) ✅
    - issue_types: 9 ✅
    - issue_severities: 3 ✅
    - working_sections: 13 ✅
    - working_section_dependencies: 15 ✅
    - working_section_supported_issue_types: 27 ✅
    - working_section_item_categories: 178 ✅
    - **issue_category_configs: 279** (63 seating all 9 types, 216 wood 8 types) ✅ — **confirmed after fix**

## Known gaps or deferred items

- None — bootstrap fully tested and validated.

## Handoff notes (if needed)

- None — no frontend contract change.

## Lifecycle transition

- Current state: `summarized`
- Next state: `archived`
- Archive target record: `backend/docs/architecture/archives/ARCHIVE_bootstrap_and_roles_20260515_1200.md`
