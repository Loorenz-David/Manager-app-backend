---
plan: 2
role: implementer
round: 6
date: 2026-08-21
project: live_clock_for_working_time_economics
---

# Session prompt — plan 2 fix (round 6), `live_clock_for_working_time_economics`

## 1. Scope — two rows, one fixture, test-only

Re-review r5 confirmed fix r4 closed four of its five findings correctly, and confirmed the
production code is right for the third independent time. **Nothing in `app/beyo_manager/`
changes this round.** Perimeter:

- `app/tests/integration/services/queries/item_economics/test_phase2_live_surfaces.py`

Any production line is an automatic finding. Two of r5's four findings (S1, S3) were record
corrections and are already applied by the coordinator — they are not yours.

Doctrine: `/Users/davidloorenz/agent-skills/pipeline-charter.md` and
`/Users/davidloorenz/agent-skills/implementation-executor.md`, by absolute path. Task list:
`plans/plan_2.md` §5 C6 and §6's *Path 3 warning verbatim*, plus §7's **six** consumption
entries. Where this prompt differs from the plan file, the plan file wins.

**Both rows below live in one new fixture.** Build that fixture first; the two rows differ
only in what they assert against it.

## 2. B1 — §4.3A path 3 has no guard anywhere in this repository

**Measured twice, independently — by the reviewer and by the coordinator.** Replacing
E-P's `typicals_by_section` with live-derived section sums, immediately before the
`divide_production_budget` call, leaves the whole suite green: **26 / 2478 / 1, added ∅,
removed ∅.** The section weights handed to the allocator are unobserved by every test in
this repository.

`typicals_by_section` is what the intention calls **"the most expensive mistake available
in this feature"** (§4.3A path 3), and `plans/plan_2.md` §6 carries the warning verbatim: a
live typical moves **every** `allowance_seconds` on the payload. The code is correct today
— both typicals sites read only `typical_times_statement`'s SQL aggregate over COMPLETED
steps, and the loader's output cannot reach them. **You are not fixing a defect. You are
writing the row that keeps it true**, and it matters beyond this phase: the
`narrow_typical_work_times` pipeline rewrites `typical_times_statement` for all four of its
consumers on *this pipeline's approval baseline*.

**Required row.**

*Fixture:* one task, **two** working sections, each with at least one step **and its own
qualifying typical** (five completed section-totals per section — the house shape is
`test_budget_allocations_query.py:_seed_typicals`); one step holding an open `working`
record. Give the two sections **different** typicals, so the weights actually distinguish
them.

*Assertions:* the **exact** `allowance_seconds` per section on the E-P payload, and the
same on E-A's `steps[]` **in the same test** — `_load_typicals` is the identical construct
at the other surface, and master §5 says sweep the class, not the instance.

*Named mutation, call site, `get_task_production_time.py:get_task_production_time`, between
the typicals loop and the `divide_production_budget` call:* add each section's
live-minus-settled delta into `typicals_by_section` (a typical that ticks while someone
works).

**Both sides must be computed before the row ships.** The section holding the open record
gains weight and the other loses it — but **if the typicals you choose make the two shares
tie, the fixture is inert and must be re-chosen.** Compute the allocator's output under both
weight vectors on paper first, and put the numbers in your ledger. Note carefully: **nothing
existing will catch a mistake in this row's own construction**, precisely because the ∅
above says the weights are unguarded today. This row has no safety net but your own
arithmetic.

## 3. S2 — C6 row 1 cannot fail on the property it names

`test_c6_allowances_are_byte_identical_after_settlement_recompute` serves the payload with
an open record, closes it through `_apply_step_transition`, runs
`_recompute_step_time_totals`, serves again, and asserts allowance byte-identity. It cannot
fail, for **two independent measured reasons**:

**(a) The two sides are computed from an identical input vector.** The reviewer captured
what `divide_production_budget` is handed on each serve:

```
BEFORE: {tsp_failed_…: 1200, tsp_live_…: 600, tsp_phase2_skipped_…: 240}
AFTER : {tsp_failed_…: 1200, tsp_live_…: 600, tsp_phase2_skipped_…: 240}   identical: True
```

After the recompute the settled figure **equals** the live one — the phase's own C9 row
asserts exactly that from the payload side (`2040 → 1440 → 2040`). Two payloads built from
identical inputs are identical for every function of those inputs, **including a defective
one**: a mechanism that made `allowance_seconds` live-dependent would move both sides
equally and the comparison would still hold.

**(b) The compared value is the identity element.** That fixture's one section reports
`(allowance_seconds, left_seconds, worked_seconds) = (0, -2040, 2040)` on **both** sides —
the excluded steps charge 1440 s against a 1200 s budget, so `distributable_seconds` is
`max(0, −240) = 0`. **This is the exact degeneracy review r3's B1 condemned in the C2 row,
on the same `_make_live_fixture`.** Fix r4 built C2 a new fixture and left this row standing
on the old one — the class swept for one instance and not the other.

**Required correction — pick one and say which you picked:**

- **Preferred:** re-anchor the row so the discriminating comparison is **live-basis versus
  settled-basis** on a fixture with a **positive** allowance — the B1(a) substitution shape
  that already works for C2, and which B1's new two-section fixture gives you for free.
- **Acceptable:** keep the row as-is and record, in a comment beside it, that what it
  guards is the **≤ 1 s settle/live parity bound** (intention §3.3A) and *not* allowance
  independence — plus the two reasons above, so no later round re-derives them.

Do not delete the row; its `left_seconds` comparison is what makes the settled-substitution
mutation redden it, and that is real coverage.

## 4. Ledger obligations

Only the rows this round creates or changes — do not re-sweep the fourteen (master §5,
narrowed mutation set). **Record the tree each row was measured at** (`git rev-parse --short
HEAD` plus your own clean count); a sweep is not a round, and a foreign commit can land
inside one. Master §7 lists the recognized external streams; **a golden JSON moved by
anything but your own work is an escalation — stop and report.**

Owed:

- B1's typicals mutation: both sides as exact allowance integers per section, on both E-P
  and E-A, plus the added/removed ID sets.
- If you re-anchor S2: the settled-substitution mutation must redden the re-anchored row —
  measure it, and confirm the four-ID set from fix r4's B1(a) row is unchanged apart from
  your addition.

## 5. Notes — recorded so you do not re-file them

- **N1** — the byte-identity rows' `counted.calls == 2` duplicates C4's own counter. That
  assertion is what carries those rows' real weight; leave it.
- **N2** — the C6 test asserts the same `all(... division_inputs ...)` clause twice, the
  second subsuming the first. Harmless.
- **N3** — the section-weight input is unguarded for *any* wrong value, not only a live
  one. That wider gap is pre-existing coverage debt in the budget-division family, recorded
  for `plans/plan_4.md`. **B1 asks only for the live-direction row**, which is this phase's
  to owe. Do not widen.
- **C6 clause (iii)** was recorded as shipped and never existed; the coordinator struck the
  claim from both records at the r5 fold. Do not write it — it named the payload's `typical`
  block, which is a different dict from the weights, and on that fixture it would have
  asserted `None == None`. B1 is its replacement.

## 6. Closing protocol

1. Full suite green except the enumerated baseline; count **and** ID-set diff, tree recorded.
2. Checkpoint commit at IMPLEMENTED, prefixed `CHECKPOINT (not approved):`.
3. Update only phase 2's tracker row; findings to `plans/plan_2.md` §7.
4. Archgraph: nothing owed; declare it if you touch it.
5. Handoff at `handoffs/implementer/2026-08-21_phase2_fix_r6_handoff.md`, frontmatter
   `plan: 2`, `role: implementer`, `round: 6`, `state`, `date`, `actor`. Carry: the
   owner-readable opening; `⚠ OWNER DECISIONS REQUIRED (n)`; the ledger with tree provenance
   and both sides per row; which S2 option you took and why; and your full write perimeter
   including every file touched by a probe.
