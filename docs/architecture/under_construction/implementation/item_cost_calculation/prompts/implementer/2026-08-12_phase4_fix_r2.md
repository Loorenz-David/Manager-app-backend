---
plan: phase 4 (configuration services)
role: implementer
round: 2 (fix cycle — test-heavy + one production defect class)
date: 2026-08-12
---

# Session prompt — phase 4 fix cycle (resolve review r1: B1, B2, S1, S2 + N2)

You are the **fixing agent** for phase 4. **Resolve, don't relitigate; add nothing
beyond the findings.** The mechanisms were verified substantially correct — this
cycle is (a) writing the ~54 missing criterion rows and (b) one production defect
class: request-bounds validation.
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(project folder: `docs/architecture/under_construction/implementation/item_cost_calculation/`).

## Doctrine (read by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md` — fix-cycle variant.

## Gate check

- Tracker: phase 4 `CHANGES_REQUESTED` (reviewer r1).
- Read the FULL reviewer r1 entry in the plan's Review log (it carries the
  per-criterion correction clauses and the corrected C6 arbiter) and the handoff
  `handoffs/reviewer/2026-08-12_phase4_review_r1_handoff.md`.
- The plan was amended by the coordinator 2026-08-12 post-review: C6's
  interleaved row (the original deadlocks — FK `KEY SHARE` vs `FOR UPDATE`), C5's
  term-index dual paths (unreachable — pre-check + note), task 2's
  request-bounds block (B2). Build against the amended text.

## Findings to resolve

### B1 — the missing criterion rows (the Review log's per-criterion clauses govern)

Coverage measured at ~6 of ~60. Write every row the amended C1–C11 enumerate,
including: C1's 18 missing admission rows (each rejection identity, both chains);
C2's adjacency rows **through command-built chains** (charter rule 3 — not
SimpleNamespace) + the closes-v1 assertion; C3's two GENUINE concurrent races per
the harness block (the reviewer already proved both buildable — see the Review
log's working recipe); C4's underflow pair + a distinct S4-parse row (a different
fixture from the B1 canonicalization row); C5's 12 cells + pre-check rows + the A6
router-surface assertion; C6's serial row + the AMENDED interleaved row
(`reference_blocked_while_locked`); C7's five rows; C8's rows 1–5 **through the
status query** (row 6 exists) + commit the structural permutation probe; C9's
router-model docs assertion; C10's 14 rows; C11's per-route rejection/retention
rows + 9 audit rows. **Router-level rows use a named harness per P-R**
(`TestClient` or `app.routes` introspection — state which in your Review log
entry).

### B2 — request-bounds validation (production code; the plan's task-2 block governs)

All eight reviewer-proven 500 cases become 422/`ValidationError` naming the field:
ranges per the amended task 2 (`> 0`, `(0,100]`, `[0, 999.999]`, `≥ 0`,
per-type nullability). One criterion row per bound per field (adjacent-pair
boundaries), asserted at the request layer.

### S1 — the router body model advertises the derived rate

> Remove `cost_per_worker_minute_minor` from the ROUTER body model (the OpenAPI
> schema must not advertise a derived value as input); the smuggled-field row
> then asserts the pydantic-level ignore at the command model per the N4 pin.

### S2 — dead scaffolding (charter rule 4)

`_common.reference_exists` has no caller; `get_group(for_update=)` is never passed
`True`. Delete them or wire them — whichever the actual C6/C7 implementations
need; nothing ships uncalled.

### N2 (required — §6A.1) — enum members compared as members, never `.value` strings

Fix the `.value` comparison the review located; one row asserting a member-level
comparison survives an enum-value rename is NOT required (next touch) — just fix
the comparison.

### Optional (documented if taken): N1 (pin S1-before-S2 flush ordering), N4
(uniform conflict message shape), N5, N10 (vestigial `version = None`).

## Mutation discipline (P-I twice-extended + P-T — binding)

Every new row family gets its named mutation run/reverted with **observed pytest
node ids per row**: the C11 MANAGER-removal must now redden committed tests (it
reddened NOTHING in r1); the C9 description-deletion likewise; C6's two split
mutations per the amended observables; C5's blanket-collapse must redden the
translation rows. Disposable worktrees; sha256s; teardown discipline for
committing rows (the reviewer's killed-run residue note shows why).

## Allowed file perimeter

- `app/beyo_manager/services/commands/item_economics/` (B2 bounds in
  `requests/__init__.py`; S2 deletions; N2 fix where located)
- `app/beyo_manager/routers/api_v1/item_economics.py` (S1 field removal only)
- new/changed tests under `tests/unit/...item_economics/` and
  `tests/integration/...item_economics/`
- plan Review log + master-plan tracker row (append; stamps preserved) + your
  handoff

No other production file. The 47 pending graph items are NOT yours (held for
post-approval; your line-number shifts will be absorbed at adjudication).

## Closing protocol

1. All amended criteria green; per-row mutation declarations with observed ids.
2. Full suite green: baseline 23 known failures byte-identical (N14 caveat);
   expect substantial collection growth (~+60).
3. Archgraph: zero delta this cycle (the phase delta exists; do not touch) —
   state it.
4. Tracker → `IMPLEMENTED` (fix r2); Review log per P-L (what was built per
   criterion, exact counts).
5. Checkpoint `CHECKPOINT (not approved): item-cost phase 4 fix r2 — <summary>`;
   **handoff deposited AFTER, citing the final hash**, at
   `docs/architecture/under_construction/implementation/item_cost_calculation/handoffs/implementer/2026-08-12_phase4_fix_r2_handoff.md`
   (full path).
