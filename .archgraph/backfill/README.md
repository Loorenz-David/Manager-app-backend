# Evidence span backfill — generated 2026-08-23

Generated by `ArchitectureGraph/scripts/backfill-evidence-spans.py`. Nothing in
this directory has been applied to the graph. See `summary.json` for counts.

| File | What it is |
|---|---|
| `batch-01.json` … `batch-11.json` | 193 `re-anchor` operations (220 evidence entries), 11–21 per file (sized to the server's 16 KB change-record cap). Each drops a line range and keeps path + symbol. Validated against `ChangeOperationSchema`; every path and symbol verified to resolve; **all 11 applied cleanly in a dry run against a copy of this workspace on 2026-08-23.** |
| `needs-repair.md` | 5 entries whose symbol no longer exists anywhere. **Not** in the batches. Real staleness — needs a human decision. |
| `pending-review.md` | 1 item still pending review. `repair_anchors` refuses pending items; put `anchors` on its review decision instead. |
| `span-only.md` | 280 entries with a span and no symbol. **Not** in the batches. Separate judgment pass, later. |

The batch files are disposable once applied. The audit trail lives in the
change records the server writes and in `metadata.evidenceHistory` on each
touched entry.

---

## Prompt for the submitting agent

```markdown
## Apply the evidence span backfill — mechanical, four calls

Context: on 2026-08-23 the operating policy changed so evidence anchors to a
path and a symbol, not a line range. The files in `.archgraph/backfill/` are
pre-generated `re-anchor` operations that drop the line range from every
evidence entry that already has a symbol. They were produced by a script from
the graph file, validated against the server's contract, and every path and
symbol was checked to resolve. Your job is to submit them, not to judge them.

Do exactly this, in order, and stop at the first failure:

1. `archgraph_status`. Note `revision`, `nodeCount`, `edgeCount`, and
   `pendingReviewCount`. These must be unchanged at the end except `revision`.
2. For each of `batch-01.json` through `batch-11.json`, in numeric order:
   a. Call `archgraph_status` and take the current `revision`.
   b. Read the batch file. Call `archgraph_repair_anchors` with
      `operations` and `humanInstruction` exactly as they appear in the file,
      plus `expectedRevision` set to the revision from step (a).
      Do not edit, reorder, drop or add operations. Do not add `startLine`
      or `endLine` to any anchor.
   c. Record the returned `applied` count and `changeRecordPath`.
      Expected: 21, 21, 20, 20, 20, 15, 15, 17, 16, 17, 11.
3. `archgraph_status` again. Confirm node and edge counts and
   `pendingReviewCount` match step 1.
4. Run the measurement from the workspace root and report the output:

   python3 - <<'EOF'
   import yaml
   d = yaml.safe_load(open('.archgraph/architecture.yml'))
   ev = span = sym = hist = 0
   for coll in (d['nodes'], d['edges']):
       for n in coll:
           for e in (n.get('metadata') or {}).get('evidence') or []:
               ev += 1; span += bool(e.get('startLine')); sym += bool(e.get('symbol'))
           hist += len((n.get('metadata') or {}).get('evidenceHistory') or [])
   print(f"evidence={ev} with_span={span} with_symbol={sym} history={hist}")
   EOF

   Before (measured 2026-08-23): evidence=546 with_span=507 with_symbol=261 history=115.
   Expected after: evidence=546, with_span=287 (280 span-only + 5 in
   needs-repair.md + 2 on the pending-review item), with_symbol=261,
   history=335 (115 + 220).

If any call is refused, report the error verbatim and stop. Do not retry with
modified operations, do not fall back to `archgraph_preview_maintenance_changes`,
and do not touch `needs-repair.md`, `pending-review.md` or `span-only.md` — those are for a human.

Report: the eleven `applied` counts, the change record paths, the
before/after status numbers, and the measurement line.
```
