---
plan: phase 8B (inline item prices at task creation)
role: implementer (fix)
round: 2 (fix cycle after review r1)
date: 2026-08-15
---

# Session prompt — phase 8B fix r2 (two test items, one file)

You are the **fixing agent** for phase 8B. Review r1: CHANGES_REQUESTED —
0 blocking / 2 should-fix / 3 notes, all test-side, all local to
`test_phase8b_inline_task_prices.py`. The production mechanism is proven
correct on every branch (including live proof that a price survives a
failed auto-commit). This is the smallest fix cycle of the project — two
items plus ledger discipline. Do not expand it.
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(commands from `backend/app/`, per master plan §10).

## Doctrine (read by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md`

## Ground

- Review handoff (read in full — its S1 probe recipe and S2 traceback are
  your specs): `handoffs/reviewer/2026-08-15_phase8b_review_r1_handoff.md`.
- Plan: `plans/phase_8b_inline_task_prices.md` — B1–B10 + the **fix-r2
  block F1–F4, GOVERNING**.
- §9 — note the four NEW rules this review earned (per-alternative rule,
  rule-11½ capture-locals mode, **P-I 10th ext**: deletion mutations
  pinned by LINE RANGE and observed-red states its SCOPE, birth-anchor
  rule). Your ledger complies with the 10th ext.

## Baseline (reviewer-verified, foreground)

Head `c1d2e3f4a5b6`; suite **2183 / 23 / 1 (2207 collected)**, failure IDs
byte-identical to the phase-1 list; focused 66; all production hashes =
`513856d` blobs (nothing changes in production this cycle).

## The work (the COMPLETE list)

1. **F1 — C4 row 4 (superseded-only accept):** seed via the three
   production commands (`set_item_valuation` ×2 → `delete_item_valuation`
   on v2 — the reviewer's probe recipe); assert the pre-state (no current
   valuation; v1 superseded-not-deleted; v2 deleted-not-superseded); then
   `create_task` + trio ACCEPTS, chain grows to THREE rows, new row
   current. Run mutation **M6** (delete `superseded_at.is_(None)` at
   `create_task.py:331` — that line exactly) → this row reddens; revert;
   mutant + restored hashes in the ledger.
2. **F2 — capture-locals in C4 rows 2/3:** hoist `workspace.client_id` /
   `user.client_id` (and any other needed instance attribute) into plain
   locals BEFORE the `try`, row 1's shape. Then prove the 11½ second
   failure mode is dead: run row 2 ONCE under the M2 mutant (invert the
   refusal predicate, declared hash `f0776418…`), observe the red, and
   assert-by-state-query ZERO `phase8b` residue afterwards — record the
   query and its zero.
3. **F3 — ledger discipline:** every mutation row pins its line range;
   observed-red states its scope (name the focused set); no state claims
   without state queries.

## Scope fence

ONE test file: `test_phase8b_inline_task_prices.py`. Docs: master plan
tracker + plan Review log + your handoff. **ZERO production edits, zero
other test files, no migration, graph READ-ONLY zero delta** (the 5
pending stay held; F4's span corrections are the coordinator's). Anything
else is stop-and-report.

## Closing protocol

1. Both items green; the M6 + M2-rerun ledger (line-pinned, scoped,
   hashed); the F2 zero-residue state query recorded.
2. Suite: expect 2183+1 / 23 byte-identical / 1 deselected (the one new
   row) — numbers READ off your foreground run; ruff on the file; DB at
   head `c1d2e3f4a5b6`; zero `phase8b` residue at close (state query).
3. Tracker → `IMPLEMENTED` (fix r2); Review log per P-L; final sha256 of
   the test file.
4. Checkpoint `CHECKPOINT (not approved): item-cost phase 8B fix r2 —
   <summary>`; handoff AFTER, at
   `docs/architecture/under_construction/implementation/item_cost_calculation/handoffs/implementer/2026-08-15_phase8b_fix_r2_handoff.md`
   (full path). `⚠ OWNER DECISIONS REQUIRED (n)` if any arise; full write
   perimeter + probe declaration.
