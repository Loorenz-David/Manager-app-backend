---
plan: phase 9 (living docs & drift routing — the LAST phase of v1)
role: reviewer (projection)
round: 0 (pre-implementation projection gate)
date: 2026-08-15
---

# Session prompt — phase 9 projection, round 0

> **HOLD — do not run until phase 8B is APPROVED.** Round 18 (2026-08-15)
> added two things after this prompt was first authored: phase 9 now
> carries the FRONTEND HANDOFF deliverable (R18-2 — see the plan's
> "Scope addition" block, GOVERNING) and is BLOCKED on phase 8B (inline
> task prices, R18-1), because the handoff and the living-docs page must
> document the post-8B flow. When you run: add the handoff's accuracy
> arbiters and its completeness (all ten routes + 8B's trio + the
> removals) to your axes, and re-verify the environment facts below
> against post-8B reality (head, baseline, graph revision will all have
> moved).

You are the **projectionist** for phase 9 — the final phase: the living-docs
page and the accumulated drift batch. The plan was written 2026-08-11,
BEFORE all eight mechanism phases shipped and seventeen decision rounds
ran. Unlike every prior phase, this one is mostly DOCUMENTATION — the gate
here is about ACCURACY of what gets written (a living-docs page that
mis-states a shipped mechanism is drift manufactured at birth) and
COMPLETENESS of the drift batch (every forward note routed here across
eight phases must land or be explicitly re-routed).
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(commands from `backend/app/`, per master plan §10).

## Doctrine (read by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/plan-projection.md`

## Ground

- Plan under projection: `plans/phase_9_docs_and_drift.md` **including its
  accumulated forward notes** — phase-5 N5, phase-6 (r1 N9 deploy
  ordering; r2 N1 drop-migration docstring line), phase-7 (fix-r1
  docstring/plan corrections already applied — verify nothing re-opened),
  phase-8 projection L17 (4B N3 redundant clause), phase-8 re-review-r3
  N1 (the STRUCTURAL compiled-statement filter arbiter — the one CODE
  item of weight in this phase), N2 (C5 closed-task premise), N4
  (serializer test formatting).
- Also owed to this phase from the intention: **§2.6's documentation-drift
  list + gate handoff D-1…D-4** (§13 makes §2.6 routing MUST-SHIP; D-1
  five serialize_step call sites; D-3's archgraph anchor drift is long
  resolved — verify and mark), and **D22** (frontend doc mirrors — the
  three money keys' removal mirrored into frontend-facing docs).
- Semantic authority: intention §11 (operations & API surface as shipped),
  §16 rounds 0–17 (the changelog IS the drift record), master plan §6
  (the registry the docs must agree with), §9 ALL rules (the projection
  axes' rulebook — now ~50 entries), §10 (environment: head
  `c1d2e3f4a5b6`, baseline 2138/23/1 = 2161 selected).
- Shipped reality: ALL EIGHT mechanism phases APPROVED; the graph is
  fully human_confirmed (173 nodes / 256 edges, 0 pending, 0 stale, rev
  `45b72196…`) — the graph is now a RELIABLE index for the docs page;
  `docs/domains/item_economics.md` does not exist yet (the phase creates
  it).

## Projection axes (minimum — the ledger is yours)

1. **Forward-note census:** enumerate EVERY forward note routed to phase 9
   across all eight phases' Review logs and closeout entries (the plan's
   own notes + grep the archives for "phase 9" / "phase-9"); the ledger
   carries one row per note — landed in a task, or explicitly re-routed
   with a destination. A note that silently evaporates is this phase's
   B-class defect.
2. **The living-docs page's accuracy criteria:** a docs page has no test
   suite — what arbitrates it? Propose the harness (e.g., every
   path/route/identity/event name in the page greps to an existing
   artifact; the §6.4/§6.5 registry as the authority; the twelve-member
   status vocabulary verbatim from the enum). A page criterion with no
   arbiter is the phase-8 lesson applied to prose.
3. **The one code item of weight:** r3-N1's structural filter arbiter
   (three services' compiled SELECTs carry the three literal clauses).
   It touches test files only — but it must satisfy the §9
   structural-filter rule as written, and the plan should name the
   compiled-statement inspection mechanism (str(query) or the SQLA
   compile API) and its expected-red mutation per site.
4. **Deploy-ordering doc (phase-6 N9):** the column-drop rolling-deploy
   hazard — where does it live so an operator finds it?
5. **Scope fence:** phase 9 must NOT grow mechanism. Every code touch is
   enumerable now (r3-N1 test rows, N2 fixture, N4 formatting, 4B N3
   one-clause, the drop-migration docstring line, §2.6's specific items) —
   the projection pins the COMPLETE list so the implementer prompt can
   forbid everything else.
6. **v1 closure shape:** this phase ends v1 — the projection should state
   what the closeout must verify (all §13 must-ship rows discharged; the
   owner's post-v1 items — the migration squash seed with Findings 1–7,
   the N11 residue research — handed off cleanly, not silently dropped).
7. **Decidability:** reject any clause whose prose and verbatim predicate
   disagree.

## Closing protocol

1. Deposit an amendment LEDGER (numbered rows, severity, section amended,
   verified corrections — executed where cheap). `⚠ OWNER DECISIONS
   REQUIRED (n)` for anything semantic (story-shaped cards, branches +
   recommendation + on-silence).
2. No code, no plan edits, archgraph READ-ONLY (state revision
   `45b72196…`, 0 pending, 0 stale — zero delta).
3. Deposit at
   `docs/architecture/under_construction/implementation/item_cost_calculation/handoffs/reviewer/2026-08-15_phase9_projection_r0_handoff.md`
   (full path, AFTER your writes): ledger; the forward-note census; full
   write perimeter + probe declaration.
