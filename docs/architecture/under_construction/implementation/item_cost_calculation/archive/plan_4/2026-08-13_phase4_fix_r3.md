---
plan: phase 4 (configuration services)
role: implementer
round: 3 (fix cycle — test-side only, corrections pre-verified)
date: 2026-08-13
---

# Session prompt — phase 4 fix cycle r3 (resolve re-review r2: B1, B2, S1–S4)

You are the **fixing agent** for phase 4, round 3. Every correction below was
verified by the r2 reviewer (each gap proven by a mutation that stayed green).
**Test-side only — no production code changes. Resolve, don't relitigate.**
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(project folder: `docs/architecture/under_construction/implementation/item_cost_calculation/`).

## Doctrine (read by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md` — fix-cycle variant.

## Gate check

- Tracker: phase 4 `CHANGES_REQUESTED` (re-review r2).
- Read the re-review r2 Review-log entry and
  `handoffs/reviewer/2026-08-13_phase4_rereview_r2_handoff.md` — corrections are
  verbatim there.

## Items (each with its reviewer-verified correction)

### B1 — the §7A.4 NULL-open column (P-V binds: rows map to the TABLE)

Three rows per chain with a **live `effective_from IS NULL` open row**: row 4
(`NULL` requested → `…_EFFECTIVE_FROM_REQUIRED`), row 5 (`≤ today` → accept AND
assert the predecessor's `effective_to == d`), row 6 (`> today` →
`…_EFFECTIVE_FROM_FUTURE`). Remove the duplicated cases so the 20 rows map
one-for-one onto §7A.4's table (state the mapping in the test's parametrize ids).
Named mutation: **drop `open_from is not None` at `_common.admission_error`'s
comparison — row 5 must redden** (observed pytest id).

### B2 — sole-cause filter fixtures + the rename dual-path (P-W binds)

Per filter row (3 queries × workspace/`is_deleted`), the filtered-out row must
otherwise land **inside** the asserted slice (sort it first or raise `limit`).
Two rename-collision rows: pre-check (`ValidationError`/422) AND DB path — note
the r2 finding that deleting the pre-check silently degrades the class to
409; the DB-path row goes through the translation table per the C3/C5 precedent.
Mutations: each of the five r2-green deletions (workspace ×2, is_deleted ×2,
pre-check delete) must now redden its named row.

### S1 — the race asserts its identity

Both C3 race rows assert the loser's message **starts with the chain-qualified
token** (`ITEM_COST_CONCURRENT_BASIS_VERSION` / `_MODEL_VERSION`) + class. The
r2 mutation (basis index → model identity) must now redden the real race row,
not only the translation proxy. (P-I third extension: your own r2 ledger
recorded this gap — it closes now.)

### S2 — the straddling parse fixture

A fixture where `Decimal(str(v))` and `Decimal(v)` differ:
`monthly_paid_hours = 2.675` → `2.68` via str-parse, `2.67` via float-parse.
Mutation: `Decimal(str(value))` → `Decimal(value)` at `requests/__init__.py:19`
must redden it (it left 126/126 green in r2).

### S3 — one adjacent pair per bound

Reject-at-excluded + accept-at-included for every bound: `0`/`1` (gt=0 fixed
cost); `100` accept / `100.01` reject (utilization); `0` accept and `999.999`
accept / `1000` reject (percent); `0` accept / `-1` reject (fixed amount);
hours per its bound. Mutation: `gt=0 → ge=0` on `fixed_monthly_cost_minor` must
redden the zero-reject row (it left 126/126 green in r2 and reintroduces a 500).

### S4 — bound every wait

Wrap C3's `flush_complete.wait()` and `release.wait()` in `asyncio.wait_for`
with explicit timeouts (C6's 0.3 s precedent). The r2 hang (a winner failing
upstream blocked 120 s and left the N3 residue) is the proof of need.

### Optional (documented if taken): N4 (assert the persisted scale, not just
`__eq__`), N5 (parametrize C8's six fixtures — P-G(b)/P-I naming), N6 (declare
the C3 audit-monkeypatch seam in the plan's harness terms via your Review-log
entry).

## Perimeter

Test files under `tests/{unit,integration}/…/item_economics/` +
`tests/unit/routers/api_v1/test_item_economics_router.py`; plan Review log;
master-plan tracker row (append; stamps preserved); your handoff. **No
production file** — if a correction seems to need one, stop and report.

## Closing protocol

1. All rows green; every mutation above run/reverted with observed pytest ids +
   sha256s (per-row, P-I).
2. Full suite: expect ~1875+N passed / 23 known failures byte-identical (N14
   caveat); run C3/C6 twice to prove teardown.
3. Archgraph: zero delta — state it (the 47 items stay held).
4. Tracker → `IMPLEMENTED` (fix r3); Review log per P-L.
5. Checkpoint `CHECKPOINT (not approved): item-cost phase 4 fix r3 — <summary>`;
   handoff AFTER, citing the final hash, at
   `docs/architecture/under_construction/implementation/item_cost_calculation/handoffs/implementer/2026-08-13_phase4_fix_r3_handoff.md`
   (full path).
