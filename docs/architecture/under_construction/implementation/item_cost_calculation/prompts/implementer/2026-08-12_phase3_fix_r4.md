---
plan: phase 3 (canonical calculator)
role: implementer
round: 4 (fix cycle — small, corrections pre-verified)
date: 2026-08-12
---

# Session prompt — phase 3 fix cycle r4 (S6 + N16 + the R10-2 shape pin)

You are the **fixing agent** for phase 3, round 4. Every correction below was
**verified end-to-end by the r3 reviewer** — this is execution, not exploration.
**Resolve, don't relitigate; add nothing beyond the findings.**
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(branch `fix/idempotent-completion-analytics`). Project folder:
`docs/architecture/under_construction/implementation/item_cost_calculation/`.

## Doctrine (read by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md` — fix-cycle variant.

## Gate check

- Tracker: phase 3 `CHANGES_REQUESTED` (reviewer r3, 0 blocking / 1 should-fix).
- Read the reviewer r3 Review-log entry and
  `handoffs/reviewer/2026-08-12_phase3_rereview_r3_handoff.md` (S6 incl. its
  verified correction; N14; N16).

## Items to resolve

### S6 — give the pinned cascade its arbiter (correction clause verbatim)

> in `test_rederive_rate_mismatch_reports_rate_and_allowed_cascade_payload`, set
> the fixture's stored rate to `Decimal("399.5000")` and its expected allowance
> entry to `rederived_value = stored_value = Decimal("5.42")`. At `399.5000` the
> allowance still re-derives to `5.42`, so the entry can only come from the
> cascade clause.

Reviewer-measured: fixture swapped + clause intact → 65 passed; clause deleted →
exactly that row fails. (`399.9000` also works; `399.0000`/`401.0000` do not —
use `399.5000`.)

### N16 — production object type (charter rule 3)

`test_rederive_malformed_purchase_snapshot_returns_integrity_marker`
(`test_calculator.py:509`) passes a `SimpleNamespace`; swap to an unsaved
`ItemCostEvaluationTerm` like the other five rederive rows.

### N14 — homogeneous payload shape (R10-2, intention §6A.11)

Every mismatch entry carries the same four keys — `field`, `rederived_value`,
`stored_value`, `error` — with `error = None` on plain value disagreements (today
only converted entries carry `error`). Update the existing exact-payload rows to
include `error` and add no new test beyond adjusting them (they already assert
exact dicts, so the shape pin rides the existing rows).

### Optional (fixer's judgment, documented if taken)

- **N12:** move the `term_row.name` read inside the defensive perimeter.
- **N13:** collapse the dead identical-return branching at `calculator.py:472-477`.

## Mutation discipline (P-I as twice-extended — binding)

- S6's mutation: **delete the `or rate != stored_rate` clause** (`calculator.py:533`)
  → report the **observed failing node id from your run** (L8: never a prose
  description; the r3 declaration defect was exactly a plausible-but-wrong name).
  Expected: exactly `test_rederive_rate_mismatch_reports_rate_and_allowed_cascade_payload`.
- N14's rows: corrupt the `error` key on a plain entry → the adjusted exact-payload
  row(s) redden; report observed node ids.
- Revert everything; sha256s of both files in the handoff.

## Allowed file perimeter

- `app/beyo_manager/domain/item_economics/calculator.py` (N14 shape + optional N12/N13 only)
- `app/tests/unit/domain/item_economics/test_calculator.py`
- plan Review log + master-plan tracker row (Note append; stamps preserved)
- your handoff

## Closing protocol

1. Focused + full suite green (expect 1749 passed / 23 failed / 1 deselected —
   no new tests unless you split rows; failure set byte-identical; N14-flake
   caveat).
2. Tracker → `IMPLEMENTED` (fix r4); Review log entry per P-L.
3. Checkpoint: `CHECKPOINT (not approved): item-cost phase 3 fix r4 — <summary>`.
   **Deposit the handoff AFTER the checkpoint, citing the final hash** (r3's
   handoff was committed inside its checkpoint and could cite nothing — do not
   repeat).
4. Handoff at
   `docs/architecture/under_construction/implementation/item_cost_calculation/handoffs/implementer/2026-08-12_phase3_fix_r4_handoff.md`
   (full path): frontmatter complete incl. `round: 4` and `verdict`; summary;
   `⚠ OWNER DECISIONS REQUIRED (n)`; per-row observed mutation node ids; final
   sha256s; full write perimeter incl. the final checkpoint hash.
