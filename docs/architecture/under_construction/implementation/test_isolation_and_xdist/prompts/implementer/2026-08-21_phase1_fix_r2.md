---
plan: 1
role: implementer (fix)
round: 2
date: 2026-08-21
project: test_isolation_and_xdist
---

# Session prompt — plan 1 fix r2, `test_isolation_and_xdist`

## 1. Scope

One fix cycle against the coordinator's consumption of implement r1 (`plans/plan_1.md`
Review log, entry dated 2026-08-21) and **two owner decisions taken in response**
(`planning/intention.md` §4, OD-1 and OD-2). Resolve them; do not relitigate; add nothing
beyond them.

**Read first, by absolute path, as doctrine:** `/Users/davidloorenz/agent-skills/pipeline-charter.md`
(including "Test-evidence scope and reuse") and `/Users/davidloorenz/agent-skills/implementation-executor.md`.
Then `planning/intention.md` §4 and `plans/plan_1.md` in full. **Where this prompt and the
plan differ, the plan wins.**

**Still out of scope: `pytest-xdist`.** No install, no `-n`, no parallel run. That is phase 2.

## 2. What was right — keep it

The guard, the lifecycle, the marker, the teardown and the perimeter discipline all hold and
were verified independently: the fail-closed check (pattern → configured-DB identity → marker,
plus `_quoted_identifier` against injection), the fixed-name reabsorption, and a clean server
after the run. **None of that is being asked to change.** The change is to what the template
contains.

## 3. OD-1 — the template carries schema only (blocking)

**Remove `_restore_baseline_data` and the whole `pg_dump`/`pg_restore` path.** The template is
built by migrating a fresh database to head and marking it — migration-owned seed rows and the
disposability marker only. This also deletes finding F6 (the local-CLI fallback that prompts for
a password and would hang a non-interactive run).

The owner's reasoning, measured by the coordinator, so you do not need to re-derive it:

| configuration | failures | wall time |
|---|---|---|
| original, shared dev database | 26 | 135 s |
| as you delivered it (isolation + dev-data restore) | 26 | 169.7 s |
| **schema-only template** | 32 | **109.1 s** |

Schema-only is **60 s per run cheaper** than what shipped and 26 s cheaper than the original.

**Then give these nine tests the reference data they need**, explicitly — as fixtures or a
version-controlled seed helper, **not** by copying from any live database. They are the entire
cost of the decision, enumerated:

- `tests/integration/scripts/backfill/test_backfill_worker_shift_state_records.py::test_backfill_matches_sweep_read_and_is_idempotent`
- `tests/integration/services/commands/test_system_transition_reasons_retirement.py::test_constraint_does_not_reject_the_declared_state_projection`
- `…::test_constraint_rejects_a_step_record_carrying_both_explanations`
- `…::test_pause_ended_shift_is_still_selectable_through_the_endpoint`
- `…::test_retirement_left_the_guarded_populations_alone`
- `tests/integration/services/commands/users/test_kiosk_floor_flow.py::test_kiosk_clock_out_reports_working_steps_it_force_closed`
- `…::test_kiosk_full_loop_from_floor_sign_in_to_clock_out`
- `…::test_kiosk_roster_matches_worker_without_a_clock_code_by_email`
- `tests/integration/services/commands/users/test_worker_shift_commands.py::test_clock_out_reconstructs_middle_from_step_history`

**Diagnose before you seed.** For each, establish *what* row it actually needs and give it that,
in the narrowest form that makes the test's own assertion the reason it passes. Do not seed a
broad "reference dataset" so the failures go away — that reproduces the coupling this decision
removes, one layer down. If any of the nine turns out to need a production-domain decision
rather than a fixture, **stop and raise a decision card**.

## 4. OD-2 — republish the baseline, explained (blocking)

The new authoritative baseline is established under schema-only isolation and is expected to be
**~22 inherited failures** (32 observed minus the nine above minus the C6 row once §5 lands).
Publish it as **failure-ID set + tree identity + database identity**, with the count subordinate
— the schema `live_clock` phase 3 earned.

**Enumerate, in the handoff, the four that leave the baseline and why**, because each is a test
that was never failing for a code reason:

- `test_seed_item_economics_configuration.py::test_human_successors_permanently_freeze_bootstrap_basis_and_model`
- `…::test_person_owned_configuration_and_section_membership_are_not_overridden`
- `test_create_shopify_metafield_preferences.py::test_create_uses_client_supplied_id_for_new_preference`
- `test_task_date_field_updates_integration.py::test_update_task_schedule_rejects_invalid_order_and_leaves_row_unchanged`

**One more, flagged not fixed:**
`test_add_task_steps_integration.py::test_adding_a_batch_of_steps_reopens_ready_task` passes when
only the 26 baseline IDs run and fails in a full suite on the same schema-only database — so it
depends on **state another test creates**, not on dev data. It **stays in the baseline**. Record
it in the handoff as the first confirmed order-dependent test, because xdist will be the first
thing in this repository's history to reorder tests. Do not attempt to fix it here.

Any other difference from the 26 is a **finding to explain, not a number to update**.

## 5. C6 — the row currently cannot fail for the right reason (should-fix)

`test_dev_database_counts_are_untouched` asserts the dev database holds exactly
`workspaces: 11253, users: 9809, tasks: 2445, working_sections: 1955`. The criterion asked for
counts **unchanged from before the run** — a *relative* invariant. As written it is absolute, so
it reddens whenever the owner touches their own database (a backfill script is running in
parallel right now), and a red then carries no information: it cannot tell "isolation broke"
from "the owner added a workspace".

Rewrite it to capture the counts **before** the run and assert equality **after**, so the only
thing that can redden it is the suite writing to the dev database. Keep its named mutation
(point the override at the dev database ⇒ counts move ⇒ red) and re-measure it.

## 6. C8 — re-measure cleanly (should-fix)

C8's named mutation **errored rather than reddened**, and left `beyo_test_main9` behind for
manual removal. An errored mutation is a weaker observation than a red one. Re-run it so the
unique-suffix mutant produces a clean red — the row must show the database set *growing* under
the mutation and returning to its original membership under the contract — and confirm no probe
artifact survives.

## 7. Testing

- C1–C6, C8 are **L1/L2**. **C7 is L4** — the new baseline is a full-suite re-enumeration.
- One authoritative L4 stamp at the close, tree identity recorded.
- Every evidence record: hypothesis · scope · command · tree identity · result · ID delta both
  directions. Where a plan divides labour between rows, **state which row did *not* bite**.
- Re-run every named mutation for rows you touch; revert each and verify the revert.
- **Foreign streams are live in this tree.** The owner is working on upholstery
  (`services/commands/task_steps/`, `services/commands/tasks/`, `app/scripts/upholstery_backfill/`)
  — purely additive, verified not to touch step-state closing semantics. Those files are **not
  yours**: do not commit, revert or stash them. If your counts move unexpectedly, attribute
  before concluding.

## 8. Closing protocol

1. The cycle's one L4 stamp.
2. Every named mutation at its site, recorded, reverted, revert verified.
3. Handoff at `handoffs/implementer/2026-08-21_phase1_fix_r2_handoff.md`, charter row schema,
   **cycle-scoped** write perimeter (the files *this session* changed), probe files listed
   separately, and the owner's deliverables (1)–(6), (9), (11), (15) refreshed — in particular
   **(11) every difference from the previous baseline, each one explained**.
4. Review log entry in `plans/plan_1.md`.
5. Checkpoint commit, `CHECKPOINT (not approved):`.

**Why the care here.** Every measurement this organisation makes passes through this instrument.
The r1 design would have kept the suite honest-looking by keeping it coupled to one developer's
database; the owner chose the version that is actually true and happens to be a minute faster.
Hold that line: nothing in this cycle should make a test pass by giving it more data than its
own assertion needs.
