---
plan: phase 2 (schema, models & migration)
role: implementer
round: 3 (fix cycle)
date: 2026-08-12
---

# Session prompt — phase 2 fix cycle r3 (resolve re-review finding B5)

You are the **fixing agent** for phase 2, round 3. One blocking finding; **resolve,
don't relitigate; add nothing beyond the findings.** No schema, migration, or
production-code change — one test fixture branch.
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(branch `fix/idempotent-completion-analytics`). Project folder:
`docs/architecture/under_construction/implementation/item_cost_calculation/`.

## Doctrine (read by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md` — fix-cycle variant.

## Gate check

- Tracker shows phase 2 `CHANGES_REQUESTED` (reviewer r2).
- Read the **reviewer r2** entry in `plans/phase_2_schema_models.md` Review log and
  the handoff `handoffs/reviewer/2026-08-12_phase2_rereview_r2_handoff.md` (B5 +
  notes N8/N12/N13).

## Finding to resolve

### B5 — INV-G1's C2 (a) row uses one group, not two (correction clause verbatim)

> in the `sections_conflict` / `sections_removed` branch, create a **second
> `ProductionCostGroup`** in the same workspace and attach the second
> `ProductionCostGroupSection` to *that* group (same `working_section_id`), so the
> shared key is `(workspace_id, working_section_id)` alone and the group differs —
> exactly the "section active in two groups" the cell specifies. Re-run the
> reviewer's named mutation (widen the index key to include
> `production_cost_group_id` at the DDL site on a disposable DB) and declare that
> `sections_conflict` turns red. Leave `sections_removed`'s `removed_at` clause row
> as it is, but move it onto the second group too so it stays a one-clause delta
> from the corrected (a) row.

The DDL mutation site rule binds (master plan §9 P-G(a) extension): the widening is
applied on a disposable DB built with the §10 from-scratch recipe, reverted, and the
index definition verified restored.

## Optional notes (fixer's judgment; documented if taken)

- **N12:** add `match=` on the index name to C2's nine (a) rows (C3 style).
- **N13:** narrow the numeric-bound row's expected exception from `DBAPIError` to
  the exact `DataError` class.
- **N8** is NOT yours — it touches the migration file (next-touch/phase 9).

## Allowed file perimeter (the re-review verifies this exactly)

- `app/tests/integration/models/item_economics/test_item_economics_schema.py`
- `docs/architecture/under_construction/implementation/item_cost_calculation/plans/phase_2_schema_models.md` (Review log: your fix entry)
- `docs/architecture/under_construction/implementation/item_cost_calculation/master_plan.md` (tracker row Note append only; keep all actor stamps)
- your handoff file

Anything else is an automatic finding.

## Closing protocol

1. Focused module green (79 tests, or 79±N12 additions); B5's named mutation run at
   the DDL site, reverted, declared with the reddened row id (P-I).
2. Full suite green vs baseline (23 known failures, none new); dev DB at head,
   untouched; no disposable DB left behind.
3. Archgraph: zero delta — state it.
4. Tracker row → `IMPLEMENTED` (fix r3, Note appended); Review log entry per P-L
   (state what was built, not only what changed).
5. Checkpoint: `CHECKPOINT (not approved): item-cost phase 2 fix r3 — <summary>`.
   **If you amend the checkpoint for any reason, the handoff must cite the FINAL
   hash** (two prior handoffs cited amended-away hashes — N9).
6. Deposit the handoff at
   `docs/architecture/under_construction/implementation/item_cost_calculation/handoffs/implementer/2026-08-12_phase2_fix_r3_handoff.md`
   (full path — do not resolve relative to the repo root): summary; `⚠ OWNER
   DECISIONS REQUIRED (n)`; the B5 resolution + mutation declaration; write
   perimeter incl. the final checkpoint hash. **Deposit before ending the session.**
