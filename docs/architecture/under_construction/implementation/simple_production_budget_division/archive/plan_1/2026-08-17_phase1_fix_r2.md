---
plan: 1
role: implementer
round: 2 (fix cycle after review r1)
date: 2026-08-17
pipeline: simple_production_budget_division
---

# Fix round 2 — plan 1 (review r1 findings S1–S5 + notes)

Review r1 verdict: CHANGES_REQUESTED — **zero blocking; both mechanisms verified
correct**. Every finding is a probe-confirmed coverage hole: a guarded construction
that can be deleted or corrupted while the phase suite stays green. You close the
five holes, apply the cheap note fixes, and prove each closure with a delta-scoped
observed-red record (ONLY the five new/changed guards — do not re-run the full
ledger; r1c's records stand).

## Read first

1. `handoffs/reviewer/2026-08-16_phase1_review_r1_handoff.md` — findings S1–S5
   (each carries its confirmed probe and its correction), notes N-a–N-g, and the
   "Verified correct" section (settled ground — do not rework any of it).
2. `master_plan.md` §4 (the newly registered `typical_times_statement` shared
   builder) and §6 (five new earned rules — your fixes must satisfy them,
   especially rationale-site and lettered-parts).
3. `planning/intention.md` §5 as amended round 7 (E2's null list now includes
   `actual_worker_seconds` on non-ok tasks — your S-fixes must not "fix" that
   behavior; it is contracted).

## Fix items

- **F1 / S1 (the only production refactor)** — extract the grouped-median
  aggregation into `typical_times_statement(...)` exported from
  `get_working_section_typical_times.py` (registered name, master plan §4);
  `_load_typicals` in `get_task_budget_allocations.py` calls it instead of its
  inline copy (keep E2's ORDER-BY/filter differences OUTSIDE the shared builder).
  Then add the E2 integration row the review specifies: two sections with
  differing typicals (3600 / 1800, ≥5 qualifying groups each), asserting BOTH the
  payload `typical_worker_seconds` values AND the 2:1 allowance split on an
  evaluated task. RED probe: `_load_typicals` → `return {}` must now fail this
  test (this was the review's green-surviving probe — it must bite).
- **F2 / S2** — C14 fixture per the criterion's rationale: add an
  evaluation-less task WITH a primary item + valuation (so
  `resolve_economics_selection` actually runs) and include one such task in BOTH
  the 1-task and 3-task calls. RED probe at the rationale-named site: insert a
  per-task workspace-wide `ProductionCostGroup` query immediately before
  `resolve_economics_selection` (`get_task_budget_allocations.py:229`) — the
  equality assertion must fail.
- **F3 / S3** — third key-set assertion:
  `set(serialize_budget_step(...))` exact-equality against its full key set, and
  extend the `"money"/"minor"` scan across `steps[]` keys. RED probe: add
  `"consumed_cost_minor": 4321` to `serialize_budget_step` — must fail.
- **F4 / S4** — service-identity assertion on the C15 E1 row:
  `calls[0][0] is working_sections.get_working_section_typical_times`
  (precedent `test_item_economics_router.py:133`). RED probe: move the E1
  declaration below the `/{working_section_id}` route — must fail.
- **F5 / S5** — on the C13 fixture, call `get_task_budget_status` and assert
  `actual_worker_seconds` equality with E2's value.
- **F6 / notes** — N-a: complete the two README detail sections with the
  `#### Request Body` and `#### Responses` blocks matching the file's house
  format, and move the E2 section into path order among the item-economics
  entries. N-b: re-word or relocate the mirror-test comment so it no longer
  claims budget-allocations picks a worker service. N-c: remove the dead
  `_binding` computation (or serialize it — removing is right; `item_binding` is
  not in the §5 contract). N-f: hoist the excluded-state value set above the
  loop. N-g: restore alphabetical import order. (N-d/N-e: add the two missing
  assertions — `share_state` on C3's zero-worked row; pin both steps' integers
  in C5b — cheap, do them.)

## Discipline

- Delta-scoped ledger: exactly the five F1–F4 red probes (F5/F6 need none) —
  mutation (byte-reproducible), test node, exact red line, revert. Nothing else
  re-probed.
- Lettered-parts rule: update the criterion→test map with C17a/b/c-style lettered
  rows for the compound criteria (C13, C14, C15, C17) so partial coverage can
  never again read as green.
- Full suite: `PYTHONPATH=. pytest -q -m 'not e2e'` — expected 23 baseline + 3
  foreign, zero others; totals + list in the handoff.
- Perimeter: the two query services (F1 refactor only), `division_serializers.py`
  untouched (F3 is a test change; the serializer is correct), the four phase test
  files, README + mirror-test comment (F6, addition/re-word only), and pipeline
  docs. No graph changes.

## Checkpoint + handoff

`CHECKPOINT (not approved): plan1 fix r2 — close review r1 coverage holes`.
Handoff: `handoffs/implementer/2026-08-17_phase1_fix_r2_handoff.md` — frontmatter
(`round: 2, state: IMPLEMENTED`), checkpoint hash, write perimeter, F1–F6
outcomes, the five-probe delta ledger, updated lettered criterion→test map, suite
totals + failure list, STOP items if any fix reveals a genuine behavior defect.
