---
plan: phase 4B (category-driven group selection)
role: implementer
round: 2 (fix cycle r1 — resolve review r1: B1, S1, S2; corrections pre-verified)
date: 2026-08-13
---

# Session prompt — phase 4B fix cycle r1 (B1 cold-build cleanup, S1, S2)

You are the **fixing agent** for phase 4B. Every correction below was verified
by the r1 reviewer (each gap proven by a probe that stayed green, or by a
reproduced defect). **Resolve, don't relitigate.** Three items — nothing else.
Workspace: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
(commands from `backend/app/`, per master plan §10).

## Doctrine (read by absolute path)

1. `/Users/davidloorenz/agent-skills/pipeline-charter.md`
2. `/Users/davidloorenz/agent-skills/implementation-executor.md` — fix-cycle variant.

## Gate check

- Tracker: phase 4B `CHANGES_REQUESTED` (review r1).
- Read the review r1 handoff
  (`handoffs/reviewer/2026-08-13_phase4b_review_r1_handoff.md`) and the plan's
  **"Fix r1 amendments" block — GOVERNING** (it carries the new criterion C9,
  C1's new row (e), and C6's shape restatement verbatim).
- **Owner authorization (card 1, OPTION ONE):** ONE more edit to
  `app/migrations/env.py` — commit the cold-build cleanup. That file only,
  this cycle only. **N6 is NOT yours** (partial-target cold-build crash —
  routed to the migration-infrastructure owner separately). Do not touch the
  OD-1 rollback lines themselves: P4B-0a proved them load-bearing.

## Items (each with its reviewer-verified correction)

### B1 — commit the cold-build cleanup (`env.py`, the authorized edit)

`cleanup_cold_build_workspace()` runs in the `finally` AFTER the per-migration
transactions commit; its two DELETEs autobegin a fresh implicit transaction
nothing commits, so every freshly built database ships "Migration workspace"
+ 7 pause reasons. Correction (reviewer-supplied): `connection.commit()` as
the last statement of the `finally`, or wrap the two DELETEs in
`with connection.begin():`. Then discharge **criterion C9** (plan amendments):
§10's recipe on a disposable DB ends at head `5caae620088c` with zero
`workspaces` / zero `pause_reasons` / zero `mig_cold_build_workspace` rows —
**state queries, never exit codes** (review L5). Named mutation (disposable):
reverting your edit must re-produce the ghost (1 workspace / 7 pause reasons)
— run, observed, reverted. Configured DB at head throughout (rule 7).
Finally, update §10's from-scratch entry (master plan) with the newly
VERIFIED facts — the coordinator has already corrected its history note;
append your verified end-state (db name, timing, row counts).

### S1 — model-side predicate structural row (C1 row (e), P-X)

`compare_metadata` is blind to the partial-index predicate: the reviewer
deleted AND inverted `postgresql_where` on the model and all 7 schema-test
rows stayed green. Add the structural row to
`test_phase4b_category_schema.py`: the named index in
`ProductionCostGroup.__table__.indexes` carries
`dialect_options["postgresql"]["where"]` textually `is_deleted = false`.
Both r1-green probes must now bite: (i) delete the predicate → row red;
(ii) flip to `is_deleted = true` → row red. Model file itself is CORRECT —
no production change; mutations are apply/run/revert probes only.

### S2 — the shared-model status row becomes exact-dict (P-Y)

`test_status_shared_model_failure_is_repeated_in_each_category_block`
(`test_phase4b_category_selection.py`) currently asserts only the two
`first_failure`s + `has_open_cost_model_version`; the reviewer collapsed
`has_open_basis_version` to `has_open_basis and evaluable` in the query and
256 stayed green. Rewrite it as an exact-dict-equality assertion over the
WHOLE payload (the C6(a)/(d) idiom). The reviewer's Probe B mutation must now
redden it — run, observed, reverted (the query itself is correct; nothing
ships in it).

## Ride-alongs

- **N5 (graph):** your closing graph delta corrects the `domain-item-economics`
  source-link imprecision — the link labelled `symbol:
  resolve_economics_configuration` spans `configuration.py:12-82` (the whole
  module surface). Record an accurate source link for the function's own span
  (additive; never adjudicate the 2 pending items).
- **Do NOT take:** N3/N4 (routed to phase 8), N6 (migration-infra owner).

## Ledger requirements (P-I as extended THIS round — review L3/N1)

Every mutation row declares: file (REAL path), sha256 pair (main → mutant),
and the **FULL observed red set** from the run that produced it — if the
observed set diverges from the plan's predicted radius, **flag the divergence
explicitly in the ledger**, never narrow the declaration to the prediction
(r1's M2 declared 1 node where 7 reddened; the narrowing hid design
information). Cite the checkpoint's FINAL hash in the handoff (deposited
AFTER the checkpoint).

## Perimeter

`app/migrations/env.py` (the single authorized production edit);
`tests/integration/models/item_economics/test_phase4b_category_schema.py`;
`tests/integration/services/commands/item_economics/test_phase4b_category_selection.py`;
master plan (§10 verified-facts append + tracker row, stamps preserved); the
4B plan Review log; your handoff. **No other production file** — if a
correction seems to need one, stop and report.

## Closing protocol

1. All rows green; C9's manual disposable evidence in the Review log; every
   mutation run/reverted with observed node ids + sha256 pairs.
2. Full suite: expect 1926+N passed / 23 known failures byte-identical / 1
   deselected (N14 caveat); focused suite twice; residue check with SCOPE
   stated (economics tables); ruff on changed files; configured DB at head
   `5caae620088c`; every disposable DB dropped (list them).
3. Archgraph: the N5 source-link delta only; state revision before/after;
   2 pending items untouched.
4. Tracker → `IMPLEMENTED` (fix r1); Review log per P-L.
5. Checkpoint `CHECKPOINT (not approved): item-cost phase 4B fix r1 —
   <summary>`; handoff AFTER, citing the final hash, at
   `docs/architecture/under_construction/implementation/item_cost_calculation/handoffs/implementer/2026-08-13_phase4b_fix_r1_handoff.md`
   (full path). `⚠ OWNER DECISIONS REQUIRED (n)`; full write perimeter +
   probe declaration.
