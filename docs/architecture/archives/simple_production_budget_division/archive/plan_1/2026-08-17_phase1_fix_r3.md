---
plan: 1
role: implementer
round: 3 (fix cycle after re-review r2)
date: 2026-08-17
pipeline: simple_production_budget_division
---

# Fix round 3 — plan 1 (S6 + README notes; test/docs only)

Re-review r2 closed everything from round 1 and found one new should-fix plus two
documentation notes. This round is small and surgical: two exact assertion values,
one README section move, two README/mirror text repairs, one fixture rename. No
production code changes of any kind.

## Read first

1. `handoffs/reviewer/2026-08-17_phase1_rereview_r2_handoff.md` — S6 (with the
   exact measured values), N-i, N-j, N-l.
2. `master_plan.md` §6 — the two rules this round satisfies:
   **no-weaker-assertions** and **fixture-property-pin**.
3. `plans/plan_1.md` — the new lettered rows (C14b is what S6 guards).

## Fix items

- **F1 / S6** — `tests/integration/services/queries/item_economics/test_budget_allocations_query.py:192-193`:
  replace both `!= "ok"` assertions with the exact resolver-produced values the
  reviewer measured on the current fixture:
  - one-task call (the evaluation-less task): `status == "not_configured_no_cost_group"`
  - three-task call: the evaluated task `== "ok"`; the evaluation-less task
    `== "not_configured_no_cost_group"`.
  These are C14b's fixture-property pins: `not_configured_no_cost_group` is
  produced only by the resolver path, so the degenerate fixture (no PRIMARY item →
  `not_evaluated` short-circuit) cannot satisfy them. RED probe (the reviewer's
  probe 5, now required to bite): remove `no_item_task_item` / `no_item_valuation`
  from the fixture's `add_all` — the exact-status assertions must fail. Record
  mutation → node → red line → revert.
- **F2 / N-i** — `beyo_manager/routers/README.md`: move the
  `### GET /api/v1/item-economics/tasks/budget-allocations` detail section (at
  `:3996`, currently wedged inside the working-sections block) into the file's
  path order for `/api/v1/item-…` (before the item-upholsteries section at
  `:1893`). Content unchanged; placement only.
- **F3 / N-j** — two repairs: (1) add the missing markdown header-separator row
  to the E2 422 response table (E1's block at `:4070` shows the house format);
  (2) in `test_phase9_item_economics_route_mirror.py`, restore the dropped v1
  sentence — that the budget-status handler picks the money-free worker service
  for WORKER and SELLER — as its own comment line above the budget-status row
  (leave the re-worded budget-allocations comment as is).
- **F4 / N-l** — in the E2 test file (you are touching it anyway): rename the
  `no_item_task` / `no_item_item` / `no_item_task_item` / `no_item_valuation`
  family to `unevaluated_*` names that say what the task lacks (an evaluation,
  not an item).

## Discipline

- **No-weaker-assertions rule**: this round may only strengthen assertions; diff
  your own change for any `==` → `!=`/`in` degradation before handoff.
- Delta ledger: exactly ONE probe (F1's). Full suite:
  `PYTHONPATH=. pytest -q -m 'not e2e'` — expected 23 baseline + 3 foreign, zero
  others; totals + failure list in the handoff.
- Perimeter: the E2 test file, `routers/README.md`,
  `test_phase9_item_economics_route_mirror.py` (comment only). NOTHING else — no
  production files, no graph changes.

## Checkpoint + handoff

`CHECKPOINT (not approved): plan1 fix r3 — pin C14b fixture property + README order`.
Handoff: `handoffs/implementer/2026-08-17_phase1_fix_r3_handoff.md` — frontmatter
(`round: 3, state: IMPLEMENTED`), checkpoint hash, write perimeter, F1–F4
outcomes, the one-probe ledger, suite totals + failure list.
