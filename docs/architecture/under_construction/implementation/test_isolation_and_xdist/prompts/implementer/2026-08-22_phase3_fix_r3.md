---
plan: 3
role: implementer
round: 3
date: 2026-08-22
project: test_isolation_and_xdist
---

# Session prompt — plan 3 fix r3, `test_isolation_and_xdist`

## 1. Role and mode

Single-purpose cycle: **ship the parallel default the owner asked for**, and prove the shipped
configuration reaches it. This is not a findings round — fix r2's findings are closed and verified.
It exists because an owner decision landed after r2 closed.

**Resolve exactly task 10 and criterion C8. Add nothing else.**

Workspace root: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
Suite from `backend/app/`. Branch `feat/test-isolation-xdist`.
Fix r2 is checkpointed at **`00ea07b`**; the coordinator's fold is on top of it.

**Read first, by absolute path, as this session's doctrine:**
`/Users/davidloorenz/agent-skills/pipeline-charter.md` (including **"Test-evidence scope and
reuse"** and **"The owner layer"**) and `/Users/davidloorenz/agent-skills/implementation-executor.md`.

**Read then:** `planning/intention.md` **OD-10** in full — it carries the decision, the reasoning,
and the three structural reasons the count is six and not `auto`. Then `plans/plan_3.md` **§4A and
§4B**, which are your task and your criterion. **The plan file is your task list; where this
prompt differs, the plan file wins.**

## 2. The decision you are implementing

The owner was shown a card recommending the default stay serial for now and answered:

> *"I will like to make it default because that is the whole point of this implementation, to have
> what was being tested faster and what will be build part of that speed."*

OD-9's escape condition — *the default stays serial unless the parallel failing-ID set matches the
serial comparator exactly* — was met by fix r2. **OD-10 records the answer and supersedes OD-9's
default clause only.** Do not relitigate the count, the distribution mode, or the decision. If you
believe six is wrong, say so in the handoff as a decision card and implement six anyway.

## 3. Settled ground — cite it, do not re-derive it

Tree-bound to `00ea07b`, coordinator-verified. **Re-running any of it is over-evidence and a
finding against the round.**

- **Serial comparator: `21 failed / 2575 passed / 1 deselected`**, the phase-2 21-ID set, `comm`
  empty in both directions. Enumerated in
  `handoffs/implementer/2026-08-22_phase3_fix_r2_handoff.md`.
- **All three parallel rows already match it** — `-n 2`, `-n 4`, `-n 6`, each `21 failed / 2574
  passed`, all `--dist loadfile`. Wall times 70.26 s / 51.04 s / 47.33 s.
- **Connection peaks 21 / 23 / 25 against `max_connections = 100`** — r1-measured and carried;
  master plan §6.3a is authoritative.
- **Collection at `00ea07b` is 2596 IDs**, criterion module 50, probes filtered out when
  `BEYO_TEST_COLLECTION_PROBE` is unset. Coordinator-measured.

## 4. Task 10 — the default

`app/pytest.ini`'s `addopts` gains **`-n 6 --dist loadfile`**.

Both parts are load-bearing. `loadfile` is the only distribution mode this suite has ever been run
under; the per-test `load` default has never been measured here and file-level fixtures have never
been exercised against it. Six is the highest count ever measured on this machine.

**Do not use `-n auto`.** It is 14 workers here. OD-10 gives three reasons; the one most likely to
surprise you is the third: **every worker serialises through `_template_operation_lock` to copy
the template at startup**, so startup cost is linear in worker count and worker 14 waits behind
thirteen copies. That crossover has never been measured.

Document writes that follow from it, all yours:

- **§6.1's command table** — the shipped default becomes the parallel invocation; the serial
  command stays, named as **the comparator**, not deleted.
- **§6.3a** — record that the shipped default sits at a peak of 25 of 100, and restate §6.3a's
  standing rule: raising the count needs a measurement, not an edit.
- **§8's phase-3 row** — state **both** baselines, parallel and serial, each with the tree it was
  measured on. The row currently carries `00ea07b` plus the caveat that the parallel rows were
  taken one criterion row before the closing tree; your re-measurement retires that caveat.
- `plans/plan_3.md` §7 — your review-log entry.

## 5. Criterion C8 — and the trap inside it

Plan §4B is the contract. The short version: a row that reads `PYTEST_XDIST_WORKER`, run under the
shipped default **with no `-n` on the command line**, asserts a `gw<n>` worker is present.

**The trap, and it is the whole point of the criterion.** The row's subject is *the configuration*,
not the resolver. If it can pass because you happened to type `-n 6` when running it, it proves
nothing about what ships. This is the row-that-cannot-fail class, and this project has recorded
fifteen instances of it — the sixteenth would be this one, written by the criterion meant to
prevent it.

Two consequences you must handle:

1. **Run the mutation from the plan at its named site** — remove `-n 6` from `addopts` in
   `app/pytest.ini`, run the criterion module with **no `-n` on the command line**, and record both
   sides. Green under the shipped default, red with the entry removed. A mutation run with `-n 6`
   still on the command line has measured nothing.
2. **The row must not be satisfiable by the serial comparator run.** The comparator is
   `PYTHONPATH=. pytest -m 'not e2e' -p no:cacheprovider` style — serial, on purpose. If C8's row
   would redden the serial comparator run, it makes the comparator unusable. Decide how the row
   distinguishes "the shipped default is not parallel" from "this run deliberately overrode it,"
   state the mechanism and its reason in the review log, and make sure the comparator still runs
   clean. **This is a real design question, not a formality** — if you cannot resolve it cleanly,
   a decision card is the correct output.

## 6. Evidence budget

**This session's L4 budget is exactly 3 runs**, all on the tree you hand over:

1. **The closing stamp at the new shipped default** — bare `PYTHONPATH=. pytest -m 'not e2e'`,
   which now means six workers. This is the phase's authoritative baseline from here on.
2. **A second run at the shipped default** — C5's second scheduling condition. Under a parallel
   default this matters more than it did serially: work distribution varies run to run, so two
   runs are the minimum evidence that the failing-ID set is a property of the suite and not of one
   scheduling.
3. **The serial comparator**, explicitly overridden with `-p no:xdist` or `-n 0` — whichever you
   choose, name it in §6.1 as the comparator command. It keeps the two modes comparable.

Each carries tree identity — SHA plus asserted `git status --porcelain`, or SHA plus a `git diff`
digest if dirty — the enumerated failing-ID set, and the delta in **both** `comm` directions.
**State your L4 count as a number.** Anything beyond these three needs the charter's authorization
line, written before the run.

Everything else is L1/L2, including C8's mutation at its named site.

Record the `pg_stat_activity` peak of the closing run if the monitor is still wired; if it is not,
say so and cite §6.3a's carried 25 rather than implying a measurement you did not take. r2 carried
those peaks without re-measuring them and labelled them as carried — that was the right call and
the right disclosure.

## 7. Scope fences

- **Nothing under `app/beyo_manager/`.** Nothing in `conftest.py`, `database_isolation.py`, the
  probe modules, or either requirements manifest. `app/requirements.txt` is byte-identical to
  `c73c017` and stays that way — restoring that was a blocking finding one round ago.
- **Allowed perimeter:** `app/pytest.ini`, the criterion module
  `app/tests/integration/infrastructure/test_database_isolation.py`, `master_plan.md`
  (§6.1, §6.3a, §8), `plans/plan_3.md` (§7 and frontmatter), and your handoff. The re-review
  verifies this against `git diff`; anything else is an automatic finding.
- **Do not re-open tasks 1–9 or C1–C7.** They are implemented, fixed, and coordinator-verified.
- **Do not change the failing-ID set.** If the parallel default reddens something new, that is a
  finding to report, not a thing to repair inside this cycle — and it is important enough to stop
  for, because it would mean six workers behaves differently from the six-worker run r2 measured.
- **The three archgraph items stay `human_confirmed`.** Record any delta additively; never
  promote, reject or edit. There are 2 pending items from r1 awaiting the coordinator — leave them.
- **A published handoff is never edited.** Corrections go in yours.

## 8. Closing protocol

1. **The closing L4 stamp at the new shipped default, on the tree you hand over**, with tree
   identity, the enumerated failing-ID set, and the delta in both directions against the serial
   comparator. Master plan §5 **rule 9**: change anything after stamping and the stamp is void;
   re-taking it is not over-budget.
2. **C8's mutation at its named site**, both sides recorded, run with no `-n` on the command line.
3. **Document writes** per §4 above. The master plan §3 tracker row is the coordinator's.
4. `plans/plan_3.md` frontmatter → `state: IMPLEMENTED` with date, actor, and the new counts under
   the parallel default.
5. **Review-log entry** in `plans/plan_3.md` §7: what you built, C8's design decision from §5.2
   with its reason, and anything a reviewer needs.
6. **Handoff** at `handoffs/implementer/2026-08-22_phase3_fix_r3_handoff.md`, frontmatter
   `plan: 3` / `role: fix` / `state` / `date` / `actor`. Body: the change, the counts, your
   **cycle-scoped** write perimeter, mutation-probe files listed separately with checksums, every
   probe database and its verified disposition, and **your L4 count as a number**.
7. Anything only the owner can settle goes in the handoff as a **decision card** in the charter's
   `⚠ OWNER DECISIONS REQUIRED (n)` section — §5.2 is the likely candidate. If nothing needs the
   owner, one line saying so.
8. Your final chat message follows the charter's **owner layer**: what you did, what it means for
   the owner in plain words, what happens next, what needs them. Not a paste of the handoff.

The handoff file, not your chat message, is what the coordinator consumes.
