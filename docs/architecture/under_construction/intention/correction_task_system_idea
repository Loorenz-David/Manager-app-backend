notes:


serializing a task for a worker: 

AFTER IMPLEMENTATION:

✅ the compact serialization of the task when obtaining the task step objects it gets the state of a task step, it should get also the latest task step record, under latest_state_records:{id, step_id, state, reason, entered_at, exited_at, created_at, created_by_id, description, accuracy, accuracy_measured_by, taken_from_average}





----- 
✅ the creation of a task step should allow for assigning a worker on the same creation process. if no worker is provided and the working section has a single worker then it autoassigns to that worker, if multiple workers then it leaves unassigned and the frontend can assign.
because creation of task allows for creation of task steps, that means that the payload accepts worker assignment for the working section that is getting passed for assigning. 
----- 


✅ implement image system for items  


--------

✅ implement case system for task. it is already created, it just needs adjustems

--------

✅ changing the work station dependencies should run a safe recreation of task step dependencies, that way updating the work station dependencies keeps the already created task steps dependencies updated.

--------

we need to add record tables to item upholstery, item upholstery requirements, item, task and add them to the serialization of a task record below.


✅ we can build a message builder utility for the record history polymorphic system:
update message, it takes a list of strings ( columns that got update ), a target in string format and writes, a username
"David updated the description on Item"

if multiple columns it truncates:

"David updated description, price, width ... etc on Item"

delete message, it takes the target of deletion, major target, plural or singular, and username

"David deleted a Note from Item"

"David deleted multiple Notes from Item"

create message, it takes a creation target, a major target, plural or singular, username

"David added a Note to Item"

"David added mutliple Issues to Item"


--------


✅ we will create history records for:

task commands record to task entity type: /Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend/app/beyo_manager/services/commands/tasks


item commands record to item entity type : /Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend/app/beyo_manager/services/commands/items






--------


✅ we need a get task costumer, which will retrieve the customer information linked to a task:

task_costumer: {
    primary_phone_number,
    secondary_phone_number,
    primary_email,
    secondary_email,
    address
}


--------
✅ we need a get task flow, which will retreat the history flow of a task, this flow will have a mixture of multiple tables records all standarized to a common format, for example shape:
example task id 1: [
    {
        type:
        description:
        entity_type:
        entity_id: 
        happend_at: iso datetime
        by: id

    }
]

task and item polymorphic history records , type will be the change_type of history record: created, deleted, updated, description will be description, entity_type will be history_record, entity id will be the id of that record
happend_at will be created_at, by will be created_by 

StepStateRecord , for this record we will include the entered_at as happend_at, entity_type will be step_state_record, entity id will be the id of that instance,
the description will be : function that generates a message from the record:
"David marked it as working"
if it is paused then the function accepts a reason:
f"{username} {state} it because of {StepEventReasonEnum with out "_"}.

by will be the created_by


--------


✅ we need a get task images flow which will retrieve all the images from task and item in compact shape:
task_images: [
    {
        id,
        image_url
        width_px
        height_px
        file_size_bytes
    }
]

✅ we need a get task images flow, which will retreive all the images from task and item in full shape. 

task_images: [
    {
        id:
        image_url:
        source_type:
        width_px
        height_px
        file_size_bytes
        created_at
        created_by: {username, id}
        image_annotations:[{
            id
            image_id,
            annotation_type
            data,
            accuracy,
            created_at
            created_by: {username, id}
        }]
        last_event:{}
    }
]

we need a get single task image flow, which will retreive a single image with all the details and annotations.


--------


--------
✅ implement socket layer to comunicate changes and notifications when task steps, change state.

implement socket later when upholstery requirements change state, to update the linked task steps in real time on the frontend.

implement the socket layer when item info changes

implement the socket layer when task details change

--------

implement notification system for commands of value:

task step assignment to worker

upholstery requirement state changes

this notifications are push also through the vapid notifiaction system

--------

implement email 


