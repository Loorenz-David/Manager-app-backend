---
plan: 1
role: implementer
round: 2
date: 2026-08-19
project: simple_valuation_editor
kind: fix cycle
---

# Session prompt — fix r2, phase 1 (`simple_valuation_editor`)

## 1. Role and scope

Review r1 returned **`CHANGES_REQUESTED`**: 0 blocking, 2 should-fix, 7 notes. **Resolve the
two should-fix findings and nothing else.** Resolve, don't relitigate; add nothing beyond the
findings.

**The arithmetic was found correct.** The reviewer re-derived every published number from a
reference implementation written from the intention alone, without importing your module —
all 22 values matched, and 18 of its own 22 mutations bit. Do not "improve" any computation.

Workspace root: `/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/backend`
Application root: `backend/app/` — run every command from here.

Doctrine, by absolute path: `/Users/davidloorenz/agent-skills/pipeline-charter.md` and
`/Users/davidloorenz/agent-skills/implementation-executor.md`.

## 2. Allowed file perimeter — the re-review verifies this with `git diff`

| Path | |
|---|---|
| `app/beyo_manager/domain/item_economics/price_scenario.py` | comment only, see F1c |
| `app/tests/unit/domain/item_economics/test_price_scenario.py` | one new test |

**Anything outside this is an automatic finding.** No other application file, no plan, no
intention, no master plan — the coordinator has already folded every note upstream.

## 3. F1 — the `max(1, quantity)` guard has no test (should-fix)

**The finding, verbatim from the reviewer:**

> `price_scenario.py:190` computes `divisor = max(1, quantity)`. Replacing it with
> `divisor = quantity` — the whole guard removed — leaves **all 52 tests green**. No
> criterion in plan 1 §4 covers the divisor, and no fixture passes a `quantity` below 1.
> Under that mutation, `slider_domain(B, 0, I)` calls `two_significant_digits(B, 0)`, which
> hits `if b <= 0: raise ValueError("b must be positive")` (`price_scenario.py:176`) — an
> unhandled `ValueError`, i.e. a 500 from phase 2's endpoint rather than a band.

**Why the input is real, not hypothetical.** Intention §2.7: *"`items.quantity` is
`Integer, nullable=False, default=1` … with **no CHECK constraint** … an **application**
invariant, not a storage one: a row written before those validators existed could in
principle hold `0`."* §9.4 names the consequence. §7A.1 states the guard as part of the M5
contract, which plan 1 task 8 assigns to `slider_domain`.

**F1a — the row.** The reviewer's suggested correction, verbatim:

```python
def test_quantity_zero_falls_back_to_a_divisor_of_one() -> None:
    assert slider_domain(1_211_335, 0, 29) == SliderDomain(
        step_minor=15_000, min_minor=420_000, max_minor=1_650_000
    )
```

**Named mutation, with its site:** in `slider_domain`'s **definition** in
`price_scenario.py`, `divisor = max(1, quantity)` → `divisor = quantity`. This row must turn
red. **Both sides already computed and confirmed independently by the coordinator:** contract
`SliderDomain(step_minor=15000, min_minor=420000, max_minor=1650000)`; mutation
`ValueError: b must be positive`.

**F1b — strengthen the row so it says what it means.** Note that the expected literals above
are *identical* to C16's `Q = 6` band — at this `B` the `Q = 1` and `Q = 6` bands coincide.
The row therefore bites on the **exception**, but its literals do not express what the guard
*does*, which is clamp to one. Add one companion assertion in the same test making the clamp
explicit:

```python
assert slider_domain(1_211_335, 0, 29) == slider_domain(1_211_335, 1, 29)
```

This is the whole content of the guard, stated directly. A future change to `max(1, …)` that
clamps to something other than 1 would pass the literals and fail this line.

**F1c — the only production edit permitted this round.** A short comment at
`slider_domain`'s `divisor` line pointing at §9.4 and §2.7, so the next reader learns the
guard exists for a storage-level gap rather than for tidiness. **Do not change the
expression.** The guard is correct; only its evidence was missing.

## 4. F2 — the mutation ledger's observation column (should-fix)

**The finding, verbatim:**

> Three rows record fewer reddened tests than the file actually produces … Every extra
> failure is correct and desirable … **No defect is hidden by the inaccuracy.** The defect is
> in the record, and the pattern (three rows, all understating, all consistent with a
> `-k`-filtered run) is what the coordinator's C10 observation predicted.

**Correction, verbatim from the reviewer:**

> Re-apply each of the six mutations, run
> `tests/unit/domain/item_economics/test_price_scenario.py` **whole** (no `-k`, no node id),
> and record the complete observed-red set per row. The measured sets above are usable as-is.

The measured sets, for your ledger:

| Ledger row | Recorded in r1 | Actual, whole file |
|---|---|---|
| C2 truncation | 2 | 2 — accurate |
| C7 tightness (`n+1 → n`) | 1 | 1 — accurate |
| C7 mechanism (float multiply) | 1 | 1 — accurate |
| **C8** shortcut conversion | 1 | **5** — `test_c8_…`, `test_c9_mockup_break_even_uses_the_exact_integer_search_literal`, `test_c10_break_even_search_is_independent_of_the_slider_band`, `test_c12_fixed_deduction_boundary_and_domain_floor_are_exact`, `test_c12_purely_proportional_mockup_still_runs_the_search` |
| **C10** cap `2**40 → 2**33` | 1 | **2** — adds `test_c12_degenerate_model_publishes_the_exact_infeasibility_cap` |
| **C17** derive-then-snap | 1 | **3** — adds `test_c12_fixed_deduction_boundary_and_domain_floor_are_exact`, `test_c18_minimum_resolves_disagreeing_floor_constraints` |

**You must re-measure rather than copy this table.** The point of the finding is that an
observation is measured; inheriting someone else's numbers reproduces the defect in a new
shape. Re-apply all six, plus F1a's, running the file whole each time, and record what you
observe. If your measurement disagrees with the table, **that disagreement is the report** —
say so, do not reconcile it silently.

**This is now a standing project rule** (master plan §5): *a mutation ledger's observation is
a property of the whole file, not of the test you were watching.* An unexpected reddening is
exactly the signal the ledger exists to surface, and a filtered run cannot produce it.

## 5. Notes — resolved without you, listed so you do not act on them

The coordinator has already routed all seven. **None is work for this round.**

- **N1** (order-dependent shape validation) → folded into **intention §3.1B**. The behaviour
  is sound; a sentence was missing. **Do not change `collapse_terms`.**
- **N2** (C19's `>=` equality unpinned) → **accepted**, unreachable on realistic data. Not
  worth a round.
- **N3** (C13 cannot isolate its predicate) → **closed**; plan task 4 already said so.
- **N4** (unreachable `P = 0` pre-check) → **closed. Keep the branch** — §4.2A mandates it.
  Do not "clean it up".
- **N5** (`_shape_error` duplicates `calculator.py:124-128`) → **sanctioned** in master plan
  §4, with a third copy forbidden in phase 2. **Not a fix**: the comment pointing at the
  other site lands with phase 2's edit, not here, because adding it now would touch a line
  this round has no finding on.
- **N6** (C21's AST walk misses relative imports) → carried to **plan 2**; theoretical here,
  the package has zero relative imports.
- **N7** (`digits` is public) → registered **internal to phase 1** at closeout.

## 6. Mutation discipline

Mutate at the named definition site → run the **whole file** → **record every test that
reddens** → revert → confirm `sha256` byte-identical. The r1 hashes for reference:

```
91dbceb4d00cb07d6ccbd558ea0e4f28fe64c5bc8754ae27ba04d96afe5bc99e  price_scenario.py
560dd0d258c95ffb402dde4c2b1235e49d73048af0862dc040b502c8e66601c9  test_price_scenario.py
```

Both will change legitimately this round — `price_scenario.py` by F1c's comment,
`test_price_scenario.py` by F1a/F1b's test. Report the new hashes.

## 7. Environment

- Commands from `backend/app/`; `PYTHONPATH=. pytest -m 'not e2e'`.
- **Current: 2372 passed / 26 failed / 1 deselected**, measured independently by both the
  coordinator and review r1. This round adds **one** test → expect 2373.
- **A single run is not evidence.** If your failure count disagrees with 26, repeat and diff
  the **ID sets** before concluding anything. A count alone is noise.
- `ruff check` and `ruff format --check` both files before finishing; they were clean at r1
  and must stay clean.

## 8. Closing protocol

1. Full suite; record before/after.
2. Handoff at `handoffs/implementer/2026-08-19_phase1_fix_r2_handoff.md`, charter
   frontmatter.
3. **Full write perimeter by path**, generated from `git status --porcelain
   --untracked-files=all` and `git diff --name-only` — never retyped.
4. **Checkpoint commit**, `CHECKPOINT (not approved):` prefix, standing authorization.
5. Do **not** touch the master plan tracker or plan 1's Review log.

## 9. The handoff must contain

- **F1**: the new test, its two assertions, and the named mutation with **both sides
  computed** and the complete observed-red set.
- **F2**: the corrected six-row ledger, **re-measured by you**, each row listing every test
  that reddened — plus an explicit statement that each run was the whole file with no `-k`.
  Flag any disagreement with §4's table rather than reconciling it.
- Confirmation that **no note from §5 was acted on**.
- New `sha256` for both files, and the revert hashes for every probe.
- Suite counts before and after.
